# phase-reports

本目录保存 phase 报告。当前项目仍处于 pre-1.0，phase 使用 `v0.<minor>.0`，例如 `v0.2.0` 到 `v0.3.0` 级别的阶段主线复盘。

只有当多个 stage 已经构成一次大版本完成或路线转向时，才在这里新增 `v0.<minor>.0_<theme>.md`。普通阶段总结不要放在这里。

已完成 phase：

- `v0.2.0_experiment_productization_closeout.md`
- `v0.3.0_product_usability_buffer_closeout.md`
