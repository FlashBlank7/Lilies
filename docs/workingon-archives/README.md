# workingon-archives

本目录保存已经归档的小版本 workingon 中间材料。

规则：

- 按版本目录归档，例如 `v0.2.18/`。
- 本目录可以保存 work file、implementation evidence、question log、临时分析和历史中间结果。
- 它不具备下一阶段指导权。
- 下一阶段任务只从 `docs/stage-reports/` 读取。
- 当前 active `docs/workingon/` 在归档后必须只剩 README。

当前归档：

- `v0.2.2/` 至 `v0.2.23/`
