# 官方智能体与内部复用

员工界面统一称为“官方智能体”。具体接入实现只展示在管理员设置中。现有 API 项目、工作流模型连接与历史结果保持独立。

## 配置和使用

1. 管理员打开全局导航的“官方智能体”。可以连接服务器已有的订阅登录，也可以打开浏览器登录或设备码登录；不收集账号密码。
2. 刷新模型和额度目录，选择实际支持的模型及思考强度。默认配置为 `gpt-5.6-luna / max`；不存在时显示具体原因，绝不降级或自动换模型。
3. 初始并发 1、个人额度保留 50%、每任务 32000 token、模型执行时限 600 秒。等待平台计算时不占用新的模型调度名额。token 根据服务返回的实际用量控制，正在生成的响应可能超过阈值。
4. 管理员在项目对话的“智能体来源”中明确允许完整智能体能力、授权官方智能体，并选择任务与工作流生成的来源。新旧项目默认均不授权。
5. 员工进入自己的会话，选取项目资料后发送消息。排队原因、运行和等待计算状态会显示在对话中。停止只影响此请求及其关联计算。
6. “创建工作流”也可以使用官方智能体。生成请求立即返回任务编号，刷新可接续查看、可以停止；只返回工作流结构，不开放业务工具。API 生成路径保持兼容。人工同时修改画布时仍以修订号检查冲突。

工作流中的 `llm`、视觉和 Embedding 继续使用原始 API。官方智能体不伪装成这些模型积木的服务商；配置业务 API 不会替换官方智能体的员工会话。

账号凭据放在平台数据目录中的私有服务目录。每个会话独立存储 thread，只有服务登录材料被引用；不会读取个人 Codex 配置、历史、记忆、插件或工作目录。子进程不继承 `OPENAI_API_KEY` 或 `OPENAI_BASE_URL`，使用订阅认证并拒绝 API Key 模式。模型出口仍遵守服务端 `MODEL_EGRESS_ENABLED`；自动测试保持关闭。管理员的“启用”开关不越过这个部署设置。

## 排队、恢复与额度

SQLite 保存请求队列。按员工轮换，同一会话的模型请求串行。客户端重试保留请求标识，重复请求不重复启动。训练由已有项目计算任务执行，不建立第二套训练运行时。

任何官方额度窗口达到保留线、服务返回不允许继续使用、账号失效或额度未知时，新请求保持等待。已开始的任务不因保留线突然停止。管理员可以调整保留比例和并发；模型/程序变化需要先停止活动请求。不会自动购买额度、兑换重置权益或换计费来源。额度保留是派发阈值，无法锁住其他客户端的消耗。

服务重启后继续尚未开始的请求；已经开始的请求标记中断，人工继续原会话。不会自动重复提交训练。工作流生成的请求和结果也保存在服务端；浏览器保存待查看的任务编号，重开原页面继续查询。

## 保存、共享与再次调用

- 对话结果的“保存方法”允许编辑并保存项目 Skill，不调用模型。项目智能体可以按用途发现、读取这些说明。
- 项目空间的“内部共享方法与流程”允许主动分享到自己可访问的指定项目；其他项目不可见。接收方加入独立副本，重复点击不会重复安装。
- 工作流分享包含关联子流程、节点内代码和来源修订号。子流程调用重新映射；实际模型、数据和凭据绑定清空，变量引用保留。不会打包原始文件、模型包、连接或聊天。
- 文件输入、文件列表及行表中的文件默认值也会清空；接收方通过表单选择本项目资料，普通参数保留。旧分享内容新安装时采用相同处理，已经安装的员工副本保持原状。
- 方法说明中的引用文件可以逐项勾选，默认不选。预览展示实际要分享的正文及所选内容；分享后保留当时版本，接收方在「资料与知识 → 项目使用说明 → 引用资料」查看和修改独立副本。方法在选择后发生修改时，要求重新选择，避免误发未检查的新内容。
- 只复制已经存入这篇Skill的所选文字或代码引用，不跟随正文中的链接或项目文件路径。外部代码、原始资料、模型包和连接仍需在接收项目明确配置；未勾选但说明依赖的引用同样需要补齐。用户写入所选代码、提示词或引用中的私人信息应在分享前移除。
- 更新分享不覆盖已加入的副本。复用原流程预测、编辑报告等能力沿用现有运行时；本轮没有重新制作已验证的官方机器学习流程。
- 组合流程会同时复制关联子流程并重映射调用。安装请求中途保存失败时清理本次新建的流程及说明，原项目内容保持；修复后可以再次加入，成功后的重复点击返回原副本。

## 使用统计

管理员页分开展示账号额度、平台任务与功能统计。记录对话、上传、生成、编辑、训练、预测、下载请求、方法保存及复用。工具调用以独立发起方和根请求关联，不能计成更多员工任务。结果可标记“有用／需要修改”。

统计只保存标识、功能、状态、时长和已报告 token，不保存消息、文件或凭据。统计写入失败不使业务失败。明细默认保留 30 天、日汇总 180 天。从本功能启用后开始统计；未采集的历史不补估。下载计数表示服务器收到的明确下载请求，不能证明用户已保存文件；页面预览和浏览器本地生成的导出不计入服务端下载。工作区结果、原文件和旧交付包入口均纳入；画布保存按所属项目记录成功请求，重复请求次数不代表独立修改数。此次补计从更新后生效，不回填过去遗漏的次数。账号在其他客户端的用量不归入平台任务。

## 验证边界

账号登录、模型目录、额度通过真实服务器只读查询核对。会话、工具、队列、生成和授权的自动测试使用协议替身，模型出口关闭。真实推理和完整员工训练闭环必须在账号额度满足保留线、模型出口明确启用时另行核验，不能用替身结果声称已通过。

2026-09-25用户进一步明确两层授权：模拟员工在项目对话中使用订阅账号的 `gpt-5.6-luna / max` 官方智能体；搭建出的业务工作流可以使用明确配置的原始模型 API Key。两者配置和用量分别记录，不能用平台自写的 API 会话替代官方智能体来完成这项员工模拟。自动测试仍关闭模型出口；主部署设置保持原状，实际验收仅在隔离实例启用订阅调用。

这也适用于工作流所承载的定制智能体：内部模型调用可以使用API Key，不受员工对话选择订阅智能体的限制。是否允许完整外部智能体积木，仍由项目能力设置决定；API模型连接与完整智能体积木是分别配置的能力。本次澄清不自动改动任何项目连接、服务商或预算。

同日真实推理验证：首次任务因重复读取草稿、随后读取节点，累计报告37,046 token后触发32,000阈值，未启动训练。修正现有流程上下文，直接提供输入定义和默认值；训练与进度的大型工具参数改为按需加载。接入时发现延迟工具需要命名空间，启动失败没有产生模型用量；协议回归和实际CLI只读启动检查通过。

复测中官方智能体直接调用现有分类流程，新训练及48条保留测试完成，准确率0.9583、宏平均F1 0.9577，与先前同数据同配置的离线基线一致。6份文件/压缩包下载核对通过，原草稿修订未变。资料是自编合成数据，不能据此认定生产效果；本项通过员工接口模拟，未新做浏览器操作。最终自然语言解释仍未完成：智能体读取60,675字节完整运行详情，累计51,621 token后被预算停止。两次实际推理合计88,667个服务报告token，包含重复上下文和缓存输入，不能换算成未经服务商报告的费用或订阅剩余额度。本轮没有调用业务模型API，且未提高预算或继续追加推理。

为解决完整详情过大，`workflow_run` 增加 `output_path`：可以只读取指定结果分支的完整指标或类别明细，原完整运行仍可查看。对上述同一真实任务，完整测试结论响应764字节；没有截掉指标和类别明细。修复时完成离线及原任务读取验证，相关后端57项通过、3项额外环境测试跳过，无前端改动；后续真实解释复测如下。

同日继续以独立员工账号、订阅 `gpt-5.6-luna / max`，新建关联已有成功任务的会话，仅请求解释结果及已有下载入口。智能体自主调用一次 `workflow_run inspect`，选择 `output_path=["test"]`，随后完成回答；没有重新训练、启动工作流或修改配置，前后任务集合及原输出一致。解释正确指出48条中46条正确、28条应复核样本中2条被误判为good，并说明合成数据、小样本、非盲测和未按批次隔离的限制；模型选择分数与保留测试分数分开说明。回答中的三个文件链接经同一员工账号下载，字节哈希与先前产物一致。

这次模型执行66.36秒，员工请求到结束69.34秒，报告28,179 token，保持原32,000监测阈值及180秒时限，未提高额度。仅验证已有结果解释；不将拆成两次请求的训练与解释称为一次无中断闭环，也不把这次用量与前两次不同任务直接换算为节省比例。累计三次实际推理报告116,846 token，包含缓存输入；仍需降低上下文成本。本轮相关后端48项通过，自动测试及下载复核关闭模型出口；隔离推理服务已停止，未新增业务API调用或浏览器验收。实际回答与逐项核对记录留在本地私有开发目录。

随后按同一任务离线核对，原结果摘要仍默认携带分布、样本和训练配置。改为保留较小结果及指标、文件路径，用字段目录或数量描述大结果分支；需要时继续通过 `output_path` 读取完整内容。超过20个顶层输出时也保留后部结果入口。该任务摘要由8,139缩至5,717 UTF-8字节，测试指标及三个下载路径完整保留，原数据与旧运行不变。这是上下文字节测量，不是token或费用节省实测，未追加模型调用；相关后端66项通过、3项额外计算环境跳过。

2026-09-25补充引用文件分享验证：两个独立模拟员工账号通过桌面与390×844页面完成只选代码、预览、分享、加入及修改。未选练习资料不被复制，接收方无权读取原项目；作者分享后改变代码，接收方仍获得当时版本。按需读取所选代码后实际在禁网计算环境执行，得到3项合计0.25；员工修改自己的副本后得到4项合计0.50，原结果保留。此为自编操作测试，无真实模型调用。相关后端12项通过、3项额外环境测试跳过；前端208项、类型检查和构建通过。

同日补充组合流程与失败恢复：新用例复现子流程或说明保存失败后留下3条半成品，现已修复。创建成员、保存子流程、保存说明及说明已写入后报错四种注入失败均可清理并重试，原流程、原说明及应用列表保持。相关21项测试通过。另由两个模拟员工账号通过实际接口复制3条组合流程，接收项目完成4行数据体检与汇总，6份产物可下载、子流程引用均指向新副本，原项目没有运行，任务约1.97秒。初始私有检查脚本误认为汇总输出也有`rows`，改为核对分组计数及合计后，复查原成功任务通过，没有重跑计算。前端本项未改，沿用208项与构建；没有模型调用。

本实现使用 [Codex App Server 官方协议](https://learn.chatgpt.com/docs/app-server)，接入账号、模型、额度与 thread/turn 接口。CLI 版本在连接时记录；变化后要求重新连接核对。技术连通不代表个人订阅的多人服务套餐适用范围已经获得确认；原计划中的这一边界仍保留。
