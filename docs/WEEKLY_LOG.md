# 每周开发记录

客户运行编号、资料及详细试验记录保留在本地项目，不随公开源码发布。此前完整记录保留在本地私有文档副本。

## W1 · 2026-09-14—09-20

### 9/20：v0.5.1 已推送，开始 v0.6

整理现有平台功能、测试与公共使用说明，客户文件和运行记录排除出公开提交。全量后端首次运行 3553 通过、11 跳过、17 失败；失败归为模型包装参数兼容、备份连接及文案三类，修复后相关 73 项全部通过。前端 89 项及生产构建通过。v0.5.1 标签已推送，进入 refactor/v0.6 并同步产品意图。下一步实现草稿独立生成与运行资源解耦。

### 9/20：v0.6 独立任务与资源实现

已实现草稿直接生成/原子替换与撤销、独立训练任务、模型名称与版本绑定、项目 Skill 及 Python 代码执行。默认项目会话移除需求确认/固定阶段/按事项反复补轮；实际后台任务完成后仍接续处理，用户停止会取消所属任务。默认积木库精简，高级兼容能力仍可展开；项目禁用的完整智能体积木保持目录、保存和执行一致限制。

最终后端全量 3594 通过、14 跳过；真实 Docker 及相关检查 43 项通过，预算/建模复测 17 项通过（集合重叠）；前端 96 项通过，生产构建通过。浏览器已完成项目创建、表格导入、未绑定草稿生成和明确运行报错、无训练工作流的 CPU 训练、绑定原预测流程、新数据预测及 CSV 下载。原生文件选择自动化仍返回 Not allowed，表格粘贴通过真实 multipart 上传路径完成；未将两者混称。

相同生成请求：默认目录和预测输出说明修正前输入 17304 token、响应 11.24 秒且导出节点配置错误；修正后输入 8587 token、响应 5.25 秒且一次保存成功。两次均只调用模型一次，没有业务运行。服务商未报告费用，不将零值称为免费；这是单例测量，未推断普遍提速比例。

页面人工修改和 AI 多节点修改均在新运行生效；运行后返回原工作流可撤销 AI 修改，人工字段保留。项目智能体完成一次线性模型反馈对照并复用原预测流程，旧报告保留；页面独立预测、项目说明编辑、中文分析报告下载及 390px 视口检查通过。真实对话暴露的操作后写文件阶段锁、试用前关联事项要求及绑定参数隐藏已修复；独立训练完成后停止计算计时，防止用户编辑期间消耗训练预算。调用与错误实录见 [V06_VALIDATION](V06_VALIDATION.md)。

下一步：9 月 27 日前补干净机器安装和原生文件选择器人工验收；跨设备访问取决于实际网络连通性。语义 RAG 按计划在之后推进，本次不扩建插件市场。

### 9/19：设计冲突审查

完成默认智能体路径、旧 Builder、积木、恢复及 UI 的源码和离线审查。受影响后端 43 项、前端 15 项通过。重构建议尚未实施，未据此宣称业务验收完成。

### 9/18：局域网入口

配置开发服务局域网监听；跨设备访问仍受所在网络连通性影响，不能以本机健康检查代替跨设备验收。

### 9/17：原始模型与页面工作流

项目主路径改用模型 API 与平台自身会话循环；按项目限制完整智能体积木。补齐资料复用、手动运行、文档环境及取消处理。实际业务报告暴露检索与恢复问题，完整原始资料的最终配置复验尚未完成。

### 9/16：项目与建模

补齐项目资料、建模摘要、组合操作、训练笔记、结果下载及备份恢复。本机试用不等于干净环境部署或客户模型效果达标。

### 9/20：登录与成员共享进入实现

新增开放注册、用户名密码登录、多设备独立会话、改密和管理员重置/禁用；前端改用 HttpOnly Cookie，移除匿名代理自动补管理员令牌。新增项目人员成员关系、负责人分配及跨项目资料/连接权限；旧项目先仅管理员可见，保留原数据。登录、注册、账号设置、账号管理和项目成员页面已接入。

相关后端首轮 19 项通过，前端 104 项通过。完整后端首轮 3558 通过、47 失败、14 跳过；已定位旧免登录链接测试、中文认证文案、接口清单及独立签名回调兼容，正在受影响复测。浏览器普通账号已注册、创建项目并创建未绑定预测流程，发现并修正画布版本列表权限误拦。未宣称跨设备及全部交付完成。下一步完成回归与会话隔离，再继续选区编辑和真实数据主链。

登录阶段收尾：完整后端 3607 通过、14 跳过；追加入口权限与启动检查 14 项通过（含重复覆盖）；前端 107 项、类型检查和生产构建通过。生产构建浏览器实测两个账号的注册登录、默认项目隔离、负责人添加协作者、协作者打开预测画布及运行表单、连接和成员管理入口按角色展示。加入跨标签页账号切换清理，以及撤销权限后终止事件流；旧无作用域运行与生成入口对普通用户关闭，用户从项目入口执行。

现有数据库完成私有备份，在副本迁移后核对七张核心表全部内容一致。本机平台已切换到登录入口，首次管理员凭据仅保存在被 Git 忽略的本地私有文件；生产代理登录、读取原有项目和退出均已验证。仍需完成：旧使用链接的项目任务运行迁移、代理部署逐访客限速、独立会话、生成/业务模型分离、局部编辑和真实跨设备机器学习主链。

### 9/20：生成模型独立配置与画布局部修改

新增独立生成连接及明确的主模型继承模式，负责人可从页面配置、复制或恢复继承；密钥保留在项目连接目录。独立生成连接禁用或失败时不自动换模型，业务模型及项目对话不受设置影响。

项目画布新增直接应用及撤销入口，支持整图、选区和多层循环内部。局部结果合并保留范围外节点、边界连线和原布局；使用画布修订号防止生成期间或撤销时覆盖其他修改。模型只返回一次结果，不创建业务任务，不增加预览凭据、审批或自测门槛。旧无项目编辑器保留兼容入口。

完整后端 3622 项通过、14 跳过；受影响后端 57 项通过、3 跳过；最终局部复测 12 项通过、3 跳过（覆盖重叠）。前端全量 110 项、类型检查及生产构建通过。生产构建页面验证独立连接保存重开与恢复继承、选择节点及“修改所选”定位、缺连接报错且草稿不变。局部合并采用替身模型测试，真实模型效果及跨浏览器并发仍待复验。下一步补人工配置表单与多个独立会话，继续完整真实输入使用链及独立环境验收。

### 9/20：人工配置表单与实际运行

引用选择只列出连线可达的上游节点；字段选择读取积木输出、输入字段、LLM 结构化字段及赋值字段。旧失效引用保留并提示修正，模型用途切换读取对应主/视觉连接，资源加载可重试。输入输出支持明确的文本、数字及布尔类型，重名不覆盖已有字段；新增条件分支、图片列表和变量列表表单，普通配置不再要求手写这些 JSON 结构。

浏览器从同一诊断草稿通过表单将阈值 10 改为 500、输出编号改为文本 010，保存并分别运行 600/100，实际返回“达标”/“不足”，文本前导零保留。另通过页面选择项目图片、切换模型用途及保存重开；期间复现并修复旧草稿省略默认系统指令时错误要求补填的问题。诊断图由开发接口准备，不将此次记录称为从空白纯手搭或真实客户业务交付。

前端全量 119 项、类型检查和生产构建通过；相关后端 98 项通过、3 跳过。没有调用真实模型供应商。开发辅助使用已选 frontend-ui-engineering，来源和内容版本见 DEVELOPMENT_SKILLS。下一步实现多个独立会话，补循环内部人工画布及项目知识库资源，继续真实数据主链、独立安装、原生文件选择及跨设备验收。

### 9/20：明确登录系统交付范围（计划更新）

按用户要求，将登录系统明确列为 9 月 27 日原型必交付能力，补齐首版账号与成员范围、旧数据保留原则及两账号和跨设备验收标准。核对现有登录实现说明和历史验证记录；本次仅修改计划文档，没有新增实现或重跑测试。未完成项继续保留：旧运行链接迁移、代理注册限速及跨设备实际使用。独立智能体对话另行验收，不以登录会话支持代替。

### 9/20：独立项目会话

新增当前账号的项目会话列表、新建、重命名与切换。每个会话分别保存模型历史、消息、活动及运行句柄；项目文件、模型、工作流、Skill 和保存结果仍共享。任务保留发起会话，停止与继续定位原会话。旧会话文件保留为负责人/管理员的历史入口，协作者通过新会话操作；兼容接口不再暴露旧会话全文。页面草稿和分页游标也按会话隔离，旧发送回调不会清空切换后的输入。

完整后端 3638 项通过、14 跳过；前端全量 122 项、类型检查和生产构建通过。测试通过真实平台模型循环及替身供应商验证私有历史、连接重建、两个同时运行的会话分别停止、服务初始化恢复及原会话继续任务；没有外部模型调用。生产构建浏览器实测新建两个会话、重命名、草稿切换与刷新恢复。下一步继续旧运行链接迁移、人工循环画布、真实数据主链及独立安装/跨设备验收；完整计划尚未完成。

### 9/20：循环内部人工画布

修复进入循环后仍从外层查找节点、选区、连线和字段的问题。画布使用完整层级路径，支持两层循环逐层编辑和返回；排队操作保留发起层级。内部增删、表单、布局与连线保存复用草稿修订号，保留外层配置；输出积木使用下拉选择，字段选择显示运行时的当前元素、序号、状态和显式变量。条件连线增加分支选择与删除入口，AI 内部选区携带层级路径。

前端 128 项、类型检查和生产构建通过；受影响后端 12 项通过。生产构建页面将第二层输出从 001 改为文本 010，运行实际返回 100/600 两项及各自 010，外层同名节点保留原值；内部积木添加、删除、布局保存也已操作。诊断图通过开发接口准备，不称为从空白纯手搭或客户业务验收。开发辅助沿用已记录版本的 frontend-ui-engineering；没有外部模型调用。下一步清理默认页面中的旧 Harness 展示，继续旧运行链接、真实数据链及独立环境验收，语义 RAG 仍在完整计划内。

### 9/20：项目默认界面与旧运行链接

项目画布移除待验收状态、内部运行版本提示及发布门槛，保留直接运行、可选测试和旧版本恢复；测试失败显示字段预期/实际值或运行错误，不再自动发起模型修复。项目对话可以阅读需求文档，但不要求先确认文档。旧无项目编辑器保留兼容，项目归属查询失败显示错误和重试，不回退到旧入口。

旧使用链接现在进入所属项目运行表单，使用项目任务、模型及工作目录；打开链接不会自动运行，页面明确执行当前已保存草稿。权限失败不回退到访问码入口，无项目旧应用只对管理员保留。

前端全量 133 项、类型检查及生产构建通过；受影响后端 18 项通过。浏览器实际执行故意失败的诊断测试，核对预期与实际输出后，直接运行同一工作流成功；旧链接也进入正确项目和已选流程。诊断测试与接口替身验证失败后没有自动修复请求，本次未调用真实模型。下一步补代理部署逐访客限速，继续真实数据主链、原生上传、停止恢复、独立安装和跨设备验收；完整 v0.6 计划尚未完成。

### 9/20：可信代理的登录与注册限速

新增部署专用代理密钥，前端与后端核验入口提供的访客 IP，仅用于登录与注册计数，不授予账号权限。未配置、错误密钥、无效地址或地址列表均不作为访客身份；前端不会转发浏览器自带的 X-Forwarded-For。IPv6 等价形式和 IPv4 映射地址统一计数，限速响应保留 Retry-After。补齐部署配置及单入口 Nginx 示例，明确同一 NAT 出口可能共享额度；默认本机直连模式不冒称按访客区分。

相关后端 21 项通过；前端全量 141 项、类型检查及生产构建通过。隔离环境实际串联 Nginx、生产前端及后端，主机客户端与容器客户端各自首次注册成功、再次注册返回 429；入口覆盖伪造头，直连前端更换伪造地址也不能绕过限速，正常 Cookie 会话可读取账号。诊断额度为每小时 1 次，未改变用户数据库或真实模型出口。最终完整后端回归 3651 通过、14 跳过。下一步继续真实数据机器学习链、原生上传、长任务停止恢复、干净安装及真实跨设备验收。

### 9/20：公开真实数据主链与任务停止修复

使用 UCI Wine Quality 红酒数据，页面粘贴导入 1260 行训练数据及 339 行无标签新数据。真实项目模型完成独立训练、发现并复用预测工作流和报告保存；Codex 通过模型页及画布创建未绑定预测流程、人工编辑、绑定并运行、下载 CSV。另在长训练期间保存预测工作流，后续绑定同一图完成预测；重新绑定模型后新运行使用新版本，旧运行及下载文件保留。双页面人工编辑复现并发冲突，已有修改和未保存表单均保留。数据、指标边界及模型调用实测见 [真实数据记录](V06_REAL_ML.md)。

首次停止长训练发现后台把中断当成结果，自动唤醒智能体并恢复任务；修复为仅成功或失败触发结果处理，中断等待用户继续。页面真实 CPU 训练已验证停止保持中断，手动继续同一任务成功；回归覆盖不额外调用模型。真实智能体重测返回服务商 HTTP 402，仍待连接恢复后复测，不将第一次自动恢复算作通过。

补齐项目主模型、视觉模型及生成模型的统一远程出口开关；测试使用替身供应商，关闭时不发起远程请求。修复 `project_action` 按任务编号等待仍要求业务事项的旧限制，等待只返回原任务结果，不启动、不恢复、不修改业务进展，跨项目仍拒绝。受影响测试 49 项通过，新增范围复测 10 项通过（集合重叠）；最终完整后端回归 3669 项通过、14 项跳过（`MODEL_EGRESS_ENABLED=false .venv/bin/python -m pytest -q`）。前端代码未改，沿用此前 141 项及类型检查、生产构建结果。

另在全新 Python 3.14 虚拟环境执行 `pip install .` 和 `pip check`，从安装包在仓库外启动空数据后端，验证注册、创建项目和空白流程、两个账号隔离、退出失效；不是干净机器完整交付。主服务加载修复后登录及项目读取正常，原有 12 个项目保留，远程模型出口关闭。下一步完成独立环境前端与计算链、原生文件选择和跨设备；授权连接恢复后补真实 AI 局部编辑与停止复测，语义 RAG 仍在完整计划内。

### 9/20：隔离安装、生产启动与依赖修复

从公开源码创建隔离副本，以新的 npm 依赖目录和空数据库启动，沙盒及建模镜像使用 `--no-cache` 重新构建。Codex 经页面注册、新建项目、粘贴导入 1260 行训练数据和 339 行无标签数据，先创建未绑定预测流程，再独立训练、绑定原流程、运行并下载；未绑定时明确报错，绑定后同一图完成预测。浏览器下载 CSV 与新运行产物字节一致。项目代码接口另验证新沙盒的独立执行和结果保存；首次诊断代码未创建结果目录而失败，补上代码中的目录创建后成功，没有将其归为平台故障。

干净安装发现锁文件中的 7 项已公开漏洞，更新 Next.js 16.3.5、Vitest 4.1.11、PostCSS 8.5.28 及兼容间接依赖，未进行跨主版本强制升级。更新后的干净 `npm ci`、141 项前端测试、类型检查和生产构建通过，`npm audit` 报告 0 项；后端未改，沿用上一提交 3669 项通过、14 跳过的结果。

实际启动还发现独立输出与 `next start` 不匹配、独立包没有静态资源。构建现在自动补齐资源，`npm start` 改用独立服务；Docker 运行层只复制完整独立包。将运行包移到源码外后实际完成页面主链，另构建并运行 Linux/Node.js 22 前端容器，通过浏览器读取项目及预测结果、下载 CSV；未登录接口仍拒绝。安装步骤及验证边界见 [V06_INSTALL](V06_INSTALL.md)。

本次没有真实大模型调用，没有复用开发项目和模型产物；仍共用这台 Mac 的 Docker Desktop。下一步在另一台物理设备完成安装和原生上传，补真实 AI 复测及项目知识库/语义 RAG。完整 v0.6 目标仍未完成。


### 9/20：项目知识库与真实语义检索

新增项目知识库、PDF/DOCX/文本正文解析、保留出处的字符切分、独立 Embedding 连接和版本化索引。页面可在模型或索引未配置时创建检索工作流；积木表单使用知识库下拉选择，项目工具共用同一检索能力。旧检索积木保留兼容但退出默认目录。连接变化拒绝混用向量，索引失败/并发编辑保留旧版本，工作流运行固定索引版本。新接口沿用账号与成员权限，连接密钥不向协作者公开。

隔离生产页面用本地 embeddinggemma:300m 完成空白项目、提前建图及准确缺项错误、添加两份诊断正文、建立索引、中文问题检索英文资料、运行原流程。删除相关资料重建后新运行返回零条，旧运行原文不变；旧结果已从页面下载并与保存结果核对。补齐结果直接展示与 JSON 下载、损坏文档的可修复错误。没有开启远程模型出口，未替换授权对话服务商。

全量后端 3684 项通过、14 跳过；最后文档错误修复后知识库 9 项复测通过。前端 145 项、类型检查及生产构建通过。范围与实测见 [V06_KNOWLEDGE](V06_KNOWLEDGE.md)。开发辅助沿用已记录版本的 codebase-design。下一步继续生成式引用回答、项目使用说明及真实智能体复用；完整 RAG、原生上传、物理跨设备及授权模型恢复后的复验尚未完成。

### 9/20：引用问答工作流与按需模型检查

知识库页面增加引用问答流程入口，生成输入、检索、条件、文本模板、原始 LLM 和结果等普通积木；可在主模型未配置时保存。没有检索原文时直接说明缺少依据，不调用 LLM；有原文时调用一次项目主模型。模型和 Embedding 独立配置，提示词模板及上游字段可用表单修改。新增项目知识 Skill，正文按需读取；回答与原文、位置和索引版本同时展示，支持 Markdown/JSON 下载，错误引用编号提示保留模型原文，不自动反复审查或改写。

实测发现项目执行把未启用连接映射成禁止含模型的整图，导致不经过模型的分支也被拒绝。改为在实际模型节点通过项目模型入口检查连接与远程出口；显式运行策略和项目能力权限继续保留。隔离生产页面完成无模型建图、空检索正常结束、命中资料后的明确节点缺项、配置本机诊断替身后原图运行和下载；替身每次只收到一次调用。人工修改模板后新运行请求使用新文字，原配置仍可在历史运行查看。

全量后端 3697 项通过、14 跳过；最终模板和输出字段复测 33 项通过（与全量有重叠）。前端 147 项、类型检查和生产构建通过。引用回答的页面联调使用明确标注的模型替身，检索仍使用真实本地 Embedding。已授权连接经官方余额接口核实仍不可用，未追加付费生成、未替换正式项目连接。使用方法和验证边界见 [V06_KNOWLEDGE](V06_KNOWLEDGE.md)。下一步仍是授权真实模型的引用回答、完整资料链和实际智能体复用，以及原生上传、物理跨设备和真实 AI 编辑/停止复验。


### 9/20：无工作流知识库管理与完整 PDF 检索

项目智能体工具新增知识库配置、添加/移除资料和建立索引，与页面使用同一份配置、资料和修订号。无需先搭建工作流，旧修订编辑返回冲突，取消构建保留旧版本并允许重试，未变化的就绪索引不重复向量化。项目知识 Skill 同步说明操作方式，未增加模型连接修改权限或调用门槛。

在隔离项目使用公开论文完整 15 页 PDF，原文件经开发接口添加，工具解析出 39,496 字符并用本地 embeddinggemma:300m 建立 82 个片段的真实索引。索引建立前已从页面保存检索流程；建立后页面和原流程可查询正文及后半图注，返回片段逐一核对原文位置。工具接口发现并复用同一流程，输出与页面下载完全一致。此项不冒充真实对话模型自主复用，也不代表图线视觉理解。资料来源、性能测量和排名边界见 [知识库说明](V06_KNOWLEDGE.md)。

实测修复原资料按钮把 PDF 送入文本预览器的问题：二进制原资料现在提供下载，切换文件清除旧预览；页面实际下载 PDF 并核对原始字节/哈希，检索 JSON 同样下载并核对。原生上传仍未验收。全量后端 3705 项通过、14 跳过；前端 152 项、类型检查和生产构建通过。下一步继续授权真实模型的引用回答与自主复用、AI 局部编辑/停止复验、原生上传和物理跨设备；不更换已授权模型，不将诊断资料或日志提交公开仓库。


## W2 · 2026-09-21—09-27

### 9/21：机器学习流程与通用工作流市场调研（未开发）

调研 CRISP-DM、scikit-learn、tsfresh、Featuretools、Feast、Optuna、AutoGluon 及 KNIME/Dataiku/Kedro/DVC/Metaflow/Kubeflow/MLflow 官方资料，形成 [调研报告](ML_WORKFLOW_RESEARCH_2026-09-21.md)。对照当前源码，核实已有划分、特征缓存、折内转换、训练引擎及模型资源；记录训练积木仍需预登记研究/候选、保存用例回放与训练模板语义不同等缺口。提出完整任务/阶段流程两层目录和内部首批模板建议，均未实施。

本次只读调研并新增文档；未安装框架、运行训练或调用付费模型，不重跑代码测试。检查 Markdown 本地引用和差异。下一步与用户讨论表格和过程数据的具体样本/标签/预测时点，再确定默认模板范围。

### 9/21：项目空间与对话创建工作流

现有项目新增“项目空间”入口，集中展示可调用工作流和待处理文件，支持添加空白流程、导入流程定义、上传或复用资料，并把所选文件及流程带入对话。智能体即使正在处理某个事项，也会看到其他业务流程摘要；文件按需读取，不自动把正文塞入提示词。

项目对话新增“完成任务 / 创建工作流”模式。生成可参考当前私有会话、已有流程和所选资料，一次返回保存新草稿，在会话卡片中打开、继续编辑或切回调用；生成不启动业务任务。复用原修订冲突和撤销机制，参考流程不被覆盖。修复首次新建会话丢失空间需求的问题，移除对话页面重复的旧生成输入框。

受影响后端 74 项通过、3 跳过；追加空间发现用例后相关 5 项通过（有重叠）。前端 157 项、类型检查和生产构建通过。浏览器实际验证项目/空白流程创建、空间资源显示、带文件进入首次会话、模式切换保留草稿及缺连接错误。开发接口准备小型示例，实际项目工具调用工作流成功返回文件路径。所有自动测试关闭模型出口，生成使用替身模型，本轮无外部模型调用。下一步根据内部业务要求准备默认机器学习流程；新组合需求的真实模型生成质量未在本轮复验。用法与边界见 [项目空间说明](V06_PROJECT_SPACE.md)。

### 9/21：控制调用量，补真实引用回答

授权 DeepSeek 账号恢复可用。沿用现有连接与完整论文索引，Codex 页面建图、将输出上限设为 256 tokens、执行一次真实引用问答并下载 Markdown/JSON。问题涉及最后一页图注，回答正确引用第 15 页，下载与运行结果一致。一次调用输入 1113、输出 215 tokens，任务约 2.824 秒，未报告实际费用。详细边界见 [知识库说明](V06_KNOWLEDGE.md)。

按用户节制消耗的要求，本轮没有重训、重建索引或启动自主循环，只追加一次生成请求；没有代码修改，不重复全量测试。下一步仍为有限次数的真实 AI 局部编辑、并发与停止复测，以及知识库自主复用；原生文件选择和物理跨设备仍未验收。


### 9/21：两次真实局部生成与并发保护

继续复用现有隔离项目，通过页面选中回答节点，请真实模型只改输出长度。第一次修改成功，结构比较证明选区外配置、布局、连线均保留，页面撤销成功。第二次模型生成期间另一页面保存人工值，AI 保存返回 409，人工修改保留。使用现有授权模型的独立生成配置，关闭思考；未启动任何业务模型运行或训练。

两次输出各 236 tokens；第一次输入 9620，第二次新增输入 148、缓存读取 9472；模型耗时约 1.944/2.673 秒。发现局部生成仍携带完整默认积木目录，记录实际输入成本，不追加调用做无依据的性能宣传。此次只更新验收文档，没有代码改动，因此不重跑全量测试。下一步为循环内部真实编辑、知识库自主复用和真实智能体停止复验；原生上传与物理跨设备仍未完成。


### 9/21：一次真实循环内部修改与运行

通过页面进入第二层迭代内部，真实 AI 将文本输出 `001` 改为 `010`；定义比较确认外层及同名节点保留，只有指定值变化和空视图默认值补齐。页面实际运行后两条输出均为 `010`，外层值不变。诊断图由开发接口准备，未将此项称为从空白纯手搭。一次生成输入 7925、输出 106 tokens，模型耗时约 1.099 秒；循环运行未调用模型。仅更新验收文档，不重跑全量测试。下一步为知识库自主复用、真实智能体停止复测、原生上传及物理跨设备。


### 9/21：对话调用限额接入与真实知识库复用

发现项目 ModelSession 写死 200 次/16384 输出 tokens，未使用任务预算配置。新增服务参数 PROJECT_AGENT_MAX_MODEL_CALLS（默认 32）和 PROJECT_AGENT_MAX_OUTPUT_TOKENS（默认 8192），接入真实对话模型循环。后台任务完成后的自动回合共享额度，达到限额保留消息/工具结果并停止新增模型调用；会话空闲后的新用户请求重置额度。限额只计对话模型，不冒充货币费用上限或工作流内部模型节点的总限额。未增加审批与固定规划步骤。

隔离服务以 6 次/768 输出 tokens、关闭思考运行新会话；真实智能体从项目上下文找到已有检索流程，检查后运行，正确回答标签平滑为 0.1、第 8 页。4 次调用，输入 19718、输出 625、缓存读取 48384 tokens；包含一次错误工具参数后自修正的成本，没有改图或重建索引。

模型会话、连接、项目智能体和对话受影响回归首轮 67 项通过；追加限额/请求继续回归 5 项通过，多会话回归 4 项通过（集合有重叠）。没有前端变更或重跑全量测试。下一步继续真实长任务停止复测及更广引用质量验证；原生上传和物理跨设备仍待完成。


### 9/21：真实训练停止与显式等待路径修复

复用公开数据进行停止复测，记录一次调用限额中断、一次任务在点击前完成的无效观察；没有把它们计为页面停止通过。延长诊断候选后页面成功停止真实 CPU 训练，发现显式等待返回 interrupted 后仍会多调用一次模型。修复 LocalAgents 对活动等待的处理：用户停止时取消当前会话后续调用；已中断任务的历史读取保持正常。

修复后通过页面继续原任务、让真实模型等待，再从页面停止；训练容器退出，同一任务与会话保持中断，超过 35 秒的三次观察均无新增模型调用和自动恢复。测试覆盖 project_action(wait) 与 workflow_run(inspect, wait_seconds) 两条路径、历史读取及原有后台等待。首轮相关 43 项通过，最终 53 项通过（集合重叠），未改前端或重跑全量测试。

本轮累计 11 次模型调用，输入 50525、输出 2429、缓存读取 352256 tokens，包含所有失败/诊断成本；最后修复复测仅一次。详细过程见 [真实数据记录](V06_REAL_ML.md)。下一步剩余原生上传、物理跨设备及明确范围的引用质量复验，避免无边界追加案例。


### 9/21：限定引用质量范围并完成阶段回归

复用完整公开论文及已有流程，页面验证错误前提纠正：将问题中的 0.2 纠正为原文 0.1，两处引用均核对第 8 页原文，下载 JSON 与运行结果一致。无关的差旅报销问题零检索，直接返回缺少依据，没有执行回答模型节点。只追加一次真实生成，输入 1085、输出 24 tokens；未重训或重建索引，费用未报告。

阶段完整后端 3709 项通过、14 跳过（320.34 秒）；前端 152 项、类型检查和生产构建通过，自动测试关闭远程模型出口。本次只整理验证及计划文档，限定案例通过不宣称普遍准确。下一步仅剩原生文件选择上传、另一台物理设备安装与完整使用的验收；本机容器访问不能替代物理跨设备，完整计划尚未完成。


### 9/21：按要求模拟跨设备，修复 HTTP 局域网运行

独立容器通过前端局域网入口，以两个独立账号完成登录、项目隔离、成员添加/撤销、multipart 上传、真实 Docker 流程运行、中文 CSV 下载和退出失效；未调用模型。浏览器复测同一项目时发现 HTTP 下 crypto.randomUUID 不可用，导致启动工作流报错。提取兼容的随机 ID 生成函数并替换运行、训练预测、撤销和说明创建处的直接调用；缺少 randomUUID 时继续使用 getRandomValues。

前端 153 项、类型检查、生产构建通过；已更新本机前端，原局域网地址页面选择同一输入后运行成功，结果为 2 条、合计 10。未改后端、未重复全量后端测试。模拟范围和实际证据见 [安装说明](V06_INSTALL.md)。按用户要求不再等待物理第二台设备；原生文件选择器仍单独标记未验证，不将协议上传冒充系统窗口验证。

### 9/21：整理原型交付，明确验证限制

用户确认继续交付。新增 v0.6 原型交付说明，汇总使用入口、已实现功能、默认关闭远程模型出口及已知限制；更新试用、登录、机器学习、表单和计划中的过时待办。原生系统文件窗口保留为工具造成的未实测项，不再与平台上传故障混为一谈；上传组件模拟及真实上传保存下载的实际结果保留，跨设备按已授权的模拟方式交付。

只读核对远端 main/v0.5.1 基线和 v0.6 分支、版本号、现有测试日志及运行产物：机器学习项目 4 个模型版本引用、9 个文件均保留，基线预测哈希一致；真实引用/复用任务成功，停止任务仍中断。后端最近全量 3709 通过、14 跳过；前端最近 153 项、类型检查和生产构建通过。此次只改文档，检查本地链接与差异，不重复测试或付费调用。后续扩展按业务需求另排，当前原型交付范围见 [交付说明](RELEASE-v0.6-prototype.md)。


### 9/22：官方机器学习流程首批制作与真实分类复验

加入五条独立可编辑官方流程及项目 Skill，提供项目文件登记、表单化字段／划分／候选／预算设置，训练积木不再要求员工填写内部研究和候选编号。继续运行固定数据快照，新任务生成新版本；预测复用模型和预处理，输出 CSV 可交给规则代码节点。项目结果页面展示训练与简单基线、独立测试、逐类样本及模型下载。

公司材料参考覆盖 15 个项目／线索、深入 8 个，清单和原始计算产物留在本地私有目录。真实分类资料经平台 Docker 完成原清洗代码、三候选比较、保留测试、预测和规则后处理。实测发现小测试集缺少某类时 macro-F1 类别范围缩小，已固定类别范围、增加缺类提示并重跑；旧结果保留。光纤原模型在隔离旧版环境复现示例、尾盘变体和缺模型失败，尚待平台模型包导入。

受影响 Docker 实算 23 项通过；前端全量 159 项、类型检查和生产构建通过。此前全量后端 3732 项通过、15 跳过，修正后的全量回归继续执行。页面模拟员工已完成加入副本、未绑定失败、表单绑定、同输入修复预测和下载入口点击；不冒称真实员工参与或原生文件选择器验收。未调用付费模型。下一步完成已有模型包版本绑定，再补充足量过程数据的公司案例验证；说明见 [官方流程使用与方法](OFFICIAL_ML_WORKFLOWS.md)。

### 9/22：已有模型导入、真实规则流程与独立员工复用

新增包含预处理的 sklearn Pipeline 导入和本地环境选择，复用原建模计算服务验证及预测；记录不可变模型版本、文件哈希和实际镜像，失败导入不覆盖可用绑定。运行时验证模型字节与环境，不自动下载或替换。模型页面可选项目文件与匹配环境完成绑定，下载模型包的预测配置带上类别信息。

原模型与规则案例已在平台绑定五个实际模型，执行原字段制备、硬规则和优先级；示例及尾盘变体与隔离原服务决策一致、概率差小于1e-10。页面完成再次导入、独立模拟员工复用、结果展示和下载入口，下载HTTP字节与项目文件一致。智能体工具接口完成发现、按需读Skill、查看输入和调用；本轮不花费大模型额度，不表述为模型自主规划验收。测试过程中另一同库验证实例的启动恢复曾中断一个页面任务，结束该实例后重跑通过，失败运行保留。

前端全量160项通过，修正新增测试的类型错误后类型检查、生产构建通过。后端全量3741通过、18跳过，唯一失败为该前端类型检查，修复后相关9项回归通过；最终模型导入与建模Docker实算11项通过。上一阶段官方流程及独立任务实算23项通过。全程模型出口关闭。

更新[方法与使用说明](OFFICIAL_ML_WORKFLOWS.md)，私有目录保存15项目矩阵、真实案例对照与阶段报告。现有过程资料不足，回归／过程模板只通过合成数据实算；光纤生产批量效果、自动业务阈值选择与独立标定、真实员工反馈仍待完成。此次仅交付已验证的链路，不把五条模板等同五项公司业务验收。

### 9/22：分类自动采纳阈值的完整链路

训练评估表单增加最低验证准确率与最少样本数。只用折外预测选择覆盖最多的可行置信度阈值，同分样本一起处理，无解则全部复核；阈值和选择曲线随模型版本保存。独立测试与新数据预测固定使用，页面区分选择成绩与测试成绩，预测CSV包含采纳／复核决策。规则流程留空手动阈值时使用模型阈值；真实运行发现可选输入引用缺失，改用开始节点已补齐默认值的输出，并增加实算回归。

焊接原数据实算选出0.84阈值，独立测试77条中采纳44条、复核33条；采纳部分准确率95.45%，覆盖率57.14%。该结果不是稀有类或生产放行保证。浏览器已查看实际报告；模型包包含阈值曲线，新输入预测不重训。保留原运行、失败输入及修复后结果。新计算镜像独立命名，旧镜像保留。自动模型出口关闭。

初轮Docker及阈值行为测试8项通过，表单／启动契约12项通过、3项计算跳过；前端受影响测试与生产构建通过。加入可选阈值真实运行回归后，最终Docker及阈值测试10项通过；同一真实输入修复后预测12条成功，模型包下载包含固定阈值。下一步光纤批量适配和受影响阶段复算；完整逐类别工艺阈值仍需业务代价与独立样本。

### 9/22：光纤批量适配与显式规则重算

私有项目新增批量流程：数据制备逐条报错，五模型各对整批有效记录预测，按原输入序号运行原硬规则及优先级，下载完整CSV／JSON。36条示例组合实算为34继承、1测量、1错误，约7.15秒；验证第30条以后结果、重复请求名、全无效输入。保存原模型输出后仅重算规则约0.44秒；不支持的阈值目标逐条报错，改回后恢复一致，旧结果保留。仍不是生产批次准确率验收。

官方目录增加「已有预测的规则重算」，使用前次保存的预测输入和内容哈希，仅执行文件读取与规则代码，修改规则无需重训／重新预测。这是显式产物复用；任意工作流修改后的自动增量执行仍未完成。真实案例代码、模型与资料继续留本地私有目录。前端全量161项、类型检查与构建通过，阶段后端全量3748项通过、18跳过；官方流程Docker实算6项通过，最终结果摘要／安装回归3项通过、3项计算跳过。浏览器用模拟员工账号重新执行36条批量、安装规则重算副本、填阈值并运行成功。关闭付费模型出口。

### 9/22：当前草稿重算与已完成步骤复用

项目结果页增加「按当前配置重算」，带入原输入、新建当前草稿运行；原任务、快照和结果保留。「继续原运行」仍恢复原快照。输入、资源绑定、节点定义、依赖与文件内容不变时，允许复用顶层数据分析、特征、训练等步骤；代码须明确开启并声明输入／输出文件，默认执行。缺少旧校验记录、产物丢失或依赖变化时重新计算。此版不覆盖任意代码依赖发现、循环／子流程和逐输入字段增量计算。

真实公司分类资料重新完整计算约9.41秒，仅修改报告后重算约0.51秒，六步与模型编号保持一致。模拟员工账号在浏览器从历史结果进入重算、确认原输入、运行成功，页面显示复用步骤与原有评价。未修改配置的页面重算复用七步。该账号由开发者模拟操作，不能算真实员工反馈。全部公司资料与运行明细留本地私有目录。

受影响后端及Docker实算20项通过，包括修改训练配置生成新候选、改报告保持模型、同名文件换内容、产物丢失、修复失败、旧记录兼容及项目隔离；前端全量162项通过，生产构建通过。阶段后端完整回归3761项通过、18项跳过；最终复用行为7项与页面行为9项另行通过，类型检查及生产构建通过。模型出口持续关闭，无付费模型调用。

下一步仍需各类真实生产数据、逐类别业务代价／阈值标定和真实员工反馈；当前阶段没有据合成样例宣称回归／工业过程业务验收完成。

### 9/22：特征与样本表可预览、下载

特征积木导出训练实际读取的确定性特征、标识／标签表和特征说明；按原始样本序号对应，缺标签排除数量可见。页面提供预览与完整下载，填补、编码和筛选仍在训练折内拟合。镜像新建 `20260922-feature-tables`，原镜像及历史模型保留。官方Skill与使用说明同步。

公司原始分类资料382行8特征导出核对通过。构造过程样例37条保留36条，未来／尚不可用测量不进入特征；短窗口失败，修改后复用登记并完成三候选训练。分类完整计算约10.71秒，各阶段容器报告的最大峰值约221.23MiB；过程样例约11.32秒、217.60MiB。模拟员工账号从页面填写文件与字段、完整重跑过程图并查看下载，无需JSON。单次同机测量不外推生产表现。

用户确认无额外生产数据，依据现有材料及构造验收评估：分类、现有模型规则可内部试用；回归和过程可内部试算，不将构造数据成绩当客户效果。不再将额外数据作为本阶段推进前提；真实员工反馈、大模型自主调用仍未验证。

Docker官方流程与复用13项通过，新增回归／过程导出2项通过；计算环境内核对导出与缓存逐值一致、标签行对应及不提前填补。宿主worker测试缺可选依赖跳过，实际计算由Docker验证。前端全量163项通过。完整后端3762项通过、18项跳过，唯一失败为示例配置仍期待旧镜像；更新断言后相关7项及 --lf 复测通过。最终前端受影响10项、类型检查和生产构建通过。

### 9/22：对话调用官方流程与自动结果入口

模拟员工从页面发出自然语言请求，原始DeepSeek模型自行发现现有过程流程、读取输入和配置后调用成功，未新建流程。使用构造样例，37条样本保留36条，完成三候选训练和独立测试。4次模型调用硬上限、每次最多1024输出token；提供方报告非缓存输入32257、缓存读取76544、输出897 token，模型累计约6.07秒，请求约16.19秒。费用字段不支持，不宣称免费。

实际发现：计算成功后模型继续检查产物，达到额度上限，最终说明未生成。修复为平台在任务结束时直接显示关联结果卡片，无需追加模型调用；保留真实失败／停止状态，同一请求同一任务去重，对话归属和请求归属独立保存。原有显式结果展示继续写入报告正文。

离线回放原4条模型回复，平台实际执行工具；原请求标识复用成功任务，没有重复训练。浏览器中额度错误仍可见，同时可从结果卡打开训练比较、特征预览并下载CSV和模型。HTTP核对完整特征CSV为36行。此为离线修复复测，不计作第二次真实模型验证。主服务和验收服务均关闭模型出口。

受影响后端47项通过，前端对话18项、全量164项通过，生产构建通过。最终完整后端回归3765项通过、18项跳过（340.47秒）；自动测试模型出口关闭。后续真实员工反馈与各生产任务的效果标定仍未取得；用户确认无额外资料，本阶段内部试用结论不再以补交数据为前提。

### 9/22：按实际输入局部重算、修复后真实对话验收

步骤复用改为运行到节点时比较解析后的参数与声明文件，不再因任意项目输入／资源绑定变化就全图失效。开始节点提供当前输入，下游按实际读取的字段判断；未知副作用、未声明复用的代码、预测／检索及其后续仍执行。旧版校验记录重新计算，旧任务和产物保留。修正文本模板节点类型识别。

真实计算验证只改报告输入保留训练，模型切换后预测使用新版本，无关训练图保留；取消绑定时报缺项。字段直引与经开始节点引用、无关文件变更、实际依赖文件变更、旧版记录回退均通过。页面仅改过程报告标题，四个计算步骤复用，同一训练模型，旧结果不变。

独立模拟员工账号从页面进行修复后的真实模型调用：沿用获准DeepSeek，4次硬上限，实际2次、1次工作流工具调用、0工具失败；新任务成功并返回中文说明。观测约15.14秒，模型累计5.22秒；提供方报告非缓存输入23167、缓存读取38912、输出912 token，费用不支持。文件链接先预览，再点击下载原文件，CSV及模型包下载事件均确认，CSV核对36行。模型出口恢复关闭，未用离线回放替代本轮实测。

Chrome文件选择窗口已触发，选CSV／Excel时扩展返回Not allowed。扩展文档要求开启本地文件网址权限，已请求临时开启、验收后恢复；未获确认不改权限。因此原生上传仍未验收，不能据此宣布计划全部完成。真实员工反馈仍只有模拟操作。

后端全量3769通过、18跳过；前端全量164通过，生产构建通过。Docker其余官方流程及复用16项通过，新增分类输入重算／模型绑定实算一项修复测试引用后通过。下一步：获准扩展文件权限后完成CSV／Excel上传整链，并保留真实员工反馈待开展。

### 9/22：新用户页面向导

新增可跳过、收起、接续和重看的六步教程，默认介绍员工使用已有流程：进入项目、准备资料、选择流程、通过对话调用、查看结果及后续创建/编辑。顶部提供“使用教程”；新注册及管理员新建账号自动获得欢迎卡，旧账号保持安静。进度通过当前账号的 GET/PATCH /api/v1/me/onboarding 保存，兼容新增表保留原账号及项目；跨登录会话接续、账号隔离和撤回项目权限后的重新选择均已测试。

教程直接定位页面入口，翻页不会勾选实际操作；不会自动导入、发消息、运行或消耗模型额度。输入框获得焦点时面板自动收起，手机底部面板可滚动；关闭后恢复入口焦点。项目空间带入流程请求时保留已有消息草稿。补充空项目、无资料/工作流、未建会话、未连接模型和无结果时的说明。

后端全量 3786 通过、18 跳过；最终前端全量 175 通过，生产构建及类型检查通过。隔离环境浏览器实际走完注册、欢迎卡、创建项目、选择测试资料、准备调用、模式切换、运行记录、CSV 预览和下载；390×844 手机尺寸无横向页面溢出，输入时自动收起、Escape 焦点恢复、刷新接续及结束后不再弹出均已核验。测试资料和无模型流程通过开发接口预置；实际运行由页面手动启动，不把它称为真实智能体业务验收。发送请求的成功/失败回调用替身测试，未调用真实模型。两个独立登录会话接续通过接口行为测试，未宣称两台物理设备验收。本机主服务已更新，原管理员不会自动弹教程，可手动打开；模型出口保持关闭。

使用说明见 [USER_GUIDE](USER_GUIDE.md)。本阶段功能完成；后续收集首批员工实际使用反馈，再调整讲解措辞和步骤，不扩建自动演示项目。

### 9/23：官方智能体、内部复用与使用统计

新增管理员订阅服务接入（已有登录、浏览器及设备码）、实际模型和思考目录、项目任务/生成来源独立选择。工作流原始 API 连接保持独立；员工线程绑定自己的会话和项目，工具执行前复查账号及权限。排队保存到 SQLite，默认并发 1、额度保留 50%，按员工轮换；重复请求去重，停止与重启按原请求处理。长计算交给既有项目任务，等待完成后再排队接续。

官方智能体生成工作流使用无业务工具的模式，返回可查询任务编号，刷新接续和停止均保留原草稿修订保护。项目结果可保存为 Skill，方法/流程主动分享给指定项目，关联子流程引用重映射，接收方独立编辑且重复加入不重复创建。分享排除项目文件、连接和实际资源绑定；外部代码文件及方法引用资料仍需接收方补齐。

新增不含正文的功能计数、平台任务用量及可选结果反馈，员工提交与工具调用分开。明细 30 天、日汇总 180 天；未知 token 不推算。自动测试关闭模型出口。真实只读连接已确认订阅登录和 Luna/max 目录；当时账号周额度已用 91%，保持 50% 保留线，没有发起真实模型推理。浏览器实际完成管理员设置、项目授权、对话排队与停止、生成排队刷新接续及取消、官方分类流程安装、分享和重复安装。模拟员工账号只看到授权项目、独立会话和自己的请求；不显示管理员入口。未将模拟操作称为真实员工试用。

本轮额外修复旧会话在数据库初始化前的恢复兼容，以及切换工作流后异步结果更新错误页面的问题。前端全量 180 项、类型检查及生产构建通过；后端最终全量 3835 项通过、18 项跳过，唯一失败是旧出口开关测试被外层部署变量覆盖；补齐该测试的变量隔离后，相关 40 项（含失败项及新增核心能力）全部通过。没有开启真实模型 HTTP。主服务与私有验收服务已更新，模型出口关闭，所有验收排队已停止。

剩余验收：额度满足保留线后，仍需一次有限预算的官方智能体真实业务推理与员工训练闭环；本轮不越过额度保留线、不购买额度。现有原生上传权限限制与真实员工反馈仍沿用此前记录。本轮新页面以桌面浏览器实测为准，手机尺寸未完成复验。

### 9/24：新用户持续聚焦引导

“使用教程”点击直接开始，新账号注册进入后自动开始。新增背景遮罩和真实控件聚焦；跨项目页面、创建项目弹窗及输入时保持引导。键盘范围限定在当前操作区域与提示卡；Escape 与“跳过引导”均先给出温和提醒，确认后退出并保存状态。取消跳过继续原步骤。教程不自动发送消息、运行任务或消耗模型，浏览步骤不会假记实际操作。

浏览器用新模拟员工账号实际注册、创建项目、加入官方分类流程、进入画布、准备对话并编辑草稿；390×844 窄屏下提示卡位于输入框下方。跳过、刷新和顶部重开也已核验。后端教程接口 9 项通过；前端全量 182 项通过，类型检查及生产构建通过，主服务和私有验收服务前端已更新。下一步收集实际新员工对引导文案与步骤的反馈。

### 9/24：20 个可编辑示例项目与引导衔接

新增日常办公、数据处理、机器学习、知识问答及流程搭建示例目录，一键创建当前员工自己的项目，携带公开自编／合成资料、默认输入、工作流和 Skill；复用官方训练流程、知识库和项目执行服务。重复创建请求复用结果，并发请求不会产生重复副本，失败回滚后同一请求可重试。示例不复制模型连接、不授予完整智能体权限，也不自动运行。

页面提供准备消息（保留草稿）、阅读说明、查看文件、编辑流程及直接运行入口；强引导接入示例选择，实测修复教程卡片遮挡原生弹窗提交按钮的问题。新示例读取开始节点的默认输入，避免未显式传参时丢失表单默认值。

新增 28 项测试通过，覆盖全部 20 个示例的运行路径，其中 6 项使用实际 Docker 训练、换数据或模型版本后复测，8 项使用大模型／Embedding 替身，6 项使用实际代码执行。受影响后端回归 51 通过、9 跳过；前端 186 通过，生产构建含类型检查通过。本轮真实模型调用为零。浏览器完成模拟员工创建、资料选择、画布打开、草稿保留、运行与换输入、旧结果及实际 CSV 下载，并检查 390×844 手机视口；真实员工的内容质量反馈仍待后续试用。详见 [示例项目说明](EXAMPLE_PROJECTS.md)。

### 9/25：经验工作流与普通用户补充信息（第一阶段）

新增可编辑「数据摸底与分析建议」及配套项目 Skill，代码计算事实、原始 LLM 解释，必要时暂停询问，未知答案也可继续形成报告。示例目录增加数据入门项目，携带合成资料及已有分类、预测和规则重算流程；各工作流自行定义处理范围，不新增阶段审批。

人工输入积木可展示上游分析，问题随原运行保存。项目对话与运行详情提供普通表单，支持同任务提交并继续；工具调用同步支持 respond。回答校验、过期问题及停止后提交有明确反馈。初步相关后端124项、真实Docker官方流程6项通过，前端相关39项、类型检查与生产构建通过；模型使用替身，无付费调用。下一步完成浏览器员工操作与公司案例复验，并记录最终范围。详见 DATA_GUIDANCE.md。

### 9/25：员工操作复验与分组模型预测修复

独立模拟员工在桌面和390×844页面完成对话调用、等待问题、刷新后继续、回答「不清楚」或补充实际信息、结果预览及CSV/Markdown下载。信息充分分支直接完成；建议按钮保留未发送草稿。修复接续后的结果卡仍显示旧等待标题，以及普通问题文本错误显示文件选择器。通过本地对话替身另外直接调用分类训练，真实计算完成并返回对话结果，没有先运行分析。

训练与预测结果补充普通语言说明：如何比较简单基线、常见指标的含义、少数类可靠性，以及模型概率与业务采纳的区别；指标解释按需展开，模型包实际下载通过。

实际试用发现旧分组模型预测强制要求训练用批次列。预测输入适配现已区分划分元数据与模型特征，保持原模型、预处理及计算环境；有无批次列预测一致，缺真实特征仍明确失败。保留失败案例后复测通过：20条新数据完成预测，仅重算规则可调整复核数量；更换资料及排除一个特征后训练成功，旧结果不变。

私有质量案例重新完成训练与已清洗表格统计；私有五模型规则案例计算与原结果一致。详细输入输出留在本地私有目录。受影响后端140项通过、9项依赖额外计算配置而跳过；另行开启真实Docker的官方流程7项全过（87.97秒），已有导入模型2项通过。前端全量189项通过，类型检查及生产构建通过，服务已更新。使用说明及官方目录同步更新。

本轮付费模型调用为零。替身只验证接口与页面衔接，真实模型解释质量和真实员工反馈仍需后续试用；不将模拟员工、合成数据成绩或私有复现等同于生产效果。下一步是收集内部员工使用反馈，按具体问题调整经验流程，不增加统一阶段门槛。


### 9/25：从多源数据方案提炼时点制备工作流

新增可编辑「按当时已知信息制备数据」：使用现有代码积木读取CSV/TSV/Excel，区分所属时间、实际可用时间与修订版本；按样本时点匹配最新可知记录，保留缺失/过期与逐项来源，输出新样本表供下游分析使用。公开部分为通用实现及自编资料，原项目数据方案定位留在私有目录，不宣称完成客户历史回测。

项目空间目录改称公共工作流市场，继续使用原安装和权限接口，加入独立副本与Skill；示例增加至22项，没有新增市场运行时或第三方投稿系统。浏览器以独立员工账号创建项目并运行，确认空/11/12/20、换资料后空/110/120/200，24小时限制后明确3项过期；桌面匹配明细及模拟手机样本CSV下载通过。

受影响后端36项通过、10项因额外计算环境配置而跳过，新方法包含乱序历史与独立逐项查找对照、输入修复、版本隔离和下游数据体检调用。前端189项、类型检查和生产构建通过，无付费模型调用。新增持续制作记录 EXPERIENCE_WORKFLOWS.md列明已有方法、未提炼方向与旧对话待核事项。下一步继续预测与实测反馈对照，再处理约束方案比较等，整体目标仍在推进。

### 9/25：预测与实测反馈工作流

新增可编辑「预测与实测反馈对照」及配套Skill，加入公共工作流市场和第23个自编示例。流程使用已有代码运行时，按单列或组合标识关联预测与实测，保存本次输入快照；显示有效数量、未匹配、缺值、整体及分组评价、可选基线和业务容差，不调用训练或预测服务。数值与类别任务通过普通下拉选择；默认输入表单支持工作流声明的选项。

私有历史220条记录、22个工单完成平台计算，MAE、RMSE、偏差及全部分组与独立计算一致，观测约1.63秒。原训练/测试来源不明，验证仅限关联算术。另将平台原有20条实际模型预测直接接入反馈流程，以构造复核标签验证路径，没有新增训练，原模型与结果不变；私有材料和详细产物没有加入公开提交。

独立模拟员工从页面创建项目，完成数值运行、换实测、类别判断、错误容差修复和CSV/Markdown下载；390×844下无页面横向溢出，表格可以横向查看。修改报告节点后重新运行，预览出现预测/实测对照；进一步固定分组及基线比较的类别集合，避免缺类改变宏平均口径。全部旧报告仍可下载。浏览器驱动的空字符串填充未清空文本框，改用全选与退格验证实际清空，未将驱动问题冒充平台修复。

受影响后端84项通过、10项因额外计算环境而跳过；新增18项包含平台执行、固定指标例、类别集合、工作表、输入隔离和修复。前端190项通过；修正一处旧测试错误捕获测试框架计时器造成的偶发失败，明确使用项目轮询计时器，产品逻辑未因此改变。类型检查与生产构建通过，服务已更新，无付费模型调用。使用说明见 PREDICTION_FEEDBACK_WORKFLOW.md。下一步继续从工艺优化和切割方案中提炼可迁移的约束检查/比较；总目标尚未完成。

### 9/25：候选方案比较与普通行表配置

新增「候选方案约束检查与比较」及Skill，加入公共目录和第24个自编示例。使用已有代码运行时，先检查员工声明的条件，再按优先级逐项比较或采用明确权重/尺度评分。保留所有候选、缺数据、失败原因、分项及并列；全不满足时不回退选择高分候选。原文件、输入配置快照与历史结果独立保存；分组比较不代表跨任务共同资源排程。

核对切割和压力优化原代码。隔离计算重现切割配对谓词全否仍返回原候选的行为；通用流程不继承该回退。以原切割实现和构造输入生成20个候选、4组任务，平台全部评分与组内首位和原方法一致，其中一组并列；全不满足变体验证首位为空。一次比较观测约2.72秒。专有系数、代码及详细结果留私有目录，没有把构造输入称作生产验收。

工作流开始输入可声明行表列，项目运行页提供新增、修改、删除、上下移动及下拉/数字控件，JSON保留在高级内容。员工模拟从页面创建项目、首次运行、新增损失限制、缺字段失败、修复并换资料、改权重和目标顺序，以及CSV/Markdown下载；修改后满足数量由3变2，再换资料为5，旧文件仍可下载。手机实测暴露报告表格中文被挤成竖排，已补列宽和容器内横向滚动。

受影响后端103项通过、10项因额外计算配置而跳过；新增24项覆盖条件比较符、精确边界、分组/标识、同分、固定尺度、错误修复和平台执行。前端191项通过，类型检查及生产构建通过，无付费模型调用。使用说明见 CANDIDATE_COMPARISON_WORKFLOW.md。后续仍需清洗/特征、参数连续区间、滚动评价等方法及旧请求核对，总目标继续推进。
