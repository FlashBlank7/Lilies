# 已知缺陷与修复顺序（病历单）

> 与 [lean-core-stage-report.md](lean-core-stage-report.md)（成绩单）配对阅读。
> 排序原则：按"什么时候会咬人"，不按学科分类。
> 建立日期 2026-08-08；每修一项在行首打勾并注提交号。

## 一、已经在咬的

- [x] **1. 数据层无节制增长**（36a5210）— 事件冷归档：JSONL 冷文件为权威全量，DB 只留近期（7 天可配），读取端合并回退，每 stream 保序号哨兵。启动自动执行。存量事件属近 2 天，首批出清在第 8 天。
- [x] **2. 构建期弱测试穿透**（本提交）— 建造者数值锚定纪律：业主材料含可计算数值时，至少一条必测用 equals 锚死期望数字；拿不到就 ask_owner 要一个 worked example。附 ERP 全零四轮实案警示。
- [x] **3. 返修的信念锁死**（本提交）— 返修强制复现：平台自动用报障运行的原始输入发起新鲜运行，run_id 写进返修证据并要求以新鲜账本为准。不再指望自觉重查。

## 二、接真实客户单就会咬的

- [~] **4. 商用外壳四缺**（部署初版 36a5210；镜像验证+报价基准 本提交）— Dockerfile ×2 已本机构建通过；compose 全栈冒烟三连绿（backend 直连 / frontend 页面 / 容器间代理）；backup.sh 真实跑通。报价基准 [pricing-baseline.md](pricing-baseline.md)：五个真实项目成本地板 + 三刀修正系数。缓存折扣已入账（9f1fbb0：三段计价，cache_read 缺省 1/10）。并发已实测（scripts/concurrency_probe.py：确定性运行 5/10 并发 30/30 全绿，无锁错误，10 并发时长 <0.7s）。仍缺：权限体系（一客户一实例路线可绕开）。
- [ ] **5. 双执行体系并存** — worker 循环不默认启动（需显式 API 开启），当前 compose 部署无 worker，影响面已收窄；连坐防护有测试。架构收敛（单一执行权）排后。
- [x] **6. 审计脱敏误伤计量字段**（36a5210）— 三处 redact 统一豁免 `*_tokens` 计量复数，凭证语义保留，带回归测试。

## 三、规模化才咬的

- [~] **7. 质量体系单腿**（CI 已立 36a5210）— GitHub Actions：后端 446 回归（纯桩 provider）+ 前端 tsc。仍缺：前端组件/交互测试、lint 强制。
- [ ] **8. 知识索引全局命名空间** — 跨应用同名索引互踩，目前靠约定。一客户一实例可天然化解；多客户同库前必须加命名空间。
- [x] **9. 转录/产物无生命周期**（9f1fbb0）— 事件冷文件 >14 天 gzip 压缩（权威全量不删，读取端 .gz 回退+复活合并）；运行产物 >14 天清除。启动钩子执行。

## 四、已知且接受的能力边界（不承诺、不宣传）

- 模型单供应商（DeepSeek；思考失控风险由自愈重试兜底）
- 训练能力 = 表格二分类逻辑回归
- 搜索 = 新闻源级别（Google News RSS）
- 真实厂商 ERP 协议未实战（确定性假靶通过 ≠ 用友/金蝶直连）
- 对话界面无服务端会话状态（记忆依赖工作流自带历史字段）
- 移动端未验证

## 开工顺序

1 → 7 → 6（纯工程，保护数据与迭代速度）→ 4 部署包装 → 2、3（交付质量复利）→ 5 → 8、9。
