# v0.6 开发计划

目标：9 月 27 日机器学习主路径可独立试用。按含 UI 的完整功能推进。

- [x] v0.5.1 源码快照及标签推送，建立 refactor/v0.6。
- [x] 草稿独立保存；一句话生成、原子修改和撤销；允许资源待配置。
- [x] 无工作流启动训练；项目任务停止和继续；页面查看进度。
- [x] 稳定模型引用与版本绑定；训练前保留预测草稿，绑定后运行；无工作流也可直接预测。
- [x] 项目 Skill 与工作流发现调用；移除默认会话阶段锁。
- [x] 项目导航及积木表单；浏览器完成表格导入至结果下载。
- [x] 完整回归与相同输入测量；记录真实模型验收结果。

后续 v0.6：可配置语义 RAG（解析、切分、Embedding、索引、检索、引用）。通用研究、PPT、更多训练框架及部署环境排在主路径之后。

验收：没有模型也能保存草稿；未绑定运行给出明确节点错误；独立训练完成后绑定原工作流并预测新数据；人工修改生效；旧快照与结果保留；智能体能复用已有工作流。

页面本地文件选择的自动化仍返回 Not allowed，当前实际导入验收使用页面粘贴表格和原上传接口。跨设备局域网、干净机器安装和客户业务模型效果分别验收，不用本机合成数据结果代替。使用路径见 [V06_TRYOUT](V06_TRYOUT.md)，实际结果和测量见 [V06_VALIDATION](V06_VALIDATION.md)。
