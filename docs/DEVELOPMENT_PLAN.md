# v0.6 开发计划

目标：9 月 27 日机器学习主路径可独立试用。产品为自主智能体、按需 Skill 和可管理工作流；生成与资源就绪解耦。工作流搭建暂由 Codex 代劳，平台能力仍需完整实现和验收。

当前交付：[v0.6 原型交付说明](RELEASE-v0.6-prototype.md)。9 月 21 日用户确认继续交付，系统文件窗口保留为验证限制，不再作为整个平台交付阻塞。

## 已保存的成果

- v0.5.1 源码快照和标签已推送，后续在 refactor/v0.6 开发。
- 本机最小链路已跑通：未绑定草稿、独立训练预测、模型绑定、人工修改、AI 整图修改和撤销、项目 Skill 与流程复用、结果下载。
- 此前全量后端 3594 通过、14 跳过，前端 96 通过并完成生产构建；这些是上一阶段结果。

## 新增必交付能力：登录系统与项目权限

开放注册；用户名密码；普通用户自行配置项目模型；项目按成员共享。新增会话、账号页面、成员管理和统一接口权限，管理员本机初始化，兼容保留旧数据。实现和验证范围见 [登录说明](V06_AUTH.md)。

登录系统属于 9 月 27 日可独立试用原型范围。用户从登录进入自己的项目，再完成上传、对话、训练、工作流编辑、运行和下载。它不增加工作流生成的需求确认、计划审批或资源就绪门槛。

首版范围：注册、登录、退出、修改密码；管理员初始化、账号禁用及密码重置；项目负责人和协作者；页面、接口、下载及运行事件使用一致的访问权限。原有项目、资料、模型和报告保留，由管理员指定旧项目负责人。短信、邮箱验证及企业 SSO 不作为本阶段前置条件。

- [x] 注册登录、账号管理、项目成员、项目画布与运行表单联调及回归。
- [x] 旧使用链接进入所属项目的运行表单，按成员权限运行当前草稿。
- [x] 可信代理按访客 IP 传递注册/登录限速地址，伪造头拒绝；实际 Nginx → 前端 → 后端链验证通过。部署配置与 NAT 限制见登录说明。
- [x] 验证旧项目迁移、失效会话和实时连接权限撤销；两个账号页面协作。
- [x] 按用户要求改用模拟设备验收：独立容器经局域网入口验证两个账号、成员权限、上传、运行与下载；浏览器实际复测 HTTP 局域网页面，见 [安装说明](V06_INSTALL.md)。不将模拟称为物理设备或路由器验证。

登录交付验收：两个普通账号默认无法访问对方项目；加入成员后可协作，移除后后续请求失去权限；退出、密码重置和禁用按约定使会话失效；按用户要求通过独立容器模拟另一台设备，并用局域网 HTTP 浏览器验证主要页面操作。登录会话与智能体对话分别验收，多个登录设备不等于多个独立智能体对话已经完成。

## 实施与验证结果

- [x] 生成模型与业务模型独立配置，支持明确沿用主模型或独立连接；见 [生成与修改说明](V06_WORKFLOW_EDITING.md)。
- [x] 项目画布整图、选区和循环内部编辑直接应用，无预览凭据门槛；撤销与并发冲突测试通过。
- [x] 浏览器双页面人工并发保存验证：后保存者冲突，已保存修改及未保存表单保留。
- [x] 真实 AI 选中节点修改、页面撤销及生成期间人工并发保存：指定字段生效、选区外保留；并发返回 409，人工值保留。
- [x] 两层循环内部真实 AI 修改、页面运行及内外层输出隔离复验；诊断图由开发接口准备，范围见循环编辑说明。无项目旧编辑器仍保留兼容入口。
- [x] 上游字段选择、模型用途、图片、输入输出类型和条件分支表单；见 [表单说明](V06_WORKFLOW_FORMS.md)。
- [x] 循环内部人工画布与表单编辑，逐层进入、返回、增删和布局保存；见 [循环编辑说明](V06_LOOP_EDITOR.md)。
- [x] 项目知识库资源选择器、资料解析与切分、独立 Embedding、版本化语义索引及原文检索；页面提前建图、真实本地模型检索、重建隔离和下载已验证，见 [知识库说明](V06_KNOWLEDGE.md)。
- [x] 可编辑的引用问答工作流、项目知识 Skill、回答与原文展示和 Markdown/JSON 下载；无模型保存及分支执行已页面验证，生成展示以明确标注的模型替身联调。
- [x] 智能体工具无需工作流即可配置、添加资料和建立知识索引，与页面共用修订号；完整公开 PDF 的解析、真实本地检索、原流程运行、工具接口复用和原文件/JSON 下载已验证。
- [x] 完整公开 PDF 的单问题真实引用回答及页面下载：沿用授权 DeepSeek，回答正确引用第 15 页图注；一次调用输入 1113、输出 215 tokens。
- [x] 新会话真实智能体从项目上下文发现已有检索流程、检查并运行，给出正确答案及页码；4 次对话调用，包含一次参数错误修正。
- [x] 有限范围的真实引用质量复验：图注问题、标签平滑查询及错误前提纠正有对应原文；无关问题零检索且不调用生成模型。页码和下载核对通过，不代表所有问题均准确。
- [x] 多个独立会话；项目共享资料、模型、Skill 及明确保存的结果。会话权限、历史、并发停止与页面切换验证见 [独立会话说明](V06_CONVERSATIONS.md)。
- [x] 项目画布移除旧验收状态与发布门槛，测试失败不自动调用模型修复；对话不要求确认需求文档。无项目旧编辑器保留兼容。
- [x] 公开真实数据主链：自主训练、训练中页面生成流程、人工编辑、绑定前后运行、智能体再次复用和 CSV 下载；见 [真实数据记录](V06_REAL_ML.md)。数据以页面粘贴方式导入，不代表客户模型效果验收。
- [x] 真实 CPU 训练页面停止及手动继续原任务；修复中断错误触发智能体自动继续，回归验证不会额外唤醒模型。
- [x] 本机隔离安装：新 Python/npm 依赖、空数据库、新计算镜像及独立生产前端完成训练、绑定、预测和下载；容器前端读取及下载验证通过，见 [安装说明](V06_INSTALL.md)。
- [x] 真实智能体训练停止复测：实际 CPU 训练中由页面停止，容器退出、任务保持中断；修复显式等待返回后多调用一次模型的问题，观察超过 35 秒无新增调用。
- [x] 跨设备访问按用户要求以独立容器模拟，结合已有隔离安装验证，不再等待第二台物理设备。
- [x] 上传按模拟方式验收：文件选择组件的 File/FormData 行为测试、真实 multipart 保存、下载字节核对均通过。原生系统文件窗口未实测，作为已知验证限制保留，不宣称该交互已通过。
- [x] 最近一次全量后端 3709 项通过、14 项跳过，前端最新 153 项通过，类型检查和生产构建通过；使用说明及实际测量已更新。后续功能继续执行受影响验证。

v0.6 的语义 RAG 已实现解析、切分、Embedding、索引、检索及引用回答，并完成上述有限范围的真实验证。9月25日补充按需汇报Skill、PPT/PDF工作流及示例，实际文件生成、换稿和下载通过，见 [汇报说明](PRESENTATION_WORKFLOW.md)。同时补充资料研究Skill，按需组织已有阅读、检索、比较及计算；[公开资料读取](PUBLIC_SOURCE_READING.md)可直接保存指定网址内容，无需先建工作流。当前harness与联网研究的具体边界见 [复核说明](V06_HARNESS_CURRENT_REVIEW.md)。完整通用联网研究、其他训练框架和部署环境仍按具体任务核对范围，不将宽泛方向记作已经交付。

真实模型调用只使用本会话已授权的连接和范围；自动测试保持 MODEL_EGRESS_ENABLED=false。详细历史结果见 [V06_VALIDATION](V06_VALIDATION.md)，不以历史通过替代当前验收。
