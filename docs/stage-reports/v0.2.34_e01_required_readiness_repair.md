# v0.2.34 E01 Required Readiness Repair

## 1. Stage Goal

Investigate v0.2.33 required-mode complex BlockFlow readiness failure and test whether the experiment harness used too little repair budget.

This stage completes the repair-budget experiment slice. It does not close original E01 because the successful retry reached `ready` but still failed the complex benchmark.

## 2. Full Task Set Disposition

Source: `docs/stage-reports/v0.2.33_e01_complex_ab.md`

| Prior next-stage task | Disposition in v0.2.34 | Evidence |
| --- | --- | --- |
| Investigate and repair required-mode complex readiness gap | Completed as repair-budget experiment slice | script change, deterministic test, paid/live rerun |
| Re-run or re-evaluate complex required-mode case after narrow repair | Completed | raw JSON, summary JSON, DOCX report |
| Run E02 human-review experiment | Deferred | Separate experiment stage |
| Run E04 local repair vs full rebuild comparison | Deferred | Separate experiment stage |
| Run E05 none/shallow/deep reuse-depth live generation comparison | Deferred | Separate experiment stage |
| Run E08 workflow-internal gate vs sidecar monitor comparison | Deferred | Separate experiment stage |
| Continue deferred Platform Harness product tasks | Deferred | Separate Platform Harness stage |

## 3. Completed Items

- Added `LIVE_BUILDER_BENCHMARK_MAX_REPAIR_CYCLES` to the live benchmark script.
- Wrote `max_repair_cycles` into result JSON for reproducibility.
- Added deterministic env-surface test.
- Ran a paid/live required-mode rerun with:
  - `LIVE_BUILDER_BENCHMARK_CASE=complex_research_brief`
  - `LIVE_BUILDER_BENCHMARK_PLANNING_MODE=required`
  - `LIVE_BUILDER_BENCHMARK_MAX_TURNS=60`
  - `LIVE_BUILDER_BENCHMARK_MAX_REPAIR_CYCLES=3`
  - `LIVE_BUILDER_BENCHMARK_TIMEOUT_SECONDS=1500`
- Retried after an initial `DeepSeek network error`.
- Generated summary JSON and DOCX experiment report.
- Updated experiment ledger without closing E01.

## 4. Code Changes

- `scripts/live_builder_benchmark_suite.py`
- `tests/test_workflow.py`

## 5. Verification

Focused tests:

```bash
.venv/bin/python -m pytest tests/test_workflow.py::test_live_builder_benchmark_case_registry_supports_complex_case tests/test_workflow.py::test_live_builder_benchmark_reads_max_repair_cycles_env -q
```

Result: `2 passed, 1 warning`.

Full backend regression:

```bash
.venv/bin/python -m pytest -q
```

Result: `91 passed, 1 warning`.

Static compile:

```bash
.venv/bin/python -m compileall -q platform/backend/src/agent_platform tests scripts/live_builder_benchmark_suite.py
```

Result: passed.

DOCX structural QA:

- Report: `docs/experiment-status/reports/2026-07-09_1910_E01_required_repair_budget_rerun.docx`
- Zip structure check: passed.
- Paragraph/readback check found `背景`, `实验设计`, `结果结论`, `图片或截图`, `证据链`, and `工程应用状态`.
- Visual render QA: not completed because `soffice` / `libreoffice` is unavailable.

## 6. Paid/Live Result

Evidence:

- First run: `docs/experiment-status/evidence/experiment_v0.2.34_e01_complex_required_repair_budget_2026_07_09.json`
- Retry: `docs/experiment-status/evidence/experiment_v0.2.34_e01_complex_required_repair_budget_retry_2026_07_09.json`
- Summary: `docs/experiment-status/evidence/experiment_v0.2.34_e01_required_repair_budget_summary_2026_07_09.json`
- Report: `docs/experiment-status/reports/2026-07-09_1910_E01_required_repair_budget_rerun.docx`

| Metric | v0.2.33 required | v0.2.34 first | v0.2.34 retry |
| --- | --- | --- | --- |
| Max turns | `36` | `60` | `60` |
| Max repair cycles | implicit `1` | `3` | `3` |
| Build status | `needs_attention` | `needs_attention` | `ready` |
| Build error | mandatory tests failed | `DeepSeek network error` | none |
| Benchmark score | `0.85` | `0.3` | `0.806` |
| Pass rate | `1.0` | `0.0` | `0.0` |
| Draft nodes | `9` | `0` | `8` |
| Missing node types | none | many | `context_assembler` |
| Model calls | `41` | `11` | `34` |
| Tool calls | `98` | `39` | `83` |

Conclusion:

- Expanding repair budget can move required-mode complex build from `needs_attention` to `ready`.
- The first v0.2.34 run is network-error evidence, not quality evidence.
- The retry still fails benchmark because the ready graph omits `context_assembler`.
- E01 remains open because no run has simultaneously achieved `ready` and full complex architecture coverage.

## 7. Historical Designs

- `docs/historical-designs/v0.2.34_design_benchmark_repair_budget_v1.md`
- `docs/historical-designs/v0.2.34_design_required_mode_rerun_v1.md`

## 8. Workingon Archive

- `docs/workingon-archives/v0.2.34/work_v0.2.34_e01_required_readiness_repair.md`

## 9. Incomplete Or Deferred Items

- E01 remains open. The current blocker is simultaneous satisfaction of:
  - Builder readiness: `ready`
  - complex benchmark coverage: required visible architecture including `context_assembler`
- E02/E04/E05/E08 original experiments remain open.
- Platform Harness product tasks remain open.
- DOCX visual render QA remains unavailable on this machine.

## 10. Engineering Closure

Claimed closure level: `research experiment` plus benchmark-script infrastructure.

Actual closure:

- Completed: explicit repair budget parameter, deterministic test, paid/live rerun, summary report, ledger update.
- Not completed: E01 final decision, complex plan-first productization, ready+benchmark simultaneous pass.

Application marker: `验证应用` to experiment method only. No Builder product capability was changed based on this result.

## 11. Next-stage Tasks

The next stage must select from the full remaining task set:

1. Repair the required-mode complex case so a `ready` build also satisfies required visible architecture coverage, especially `context_assembler`.
2. Re-run or re-evaluate required-mode complex case after that narrow repair.
3. Run E02 human-review experiment.
4. Run E04 local repair vs full rebuild comparison.
5. Run E05 none/shallow/deep reuse-depth live generation comparison.
6. Run E08 workflow-internal gate vs sidecar monitor comparison.
7. Continue deferred Platform Harness product tasks.

## 12. Automatic Evolution Handoff

Automatic Evolution Mode should continue to `v0.2.35`.

Recommended next version: `v0.2.35_e01_required_architecture_coverage`.

Reason: v0.2.34 solved the readiness side of the required-mode complex case with a larger repair budget, but the ready graph missed `context_assembler`. The next serious step is to make required mode preserve full visible architecture while staying ready.
