# v0.2.30 Builder Terminal Node Repair

## 1. Stage Goal

Apply the narrow v0.2.29 paid/live finding that BuilderBenchmark treated a runtime-valid `answer` terminal as missing required `end`.

This stage fixes benchmark terminal-node graph coverage only. It does not claim the old Builder build is now ready, and it does not close E01 plan-first vs node-by-node.

## 2. Full Task Set Disposition

Source: `docs/stage-reports/v0.2.29_formal_experiment_tranche.md`

| Prior next-stage task | Disposition in v0.2.30 | Evidence |
| --- | --- | --- |
| Analyze and fix the paid/live Builder terminal-node failure | Completed as narrow benchmark semantics fix | `end -> answer` equivalence, regression test, reused paid result recheck |
| Run a real E01 A/B after terminal issue is fixed or modeled | Deferred | Output-contract failure remains; old build is still `needs_attention` |
| Run E02 human-review experiment | Deferred | Separate experiment stage |
| Run E04 local repair vs full rebuild comparison | Deferred | Separate experiment stage |
| Run E05 none/shallow/deep reuse-depth live comparison | Deferred | Separate experiment stage |
| Run E08 workflow-internal gate vs sidecar monitor comparison | Deferred | Separate experiment stage |
| Continue deferred Platform Harness product tasks | Deferred | Separate Platform Harness stage |

## 3. Completed Items

- Added default BuilderBenchmark equivalence:
  - `end -> answer`
  - existing `model_turn -> llm` preserved
- Added API-level regression:
  - `test_builder_benchmark_treats_answer_as_terminal_end_equivalent`
- Reused v0.2.29 paid/live result to re-evaluate benchmark without a new Builder run.
- Updated E01 experiment report with an `工程应用补充`.
- Updated `docs/experiment-status/v0.2_experiment_status.md` with an `已应用（窄修复）/ 原始 A/B 未关闭` marker.

## 4. Code Changes

- `platform/backend/src/agent_platform/builder_benchmark.py`
- `tests/test_workflow.py`

## 5. Verification

Focused tests:

```bash
.venv/bin/python -m pytest tests/test_workflow.py::test_builder_benchmark_treats_llm_as_model_turn_equivalent tests/test_workflow.py::test_builder_benchmark_treats_answer_as_terminal_end_equivalent -q
```

Result: `2 passed, 1 warning`.

Full backend regression:

```bash
.venv/bin/python -m pytest -q
```

Result: `86 passed, 1 warning`.

Static compile:

```bash
.venv/bin/python -m compileall -q platform/backend/src/agent_platform tests
```

Result: passed.

## 6. Paid Result Recheck

Command:

```bash
LIVE_BUILDER_BENCHMARK_REUSE_RESULT=1 \
LIVE_BUILDER_BENCHMARK_REUSE_SOURCE_PATH=docs/experiment-status/evidence/experiment_v0.2.29_paid_builder_tranche_2026_07_09.json \
LIVE_BUILDER_BENCHMARK_RESULT_PATH=docs/experiment-status/evidence/experiment_v0.2.30_terminal_node_recheck_2026_07_09.json \
.venv/bin/python scripts/live_builder_benchmark_suite.py
```

Result:

- Evidence: `docs/experiment-status/evidence/experiment_v0.2.30_terminal_node_recheck_2026_07_09.json`
- Build status remains: `needs_attention`
- Script status remains: `build_failed_benchmark_evaluated`
- Benchmark passed: `true`
- Score: `0.85`
- Pass rate: `1.0`
- Missing node types: `[]`
- Equivalences: `end -> answer`, `model_turn -> llm`

Interpretation: the benchmark graph-coverage false-negative is fixed. The underlying old build still failed its mandatory tests/output contract and must not be treated as a ready BlockFlow.

## 7. Historical Designs

- `docs/historical-designs/v0.2.30_design_terminal_node_semantics_v1.md`
- `docs/historical-designs/v0.2.30_design_paid_result_recheck_v1.md`

## 8. Workingon Archive

- `docs/workingon-archives/v0.2.30/work_v0.2.30_builder_terminal_node_repair.md`

## 9. Incomplete Or Deferred Items

- E01 original plan-first vs node-by-node A/B remains open.
- Builder output-contract issue remains: workflow-mode requirements asking for named outputs should preferably produce `end` with named outputs, or the system must explicitly model `answer` output-field consequences.
- E02/E04/E05/E08 original experiments remain open.
- Browser visual QA and deferred Platform Harness product tasks remain open.

## 10. Engineering Closure

Claimed closure level: `backend slice` plus `experiment application supplement`.

Actual closure:

- Completed: deterministic benchmark semantics fix, regression tests, paid-result recheck, experiment report supplement, and ledger applied marker.
- Not completed: Builder prompt/tool behavior for named workflow outputs; E01 A/B; any new paid generation run.

## 11. Next-stage Tasks

The next stage must select from the full remaining task set:

1. Fix or explicitly model Builder output-contract behavior for workflow-mode named outputs, especially cases where the requirement asks `End returns summary` but the draft uses `answer`.
2. After output contract is deterministic, run the E01 plan-first vs node-by-node A/B.
3. Run E02 human-review experiment.
4. Run E04 local repair vs full rebuild comparison.
5. Run E05 none/shallow/deep reuse-depth live generation comparison.
6. Run E08 workflow-internal gate vs sidecar monitor comparison.
7. Continue deferred Platform Harness product tasks.

## 12. Automatic Evolution Handoff

Automatic Evolution Mode should continue to `v0.2.31`.

Recommended next version: `v0.2.31_builder_output_contract_repair`.

Reason: v0.2.30 removed a benchmark false-negative, but the reused paid result still shows `needs_attention`. Before spending new paid calls on E01 A/B, the system should reduce the known output-contract failure mode.

