# v0.4.0_ai_requirement_intake

## Stage Identity

| Field | Value |
| --- | --- |
| Version | `v0.4.0` |
| Source stage report | `docs/stage-report-archives/v0.3.x/v0.3.56_legacy_canvas_requirement_completion.md` |
| Stage type | product / repair / architecture |
| Closure level | vertical slice / product capability |
| Stage scope justification | This is version-sized because it starts the `v0.4.x` line, corrects a wrong v0.3.56 product conclusion, replaces a deterministic template intake with a model-backed requirement-intake API, connects the homepage UI to that API, records the action in Platform Harness, adds regression coverage, updates runtime identity and current gate policy, and validates the exact user complaint with a live model call. |

## Source Task Set

| Source task from previous stage report | Disposition in this stage | Design / evidence | Reason |
| --- | --- | --- | --- |
| Start `v0.3.57_acceptance_semantic_test_authoring` | deferred_with_reason | this report | Superseded by the user's direct v0.4.0 request about AI requirement completion. |
| Add browser click smoke for canvas node selection | deferred_with_reason | this report | Not part of the AI intake repair. |
| Add model-planned repair lane for unsafe blocks | deferred_with_reason | this report | Still relevant, but after requirement intake no longer lies about missing fields. |
| Resume Markdown renderer reuse breadth | deferred_with_reason | this report | Not part of the AI intake repair. |
| User direct task: "做一个工作流可以像codex一样" should be completed by AI and ask questions like Claude Code plan mode | accepted and completed | `platform/backend/src/agent_platform/api.py`; `platform/frontend/app/page.tsx`; `.tmp/v040-intake-live-response.json` | The previous implementation produced generic fallback text instead of AI questioning. |

## Goal

Replace the template-based requirement completion with AI-backed workflow intake: for vague requests such as "make a workflow like Codex", Lilies must first ask targeted customer questions and only produce an applicable workflow requirement draft when enough information exists.

## Completed Work

| Item | Status | Evidence |
| --- | --- | --- |
| Added AI requirement-intake endpoint | completed | `POST /api/v1/requirements/complete`; `RequirementIntakeRequest`; `RequirementIntakeResponse` |
| Added Claude Code plan-mode inspired prompt boundary | completed | Backend prompt explicitly references plan-mode questioning while requiring workflow-generation JSON, not code-plan output. |
| Removed deterministic template completion path from homepage | completed | `function buildRequirementCompletionPlan` and `function requirementCompletionQuestions` removed from `platform/frontend/app/page.tsx`. |
| Added AI-backed homepage intake UI | completed | `data-requirement-completion="ai-workflow-intake"`; AI status, detected goal, rationale, questions, and ready-only apply action. |
| Prevented fake completion when AI is unavailable or still needs input | completed | Apply button is disabled unless `requirementCompletionReady`; API errors surface instead of falling back to generic placeholders. |
| Added Platform Harness visibility | completed | `requirement_intake` task kind; each AI intake records a `model_call`. |
| Updated runtime identity to v0.4.x | completed | `PRODUCT_PHASE = "v0.4.x"` and `__version__ = "v0.4.0"`. |
| Updated v0.4 current regression gate | completed | `docs/testing/regression_lanes.json` uses `v0.4.x_current_release_gate`. |
| Corrected v0.3.56 legacy checks | completed | v0.3.56 static markers now treat template completion as superseded by v0.4 AI intake. |

## Verification

| Check | Result | Evidence |
| --- | --- | --- |
| Focused v0.4/v0.3.56 regression | pass, `7 passed, 1 warning` | `.venv/bin/python -m pytest tests/test_v04_00_ai_requirement_intake.py tests/test_v03_56_legacy_canvas_requirement_completion.py -q` |
| Frontend TypeScript | pass | `PATH="/Users/zhonghaoyang/.nvm/versions/node/v24.15.0/bin:$PATH" npm run lint` |
| v0.4 current gate | pass, `10 passed, 1 warning` | Command in `docs/testing/regression_lanes.json` |
| v0.4 evidence script | pass | `.venv/bin/python scripts/v04_00_ai_requirement_intake.py --output docs/workingon/ai_requirement_intake_v0.4.0.json` |
| Backend health after restart | pass | `GET http://127.0.0.1:8001/health` returned `v0.4.0`, `v0.4.x`, and `requirement_intake: true`. |
| Frontend proxy health after restart | pass | `GET http://127.0.0.1:3000/api/platform/health` returned `v0.4.0`, `v0.4.x`, and `requirement_intake: true`. |
| Exact user complaint live replay | pass | Input `做一个工作流可以像codex一样` returned `status: needs_input`, no completed draft, and questions about Codex behavior, target user, runtime input, permissions, and success criteria. |
| Homepage render | pass | `GET http://127.0.0.1:3000/` returned AI intake panel with disabled apply before AI-ready status. |

## Unresolved / Blocked / Deferred

| Item | Status | Reason | Next action |
| --- | --- | --- | --- |
| Full v0.3 historical current-gate sweep | deferred_with_reason | Old v0.3 static scripts encode `v0.3.x_current_release_gate` assumptions and are no longer the current v0.4 gate. A diagnostic run showed many manifest-name failures after the v0.4 transition, not AI intake failures. | Keep historical sweep diagnostic; only migrate old scripts when a v0.3 artifact must remain gating in v0.4. |
| Browser-clicked AI intake form submission | deferred_with_reason | This stage validated HTTP render and live API replay; no Playwright/browser tool was used to type/click the form. | Add browser interaction smoke for AI intake panel in the next usability slice. |
| Building the final Codex-like workflow | deferred_with_reason | The user request is still intentionally under-specified. v0.4.0 now asks the needed questions instead of pretending it knows. | After the customer answers, generate the editable workflow from the AI-ready requirement. |

## Experiment / Product Status Updates

| Ledger / surface | Update | Evidence |
| --- | --- | --- |
| Product phase | Current phase moved from `v0.3.x` usability buffer to `v0.4.x` intelligent intake and usable workflow generation. | `platform/backend/src/agent_platform/__init__.py`; `docs/README.md` |
| Requirement intake | Template fallback is no longer accepted as completion; AI must either ask targeted questions or return a ready requirement. | `tests/test_v04_00_ai_requirement_intake.py` |
| Task monitor boundary | Requirement completion is now a monitored resource-consuming action. | `requirement_intake` Platform Harness task from live replay |
| Regression policy | Current gate narrowed to v0.4-critical checks; full historical sweep remains diagnostic. | `docs/testing/regression_lanes.json` |

## Historical Designs

| Historical design | Final status | Evidence |
| --- | --- | --- |
| v0.3.56 local requirement completion | superseded | v0.4.0 removes local template functions and replaces them with AI intake. |
| Claude Code plan-mode reference | preserved_as_interaction_pattern | AI prompt and UI use `needs_input` style questioning without copying code-plan output. |

## Workingon Archive

| Archive | Contents |
| --- | --- |
| `docs/workingon/ai_requirement_intake_v0.4.0.json` | v0.4.0 static evidence for backend route, frontend AI intake, and template-removal markers. |
| `docs/workingon/legacy_canvas_requirement_completion_v0.3.56.json` | Updated v0.3.56 compatibility evidence after v0.4.0 supersession. |
| `.tmp/v040-intake-live-response.json` | Live model response for the exact vague Codex-like requirement. |

## Next-stage Task Set

| Task | Why now | Closure target |
| --- | --- | --- |
| Add browser smoke for AI intake question-answer loop | HTTP and static checks pass, but the customer path should be proven by typing, clicking AI analyze, answering, and applying. | Browser-level evidence that the homepage AI intake interaction works end to end. |
| Generate a real editable Codex-like workflow after clarified answers | v0.4.0 correctly refuses to guess. The product now needs a high-quality workflow once answers are available. | A generated workflow with blocks for intake, planning, context, permission, tool execution, testing evidence, and user-facing output. |
| Add AI intake to application detail rebuild flow | Homepage creation is fixed, but existing draft rebuild/edit flows still need the same intake quality. | Detail build panel can run AI clarification before launching Builder Team. |
| Reconcile historical regression scripts with v0.4 gate policy | Many old scripts assume the v0.3 lane name. | A compatibility strategy for old stage checks that does not block current v0.4 development. |

## Archive Commit

- Commit: not created in this turn.
- Active current-design clean: no active design files created.
- Active workingon clean: no; `docs/workingon/ai_requirement_intake_v0.4.0.json` and updated v0.3.56 evidence remain active until archive.

## Automatic Evolution Handoff

- Continue: only after user requests continuation.
- Next version: `v0.4.1_ai_intake_browser_loop`, unless the user supplies the missing Codex-like workflow answers first.
- First workingon: browser-level evidence for AI intake form interaction, then a clarified Codex-like workflow build.
