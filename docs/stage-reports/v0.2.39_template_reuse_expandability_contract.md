# v0.2.39 Template Reuse Expandability Contract

## 1. Stage Goal

Apply the E05 result by fixing the contract between Builder-facing Template suggestions and Builder-expandable templates.

This stage closes the marketplace Template expandability gap. It does not close the original E05 question about whether deeper Template reuse is generally better.

## 2. Full Task Set Disposition

Source: `docs/stage-reports/v0.2.38_e05_template_reuse_depth_live_comparison.md`

| Prior next-stage task | Disposition in v0.2.39 | Evidence |
| --- | --- | --- |
| Fix the Template reuse expandability contract exposed by E05 | Completed | Builder code, deterministic tests, paid/live shallow/deep evidence |
| Rerun E05 after the fix to decide applied status | Completed with bounded conclusion | raw JSON, DOCX report, ledger update |
| Run E08 workflow-internal gate vs sidecar monitor/passmode comparison | Deferred | separate Harness experiment stage |
| Continue deferred Platform Harness product tasks with explicit closure level | Deferred | separate Platform Harness product stage |
| Run actual E02 human-panel review if a human reviewer pool is available | Deferred | no human reviewer pool in this execution context |
| Broaden E04 failure classes | Deferred | separate repair-policy experiment |
| Add more complex plan-first cases | Deferred | optional product-strategy evidence |

## 3. Completed Items

- Updated Builder `template_list` so it exposes both server-defined and marketplace templates.
- Updated Builder `template_suggestions` so suggested marketplace templates carry `source="marketplace"`.
- Updated Builder `template_expand` so marketplace templates expand through `TemplateStore.expand_into_workflow()`, while server-defined templates still expand through `BlockRegistry.expand_template()`.
- Added deterministic Builder tests for marketplace template listing and expansion.
- Parameterized E05 script result version with `E05_REUSE_DEPTH_EXPERIMENT_VERSION`.
- Ran bounded paid/live post-fix E05 validation.
- Generated DOCX experiment report with embedded usage chart.
- Updated experiment ledger.

## 4. Code And Artifact Changes

- `platform/backend/src/agent_platform/builder.py`
- `scripts/e05_template_reuse_depth_experiment.py`
- `tests/test_workflow.py`
- `docs/experiment-status/evidence/experiment_v0.2.39_e05_after_expandability_fix_2026_07_09.json`
- `docs/experiment-status/reports/2026-07-09_2305_E05_after_expandability_fix_validation.docx`
- `docs/experiment-status/v0.2_experiment_status.md`

## 5. Verification

Focused deterministic tests:

```bash
.venv/bin/python -m pytest \
  tests/test_e05_template_reuse_depth_experiment.py \
  tests/test_workflow.py::test_builder_can_expand_claude_like_template_into_editable_draft \
  tests/test_workflow.py::test_builder_template_list_includes_marketplace_and_server_defined_templates \
  tests/test_workflow.py::test_builder_can_expand_marketplace_template_into_editable_draft \
  -q
```

Result: `6 passed, 1 warning`.

Full backend regression:

```bash
.venv/bin/python -m pytest -q
```

Result: `99 passed, 1 warning`.

Static compile:

```bash
.venv/bin/python -m compileall platform/backend/src scripts tests -q
```

Result: passed.

DOCX QA:

- Report: `docs/experiment-status/reports/2026-07-09_2305_E05_after_expandability_fix_validation.docx`
- Structural readback: 24 paragraphs, 3 tables, and embedded `word/media/image1.png`.
- Visual render QA: not completed because `soffice` / LibreOffice is unavailable.

## 6. Paid/Live Result

Evidence:

- Raw JSON: `docs/experiment-status/evidence/experiment_v0.2.39_e05_after_expandability_fix_2026_07_09.json`
- DOCX: `docs/experiment-status/reports/2026-07-09_2305_E05_after_expandability_fix_validation.docx`

Provider/model:

- Provider: `multi`
- Model: `deepseek/deepseek-v4-pro`

| Depth | Build status | BuildPlan reuse_depth | Elapsed | Model/tool calls | Template expand | Benchmark |
| --- | --- | --- | --- | --- | --- | --- |
| `none` | `needs_attention` | `none` | `536.444s` | `46 / 59` | none | score `0.85`, pass_rate `1.0` |
| `shallow` | `needs_attention` | `shallow` | `482.334s` | `22 / 30` | `code_reviewer`, `source=marketplace`, success | score `0.85`, pass_rate `1.0` |
| `deep` | `published` | `deep` | `147.504s` | `23 / 32` | `code_reviewer`, `source=marketplace`, success | score `0.85`, pass_rate `1.0` |

Conclusion:

- Marketplace Template expandability is fixed and verified: both shallow and deep live arms successfully expanded `code_reviewer` through Builder `template_expand`.
- `deep` reached `published` in this run and had lower elapsed time/call count than `none`.
- E05's original reuse-depth quality-benefit question remains open. `none` and `shallow` still ended `needs_attention`, and one run is not enough to make a product-default claim.

## 7. Historical Designs

Archived with this stage:

- `docs/historical-designs/v0.2.39_design_marketplace_template_expand_contract_v1.md`
- `docs/historical-designs/v0.2.39_design_e05_after_expandability_fix_validation_v1.md`

## 8. Workingon Archive

- `docs/workingon-archives/v0.2.39/work_v0.2.39_template_reuse_expandability_contract.md`
- `docs/workingon-archives/v0.2.39/implementation_v0.2.39_template_reuse_expandability_contract.md`

## 9. Incomplete Or Deferred Items

- Original E05 is not fully closed: deeper reuse needs more cases and success-condition validation.
- Builder/provider timeout behavior surfaced again: the shallow arm ended with `DeepSeek request timed out`, and long-running build states are still hard to reason about operationally.
- E08 original Harness sidecar/passmode experiment remains open.
- Platform Harness product tasks remain open.
- E02 actual human-panel timing remains open.
- E04 broad failure-class comparison remains open.
- DOCX visual render QA remains unavailable on this machine.

## 10. Engineering Closure

Claimed closure level: `backend slice + research validation`.

Actual closure:

- Completed: Builder tool contract, deterministic regression, paid/live post-fix validation, DOCX report, ledger update, historical design recovery.
- Not completed: broad Template reuse quality proof, Template marketplace UI, embedding/RAG retrieval, provider timeout hard-boundary improvement.

Application marker: `验证应用 / original E05 not fully closed`.

## 11. Next-stage Tasks

The next stage must select from the full remaining task set:

1. Fix the Builder/provider timeout task boundary surfaced by v0.2.39:
   - make model/provider timeouts deterministic build failures rather than long ambiguous `building` states;
   - record timeout evidence in Platform Harness task metadata/events;
   - add deterministic tests and a bounded live acceptance if the change touches provider behavior.
2. Continue E05 success-condition validation after timeout handling:
   - rerun or re-evaluate shallow/deep with the same evidence structure;
   - do not mark original E05 closed until multiple cases or explicit scope limits are recorded.
3. Run E08 workflow-internal gate vs sidecar monitor/passmode comparison.
4. Continue deferred Platform Harness product tasks with explicit closure level.
5. Optionally run actual E02 human-panel review if a human reviewer pool becomes available.
6. Optionally broaden E04 failure classes.
7. Optionally add more complex plan-first cases before making a product-default E01 decision.

## 12. Automatic Evolution Handoff

Automatic Evolution Mode should continue to `v0.2.40`.

Recommended next version: `v0.2.40_builder_provider_timeout_boundary`.

Reason: v0.2.39 completed the Template expandability contract, but the paid/live validation exposed an industrial reliability issue: provider timeouts and long-running Builder states need a clearer Platform Harness boundary before more broad reuse-depth experiments are trustworthy.
