# v0.2.16 Platform Harness Network Egress Policy

## 1. Stage Goal

在 Automatic Evolution Mode 下，从 `v0.2.15` 的 Next-stage Task Pool 选择首项任务：为 WorkflowRuntime HTTP block 增加 Platform Harness network egress hard boundary。

## 2. Completed Items

| Item | Evidence | Status |
| --- | --- | --- |
| Egress policy config | `Settings.platform_harness_network_egress_policy` | completed |
| Egress allowlist config | `Settings.platform_harness_network_egress_allowlist` | completed |
| Platform checker | `PlatformHarness.enforce_network_egress_policy()` | completed |
| HTTP block boundary | `WorkflowRuntime._http()` | completed |
| Regression coverage | `test_platform_harness_network_egress_policy_blocks_http_requests` | completed |
| Design archive gate applied | `docs/historical-designs/v0.2.16_design_platform_harness_network_egress_policy_v1.md` | completed |

## 3. Verification Evidence

Focused policy tests:

```bash
.venv/bin/python -m pytest tests/test_workflow.py::test_platform_harness_network_egress_policy_blocks_http_requests tests/test_workflow.py::test_platform_harness_secret_policy_blocks_http_secret_headers -q
```

Result:

- `2 passed, 1 warning`

Backend regression:

```bash
.venv/bin/python -m compileall -q platform/backend/src/agent_platform tests
.venv/bin/python -m ruff check platform/backend/src/agent_platform/api.py platform/backend/src/agent_platform/config.py platform/backend/src/agent_platform/platform_harness.py platform/backend/src/agent_platform/workflow_runtime.py tests/test_workflow.py
.venv/bin/python -m pytest -q
```

Result:

- compile passed
- ruff passed
- `63 passed, 1 warning`

Paid/live model test:

- Not required. This stage is deterministic Platform Harness policy enforcement.

## 4. Incomplete Items

| Item | Reason | Next Step |
| --- | --- | --- |
| WebSearch/MCP/sandbox egress | This stage only covers HTTP block | Add tool-level egress boundary |
| DNS/IP firewall | Hostname matching only | Later sandbox/network layer |
| UI policy controls | Backend only | Add after policy surfaces stabilize |

## 5. Next-stage Tasks

1. Add tool-level network egress boundary for WebSearch/MCP or explicitly route those tools through Platform Harness policy.
2. Add Natural-language Draft Patch preview UI with preview-confirm-apply flow.
3. Plan worker lease / durable execution semantics as a later phase.
4. Consider benchmark history UI after frontend priority is confirmed.
5. Design secret store and secret reference injection after policy boundary stabilizes.

## 6. Intellectual Asset Candidates

| Candidate | Evidence Chain | Decision |
| --- | --- | --- |
| Network egress hard boundary | v0.2.16 HTTP block enforcement | Candidate; wait for tool-level egress stage |

## 7. Historical Designs

Archive readiness:

- Previous stage design archive audited: `v0.2.15` has historical design record.
- Corresponding version/state document exists: yes.
- Version or stage marker: `v0.2.16_platform_harness_network_egress_policy`.

| Historical design path | Source current-design | Final status | Evidence |
| --- | --- | --- | --- |
| `docs/historical-designs/v0.2.16_design_platform_harness_network_egress_policy_v1.md` | `docs/current-design/design_platform_harness_network_egress_policy_v1.md` | implemented | `docs/workingon/work_platform_harness_network_egress_policy_2026_07_09.md` |

## 8. Archive Commit

- Auto commit required: yes.
- Staged paths verified with `git diff --cached --name-status`: required before commit.
- Commit message: `docs: archive v0.2.16 platform harness network egress`.
- Commit hash: report from `git log` after archive commit.

## 9. Automatic Evolution Handoff

- Automatic Evolution Mode active: yes.
- Continue to next version: yes, unless user pauses.
- Next version: `v0.2.17`.
- Next task source: this stage report next-stage task pool.
- First workingon file to create/update: likely `docs/workingon/work_platform_harness_tool_egress_policy_2026_07_09.md`.

