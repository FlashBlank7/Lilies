# v0.4.13 莉莉丝本地智能体、黑箱平台接耦与临时合作管道

## 阶段信息

| 字段 | 值 |
| --- | --- |
| 模板版本 | `2.0` |
| 版本 | `v0.4.13` |
| 项目总纲 | `docs/evolution-control/PROGRAM_CHARTER.md` |
| 来源报告 | `docs/PRODUCT_NORTH_STAR.md` 与已批准展开计划 |
| 来源阶段报告 | `docs/stage-reports/v0.4.12_openapi_connector_generation_loop.md` |
| 阶段类型 | 产品 / 智能体 / 架构 / 后端 / 前端 / 实验 / 验收 |
| 闭环等级 | 独立本地莉莉丝、黑箱工作流交付、受审批协作补能与真实企业实验完整闭环 |
| 阶段范围说明 | 本阶段由本地 daemon/CLI、黑箱合同与最小权限工具、平台 assignment 桥、临时合作管道、Developer Studio、不可变题包与隐藏验证器、23 项资格测试、真实 Paperless/InvenTree 企业工作流、人格整合和发布归档十个相互依赖工作面组成。只完成 daemon、管道或视觉中的任何单项都不能证明莉莉丝能独立交付平台工作流，因而必须作为一个版本级垂直闭环实施。 |

## 总体目标对齐

| 字段 | 值 |
| --- | --- |
| 总体目标 | 让莉莉丝成为真正独立运行、持续对话并仅通过平台公开合同搭建和修复企业工作流的本地 Builder，而不是平台内部 `WorkflowBuilder` 的别名。 |
| 本阶段贡献 | 建立莉莉丝成熟主路径和正式实验开发路径：正常客户交付不依赖 Codex；只有正式题目临时注入经用户审批的能力补能管道；最终业务结果由隐藏 oracle 独立复验。 |
| 声明边界 | 完成后最多声明本地受控环境中的莉莉丝会话/恢复、黑箱平台搭建、临时合作协议和指定 Paperless/InvenTree 题目结果；没有客户生产、多租户 IAM、安全认证、跨设备同步或其他宿主/协议泛化声明。 |
| 总体阻塞判断 | T01A-G 与大部分 T01I/J 可在当前仓库确定性推进。真实企业实验需要 Docker daemon、冻结宿主和受限模型预算；环境缺口只限制对应证据层级，必须留档并继续所有仍可实施任务，不能回退到 legacy Builder、mock 或人工工作流。 |

## 来源任务集

| 任务 ID | 来源意图 ID 列表 | 上一阶段报告中的来源任务 | 本阶段处置 | 设计 / 证据 | 授权 / 原因 |
| --- | --- | --- | --- | --- | --- |
| `V04-13-T01` | `PRODUCT-001`, `PRODUCT-005`, `PRODUCT-008`, `PRODUCT-015`, `PRODUCT-016`, `ARCH-003`, `EVAL-003` | 莉莉丝持续上下文自主搭建与平台补能闭环。<!-- lock:Run Lilies as an independent local Builder that uses only black-box platform contracts, preserves continuous context, reports approved generic capability gaps through a temporary collaboration pipeline, and completes an independently verified enterprise workflow experiment. --> | 已接受，强制<!-- lock:accepted mandatory --> | `docs/current-design/v0.4.13_lilies_local_agent_and_collaboration_pipeline_expanded_plan.md`、本阶段十项强制任务及其机器证据。 | 用户于 2026-07-22 批准 expanded plan 全部四个审批包，并在本轮要求完成报告所有设计内容。<!-- lock:User approval on 2026-07-22 of all four expanded-plan review packages plus the current instruction to complete every design item after v0.4.12 closure. --> |

## 阶段合同

- 合同状态: 已锁定
- 合同修订: `1`
- 合同锁文件: `docs/evolution-control/stage-contracts/v0.4.13.json`
- 合同指纹: `sha256:a7b5f3f41356deb8333bf9c895c9030a67404ad5b0fa097795ef9ceb756db2a4`
- 合同授权: User approval on 2026-07-22 of all four expanded-plan review packages plus the current instruction to complete every design item after v0.4.12 closure.
- 合同基线提交: `86a4bc87a574f091260958602c7a6177343677a1`
- 合同变更权限: 强制范围或验收标准的变更必须由用户批准

### 强制任务

| 任务 ID | 来源意图 ID 列表 | 涉及界面 / 角色 | 验收标准 | 必需证据 | 状态 |
| --- | --- | --- | --- | --- | --- |
| `V04-13-T01A` | `PRODUCT-001`, `ARCH-003` | 本地莉莉丝内核 / daemon / CLI<!-- lock:local Lilies core / daemon / CLI --> | 独立 loopback 莉莉丝 daemon 与 CLI 提供持久多轮会话、最小 scope 配对、重启恢复、持久权限与 ack、预算和取消；不得导入平台 WorkflowStorage/Builder 或共享平台数据目录/token，并补齐 pair code、status、stop 和私密凭证 provision/revoke 协议。<!-- lock:A standalone loopback Lilies daemon and CLI provide persistent multi-turn sessions, scoped pairing, restart recovery, durable permissions and acknowledgements, budgets and cancellation without importing platform workflow services or sharing the platform data directory or token; pairing-code creation, status, stop, and private credential provision and revoke protocols are included. --> | 严格 schema、会话/turn/权限/配对/ack/恢复测试，独立子进程 CLI transcript，三轮受限真实模型运行和重启证据。<!-- lock:Strict-schema, session, turn, permission, pairing, acknowledgement, and recovery tests plus independent-process CLI transcripts, a bounded three-turn live-model run, and restart evidence. --> | 已完成 |
| `V04-13-T01B` | `PRODUCT-001`, `PRODUCT-016`, `ARCH-003` | 平台黑箱合同 / scoped HTTP 工具 / 审计<!-- lock:platform black-box contract / scoped HTTP tools / audit --> | 版本化公开合同和任务级 HTTP 工具让隔离莉莉丝进程只能对 assignment 白名单应用执行创建、单步草稿编辑、测试、运行、trace/artifact 读取和发布；强制 scope（含 publish）、应用隔离、撤销/过期、幂等 payload 冲突、审计关联、合同漂移、role-filtered schema 与内部 endpoint 拒绝。<!-- lock:A versioned public platform contract and task-scoped HTTP tools let an isolated Lilies process create, incrementally edit, test, run, inspect artifacts and traces, and publish only assigned applications; publication scope, application isolation, expiry and revocation, idempotent payload conflict detection, audit correlation, contract drift, role-filtered schemas, and internal-endpoint denial are enforced. --> | 合同摘要/漂移、403/404 scope、跨应用、幂等、审计、trace 脱敏、artifact 路径安全及独立 HTTP 进程最小 Build/修复证据。<!-- lock:Contract digest and drift, scope denial, cross-application, idempotency, audit, trace-redaction, artifact-path-safety, and separate-process HTTP-only minimal-build-and-repair evidence. --> | 已完成 |
| `V04-13-T01C` | `PRODUCT-001`, `PRODUCT-008` | 平台配对 / BuildAssignment / Studio 状态桥<!-- lock:platform pairing / BuildAssignment / Studio bridge --> | 平台与 daemon 安全配对并私密 provision assignment credential；严格 BuildAssignment 不含明文秘密或 oracle，关联 application/build/assignment/session，持久 relay/ack 事件，在任一进程重启后续传；daemon 不可用时明确失败，绝不静默 fallback 或预建最终草稿。<!-- lock:The platform securely pairs with the daemon and privately provisions assignment credentials; a strict BuildAssignment without plaintext secrets or oracle data links application, build, assignment, and session, relays persisted acknowledged events across either-process restart, fails explicitly when unavailable, and never silently falls back or prebuilds the final draft. --> | API/存储/凭证、assignment 幂等、事件重放、断线重连、双向定位、取消、daemon unavailable 和 Studio 浏览器路径。<!-- lock:API, storage, credential, assignment-idempotency, event-replay, reconnect, bidirectional-lookup, cancellation, daemon-unavailable, and Studio-browser evidence. --> | 已完成 |
| `V04-13-T01D` | `PRODUCT-005`, `PRODUCT-008`, `PRODUCT-015` | 合作管道 / 审批 / Codex 隔离 / claim<!-- lock:collaboration pipeline / approval / Codex isolation / claims --> | 仅正式题注入持久临时 channel；独立表和严格状态机实现报告创建/补证/修订、确定性分类路由、用户审批/题内 auto-forward、权限隔离、reader cursor、幂等消息、Codex lease/response、题包 amendment、环境 response、莉莉丝 re-probe、独立 claim 冻结/失效/验证和关闭后 verifier 例外；未审批正文对 developer 完全不可见。<!-- lock:Only formal tasks receive a persistent temporary channel; dedicated tables and strict state machines implement report creation, evidence supplementation and revision, deterministic routing, user approval and task-local auto-forward, permission separation, reader cursors, idempotent messages, Codex leases and responses, task amendments, environment responses, Lilies reprobe, independently stored claim freeze invalidation and verification, and verifier-only post-close completion while unapproved content remains invisible to developers. --> | 数据库迁移、状态/CAS、访问控制、100 次幂等/重连/溢出/租约故障注入和不含日志扫描的因果导出。<!-- lock:Database-migration, state and CAS, access-control, one-hundred-iteration idempotency, reconnect, overflow, and lease-fault tests plus causal export without log scanning. --> | 已完成 |
| `V04-13-T01E` | `PRODUCT-005`, `PRODUCT-008` | Developer Studio / 用户审批 / developer CLI<!-- lock:Developer Studio / user approval / developer CLI --> | 全局 `/developer/collaboration` 在桌面和移动端展示题目、莉莉丝可观察上下文、工具/合同/草稿/run/trace、完整报告因果链、处理人和下一动作；权限与能力审批分离，审批前 developer inbox 无可推断正文，developer CLI 支持 inbox/lease/respond，Customer Runtime 不暴露开发内容或私有思维链。<!-- lock:The global developer collaboration workspace explains task, observable Lilies context, tools, contracts, drafts, runs, traces, report causality, ownership, and next actions on desktop and mobile; permission and capability approval remain separate, preapproval developer views reveal no report content, a developer CLI supports inbox lease and response, and Customer Runtime exposes neither development content nor private reasoning. --> | 前端源码/typecheck/build、桌面/移动浏览器截图、键盘/溢出/reduced-motion、网络/控制台、审批幂等与 developer 隔离证据。<!-- lock:Frontend source, typecheck, build, desktop and mobile browser screenshots, keyboard, overflow, reduced-motion, network, console, approval-idempotency, and developer-isolation evidence. --> | 已接受 |
| `V04-13-T01F` | `PRODUCT-015`, `EVAL-003` | 题包 / 环境锁 / 归档 / 隐藏验证器<!-- lock:task packages / environment lock / archive / hidden verifier --> | 不可变 revision 题包、环境 preflight、允许动作、预算、过滤 workspace、运行归档和独立 verifier 可重放；莉莉丝/Codex developer workspace 排除 `.git`、平台 data 和 protected/oracle，claim 绑定冻结 hash，旧 revision 修改、oracle 泄漏、缺健康证据和未授权替代验证均被拒绝。<!-- lock:Immutable task revisions, environment preflight, allowed actions, budgets, filtered workspaces, run archives, and an independent verifier are replayable; Lilies and Codex developer workspaces exclude git metadata, platform data, and protected oracle content, claims bind frozen hashes, and old-revision mutation, oracle leakage, missing health evidence, and unauthorized substitute validation are rejected. --> | schema/digest/权限/mount、环境健康、archive manifest/replay、oracle 泄漏反向测试和只读 verifier 进程证据。<!-- lock:Schema, digest, permission, mount, environment-health, archive-manifest, replay, oracle-leak negative, and read-only-verifier-process evidence. --> | 已接受 |
| `V04-13-T01G` | `PRODUCT-005`, `EVAL-003` | 管道资格 Q01-Q23 / 故障注入<!-- lock:pipeline qualification Q01-Q23 / fault injection --> | `PIPE-Q01` 至 `Q23` 全部实际运行且无 mandatory xfail；普通会话管道不可发现，100 次 reconnect/idempotency/lease/concurrency 循环零丢失和零重复副作用；资格结果只证明基础设施，不进入企业成功率分母。<!-- lock:PIPE-Q01 through Q23 all run without mandatory xfails; ordinary sessions cannot discover collaboration, one hundred reconnect, idempotency, lease, and concurrency iterations lose no messages and duplicate no side effects, and qualification results remain outside the enterprise-success denominator. --> | 机器 qualification bundle，逐 Q/iteration/counter/命令/摘要，API 与浏览器证据。<!-- lock:A machine qualification bundle records every Q case, iteration, counter, command, digest, API result, and browser result. --> | 已接受 |
| `V04-13-T01H` | `PRODUCT-001`, `PRODUCT-015`, `PRODUCT-016`, `EVAL-003` | EXP-LILIES-001 / Paperless / InvenTree / Excel / 动态补能<!-- lock:EXP-LILIES-001 / Paperless / InvenTree / Excel / dynamic capability repair --> | 冻结真实 Paperless-ngx 2.20.15 与 InvenTree 1.4.2 题包后，莉莉丝在同一上下文仅通过黑箱平台完成文档/OCR、匹配、冲突/低置信度 Human Input、幂等写回、receipt 与 Excel；只处理经用户审批的通用能力报告，Codex 不写 adapter/映射/最终图；隐藏 oracle 检查 36/36、所有安全门和稳定后三个独立 seed，任何预算/环境/功能失败完整保留且不能用 mock 抬高。<!-- lock:Against a frozen real Paperless-ngx 2.20.15 and InvenTree 1.4.2 package, Lilies uses only the black-box platform in one continuous context to deliver document and OCR intake, matching, conflict and low-confidence Human Input, idempotent writeback, receipts, and Excel; only user-approved generic capability reports may change the platform, Codex authors no adapter mapping or final graph, and a hidden oracle checks all 36 records, every safety gate, and three stable independent seeds while every budget environment or functional failure remains in the denominator without mock substitution. --> | 冻结题包/环境/seed、完整 session/tool/draft/run/report/diff/receipt/xlsx/oracle/archive、模型 provider/cost、全分母与 forbidden-assistance 扫描。<!-- lock:Frozen package, environment, seeds, complete session, tool, draft, run, report, diff, receipt, workbook, oracle, and archive evidence plus model provider and cost, full denominators, and forbidden-assistance scans. --> | 已接受 |
| `V04-13-T01I` | `PRODUCT-008` | CLI ASCII / 平台背景 / 可访问性<!-- lock:CLI ASCII / platform background / accessibility --> | 功能闭环后加入原创低干扰莉莉丝人格：CLI 宽/窄/NO_COLOR/非 TTY/屏幕阅读器行为正确；平台背景只在允许界面出现，Customer Runtime 无 developer 身份，reduced-motion 静止且不降低可读性或改变功能状态。<!-- lock:After the functional loop, an original low-distraction Lilies persona behaves correctly in wide, narrow, NO_COLOR, non-TTY, and screen-reader CLI modes; the platform background appears only on allowed surfaces, Customer Runtime shows no developer identity, reduced motion is static, and readability and functional state are unchanged. --> | 终端快照、前端源码/typecheck/build、desktop/mobile/reduced-motion/contrast 浏览器证据。<!-- lock:Terminal snapshots, frontend source, typecheck, build, and desktop, mobile, reduced-motion, and contrast browser evidence. --> | 已接受 |
| `V04-13-T01J` | `PRODUCT-001`, `PRODUCT-005`, `PRODUCT-008`, `PRODUCT-015`, `PRODUCT-016`, `ARCH-003`, `EVAL-003` | 发布门禁 / 中文报告 / 归档<!-- lock:release gate / Chinese report / archive --> | 机器发布门禁必须包含 T01A-I 聚焦与全量回归、全范围 Ruff 或诚实基线、前端、Docker 配置、浏览器、真实模型、Q bundle、真实实验全分母、禁止协助、archive digest 和演进控制；没有当前失败，独立 Closure Audit 与版本规模通过，设计移入 historical 且报告不把 partial/not_run/environment failure 写成完成。<!-- lock:The machine release gate includes T01A through I focused and full regression, full-scope Ruff or an explicitly truthful baseline, frontend, Docker configuration, browser, live-model, qualification-bundle, real-experiment full-denominator, forbidden-assistance, archive-digest, and evolution-control evidence with no current failure; independent Closure Audit and version size pass, the design moves to historical, and the report never labels partial, not-run, or environment failure as completion. --> | `release-gate.json`、closure review、阶段报告、archive manifest/SHA、提交和 validator 输出。<!-- lock:Release-gate JSON, closure review, stage report, archive manifest and hashes, commits, and validator outputs. --> | 已接受 |

### 可选任务

| 任务 ID | 来源意图 ID 列表 | 涉及界面 / 角色 | 验收标准 | 必需证据 | 状态 |
| --- | --- | --- | --- | --- | --- |
| `无` | `无` | 无 | 无 | 无 | 无 |

## 阶段目标

让用户能够从平台或终端启动同一个独立莉莉丝会话，由她仅通过版本化黑箱合同持续搭建、运行、验收和修复工作流；正式实验中通用平台缺口经过用户审批、Codex 通用修复、莉莉丝复验和隐藏 oracle 独立判定，最终以一个真实企业工作流的完整分母证明或否定能力。

## 已完成工作

| 任务 ID | 事项 | 状态 | 证据 |
| --- | --- | --- | --- |
| `V04-13-T01` | expanded plan 四个审批包已由用户批准；v0.4.12 前置清障和第五次 Closure Audit 已通过。 | 已完成 | approved plan、`closure-review-5-pass.md`、提交 `6360e35`。 |
| `V04-13-T01A` | 抽出平台与本地进程共用的 provider-neutral agent core；新增独立 `lilies` daemon/CLI、本地 API、SQLite/WAL schema v3、X25519 daemon 身份、scope 配对与安全 token 连续性轮换、ACL、持久 ack/权限/checkpoint、跨轮预算/截止时间/取消、私密 assignment credential 和受限 workspace 工具。普通会话不导入平台 WorkflowStorage/Builder/Storage/API，也不暴露 collaboration schema 或私有思维链。 | 已完成 | `platform/backend/src/agent_platform/lilies_*.py`、`agent_core.py`、`agent_runtime_factory.py`、九组聚焦/回归测试和 `docs/evidence/v0.4.13/t01a/manifest.json`。 |
| `V04-13-T01B` | 新增单调版本化的 16 项公开合同、task-scoped token 与 HTTP-only 客户端/工具；强制 scope、assignment/session/application/run 绑定、并发单应用授权、撤销/过期、语义幂等和合同漂移。黑箱运行只允许五项本地文件工具、无网络/secret/MCP，持久化 workspace/递归策略；公开输出剔除私有推理，超大结果返回完整标准错误，artifact 以 opaque UUID、2 MB 总上限和摘要校验的 64 KiB 分块读取。独立莉莉丝进程仅凭合同完成空应用 Build、故意失败、一次修复、验收、发布、合同刷新和业务运行。 | 已完成 | `docs/evidence/v0.4.13/t01b/manifest.json`、独立只读审计 `PASS`。 |
| `V04-13-T01C` | 完成平台与 daemon 的安全 pairing、精确 scope/identity/expiry 绑定、严格四 ID BuildAssignment、私密 credential provision/revoke、崩溃安全 pair/reconnect outbox、跨进程 desired-state CAS、持久 relay/strict ack、取消终态 drain 和 Studio 状态桥。daemon 不可用时保留明确 503 与可恢复 ID；不回退 legacy Builder、不预建草稿。 | 已完成 | `docs/evidence/v0.4.13/t01c/manifest.json`；独立只读 Closure Audit `PASS at the scoped evidence floor`。 |
| `V04-13-T01D` | 完成只对正式题激活且普通会话不可发现的临时合作管道；新增独立 SQLite 表、严格报告/审批/租约/回复/题包修订/环境恢复/re-probe/claim 状态机、持久 reader cursor 与单快照 developer inbox。所有写入使用完整请求摘要、CAS 和幂等 receipt；daemon 能跨重启恢复等待、截止时间和不确定远端副作用。implemented DeveloperResponse 只接受当前 Git 历史中完整可达提交及同一 commit tree 内摘要匹配的 `gitblob` 原始证据；关闭后仅 verifier 可完成既有 claim。压缩恢复增加不推进 durable ack 的有界历史回放、exact-sender verifier claim ACL、schema-max run ID 分页和事务 provenance；工作流状态只接收配对成功的公开工具 envelope，以分页索引和 exact state digest 恢复指定应用而不猜 transcript tail；current claim 按创建顺序确定；121 reports/claims/workflows、200 decisions、10000 字符业务目标及最大证据负载仍在 30000 字符内保持可执行恢复。 | 已完成 | `docs/evidence/v0.4.13/t01d/manifest.json`；147 项聚焦、1243 项全仓、真实 `create_app` HTTP 正反路径和独立只读审计 `PASS`。 |

## 验证

| 任务 ID | 检查 | 结果 | 证据 / 准确命令 |
| --- | --- | --- | --- |
| `V04-13-T01` | 前置 v0.4.12 闭环 | 通过 | `docs/evidence/v0.4.12/closure-review-5-pass.md`。 |
| `V04-13-T01A` | strict schema、core/runtime、存储、权限、配对/轮换、ack、预算、取消、credential、本地 API 与 CLI 聚焦回归 | 通过 | `.venv/bin/python -m pytest -q tests/test_agent_core.py tests/test_runtime.py tests/test_api.py tests/test_v04_13_lilies_models.py tests/test_v04_13_lilies_storage.py tests/test_v04_13_lilies_daemon_and_tools.py tests/test_v04_13_lilies_service.py tests/test_v04_13_lilies_api.py tests/test_v04_13_lilies_cli.py`，`102 passed`。 |
| `V04-13-T01A` | 最终全量后端回归 | 通过 | `.venv/bin/python -m pytest -q`，最终复跑 `885 passed, 85 xfailed`，无当前失败；一次既有 10 ms lease 测试的负载抖动失败及其单测/全量复跑结果如实保存在 `deterministic-tests.txt`。 |
| `V04-13-T01A` | 相关 Python Ruff、diff 与依赖锁一致性 | 通过 | `.venv/bin/ruff check ...`、`git diff --check`、`uv lock --check` 全通过；没有修改用户未跟踪的 `uv.lock`。 |
| `V04-13-T01A` | 最终 X25519 代码独立进程 CLI 生命周期 | 通过 | `cli-lifecycle-transcript.txt`：serve/pair/chat/sessions/status/stop、0700/0600、stop 后删除 daemon.json、第二个 daemon 进程 attach 同一 session。 |
| `V04-13-T01A` | DeepSeek 受限真实模型与重启连续性 | 通过 | `live-model-transcript.txt` / `live-model-summary.json`：`deepseek-v4-flash` 三轮含 `local_time` 与首轮回忆；重启后第四轮回答 `local_time`；总计 1987 tokens、USD 0.00033054、5 model calls、1 tool call。 |
| `V04-13-T01A` | 架构边界、恢复与秘密扫描 | 通过 | `architecture-boundary.json`、`recovery-evidence.json`；禁止导入扫描无匹配，最终 SQLite 消息/事件/安全事件的 thinking/Authorization/api_key/access_token marker 均为 0，manifest SHA 全部复核通过。 |
| `V04-13-T01B` | v0.4.13 聚焦、运行时兼容和全仓回归 | 通过 | `uv run --extra dev --frozen python -m pytest -q tests/test_v04_13*.py tests/test_runtime.py tests/test_workflow.py tests/test_v04_03_regression_time_boundary.py` 为 `303 passed`；兼容性复跑 `42 passed`；最终全仓 `1004 passed, 85 xfailed`，无当前失败。 |
| `V04-13-T01B` | HTTP-only 独立进程最小 Build/失败修复 | 通过 | `uv run --extra dev --frozen python -m pytest -q tests/test_v04_13_lilies_platform_process_boundary.py`，`2 passed`；平台与 LocalLiliesService 为独立进程，Builder 引用被移除且 build rows 为 0。 |
| `V04-13-T01B` | scoped contract、授权/审计、运行隔离、脱敏和 artifact 边界 | 通过 | `contract-and-scope.json`、`security-audit-artifact.json`、`architecture-boundary.json`；最终 digest `sha256:6836b56a5eb76c269e49b4edb9c8078011e4c24955701966290a617735d56279`，合同 schema digest `sha256:578e399402c7d4e589a60ed83f021c1ce3c0d2143c2754f8fdf0c21a850cdece`。 |
| `V04-13-T01B` | Ruff、lock、diff、JSON/secret、manifest 与独立只读审计 | 通过 | `deterministic-tests.txt` 的精确 Ruff 文件集、`uv lock --check`、`git diff --check`、6 项 manifest hash 均通过；`independent-audit.md` 结论 `PASS`，无 mandatory residual。 |
| `V04-13-T01C` | 聚焦、v0.4.13 与全仓回归 | 通过 | bridge `51 passed`；全部 v0.4.13 `294 passed`；全仓 `1096 passed, 85 xfailed`，无当前失败。 |
| `V04-13-T01C` | pairing/reconnect/credential/relay/cancel 安全与真实 ASGI | 通过 | pairing/reconnect response-loss、四 ID、并发取消、completed-wins、terminal commit-before-ack 均有回归；独立真实 ASGI 从 `12/0` 恢复到 exact `12/12` 且不复活 assignment/build。 |
| `V04-13-T01C` | 最新独立进程 HTTP、restart/unavailable/replay/no-prebuild | 通过 | `platform-daemon-http-evidence.json`：两个 assignment、22/22 事件、offline 503 保留四 ID、same-ID resume、2/2 凭证撤销、0600 与 32 文件秘密扫描；sanitized SHA `53668f3ce38da91a3881e77a9ae1fa9df2ac6fa3ae8551ec92aef5b4e4823278`。 |
| `V04-13-T01C` | Studio 源码、TypeScript、生产构建与浏览器证据 | 部分通过 / 证据债务 | 16 项前端契约、2 项 operation-attempt、TypeScript 和 Next production build 通过；浏览器运行时无可用 browser，记录为 `blocked_by_environment`，不声明 browser pass。 |
| `V04-13-T01D` | 合作管道聚焦、事务/安全、真实 HTTP DeveloperResponse、压缩/历史回放/provenance 反例与全仓回归 | 通过 | `docs/evidence/v0.4.13/t01d/deterministic-tests.txt`：协作聚焦 `147 passed`，存储/安全子集 `21 passed`，耐久逻辑时钟 `9 passed`，全仓 `1243 passed, 85 xfailed`；32 项压缩/恢复反例、exact-sender SQL-before-limit replay、exact-state 多应用 workflow recall、按创建顺序的 current claim、公开工具 envelope 来源约束、目标文件 Ruff、`git diff --check` 与演进验证通过。默认 dev extra 缺既有 `python-docx`，全仓使用临时 `--with python-docx` 且未修改用户 `uv.lock`。 |
| `V04-13-T01E-J` | 后续实现和证据 | 尚未开始 | 用户要求 T01D 刷新证据并提交后暂停；后续恢复时从 T01E 继续，不能以 T01D 协议证据冒充监控 UI、题包/verifier、资格包或企业实验。 |

## 闭环审计

- 审计上下文: 使用新的审查上下文，根据阶段合同反向检查，而不是照抄实现总结
- 结论: 版本闭环仍待定；T01A-D 子任务验收通过，T01E-J 未完成
- 缺失的强制任务: T01E-J
- 不受支持的声明: 当前可声明受控本地环境中的独立莉莉丝会话/恢复、assignment-scoped HTTP 黑箱 Build/修复、平台到 daemon 的安全 assignment 接耦和确定性临时合作协议通过；不能声明 Developer Studio 合作监控浏览器路径、题包/独立 verifier 进程、Q01-Q23 机器资格包、真实企业实验或产品默认切换已经实现
- 版本规模门禁: 待定

| 强制任务 ID | 验收结果 | 证据有效 | 审计结论 |
| --- | --- | --- | --- |
| `V04-13-T01A` | 通过 | 是 | 独立只读审计反向发现并推动修复累计预算/usage、cancel/recovery、durable ack、严格响应、fingerprint 绑定与依赖锁问题；最终实现、102 项聚焦、885 项全量、独立进程和真实模型证据满足合同，claim ceiling 未扩大。 |
| `V04-13-T01B` | 通过 | 是 | 最终代码、303 项聚焦、1004 项全仓、2 项独立进程、合同/安全/artifact 证据和只读审计满足锁定合同；2 MB artifact 上限与 autonomous task-token 发布边界已写入 claim ceiling。 |
| `V04-13-T01C` | 通过 | 是（浏览器为诚实证据债务） | 独立审计发现并推动修复配对/重连响应丢失、expiry 绑定、跨进程取消竞争、completed-wins 与取消终态事件 drain；最终确定性、真实 ASGI、独立进程 HTTP 和受限 Studio 源码证据满足合同的 scoped evidence floor。 |
| `V04-13-T01D` | 通过 | 是 | 独立审计反向检查正式题激活/普通会话不可发现、未审批隔离、状态/CAS、单快照 inbox、100 次重连/幂等/租约故障、daemon 等待与副作用恢复、因果导出、claim/post-close verifier、历史 replay 的 durable-ack/exact-sender ACL、provenance 防伪、公开工具 envelope/迟到 run/digest 冲突防伪、多应用 exact-state archive 和 claim 创建顺序，以及 121 reports/claims/workflows + 200 decisions 压缩与生产 `create_app` 的完整可达 commit + 同树 gitblob 证据门；最终 147 项聚焦和 1243 项全仓无当前失败，claim ceiling 未扩大到 T01E-H。 |
| `V04-13-T01E` | 待执行 | 待定 | 待定 |
| `V04-13-T01F` | 待执行 | 待定 | 待定 |
| `V04-13-T01G` | 待执行 | 待定 | 待定 |
| `V04-13-T01H` | 待执行 | 待定 | 待定 |
| `V04-13-T01I` | 待执行 | 待定 | 待定 |
| `V04-13-T01J` | 待执行 | 待定 | 待定 |

## 偏移记录

| 偏移 ID | 类型 | 任务 ID | 原合同 | 调整后的路线 | 验收是否保持 | 授权 / 证据 |
| --- | --- | --- | --- | --- | --- | --- |
| `V0413-DEV-001` | D0 协议补全 | `V04-13-T01A-F` | expanded plan 定义了目标、角色和主要 API，但遗漏了 CLI 所需 pair-code/status/stop/credential provisioning，以及 report 补证、莉莉丝 re-probe、题包/环境回复和 claim 独立存储入口。 | 在不改变任何角色、审批或验收的前提下，把这些入口写入锁定合同；publish 使用显式 scope；普通会话的 schema/错误不能泄漏管道存在。 | 是；只补齐实现协议，让原时序可执行。 | 实施前只读 gap map 与 expanded plan 已锁定行为。 |
| `V0413-DEV-002` | T01A 实现加固 | `V04-13-T01A` | daemon.json 需要公钥指纹且本地会话/ack/预算必须跨 token 生命周期、取消和重启保持真实。 | 使用仓库内经 RFC 7748 向量验证的 X25519 公钥派生，避免新增依赖与改写用户 lock；过期 bearer 只可与新一次性 code 组合为 continuity proof，不能调用 API，revoked 不可轮换；schema v3 用 `metrics_settled_at` 原子结算 checkpoint，SSE 只信 durable ack。 | 是；安全和耐久语义更严格，未改变公开角色或验收。 | 负向测试、`architecture-boundary.json`、`deterministic-tests.txt` 和只读审计记录。 |
| `V0413-DEV-003` | T01B 黑箱策略持久性加固 | `V04-13-T01B` | task credential 可运行和发布白名单应用，但 immutable version 尚未保存 assignment workspace/tool/network/nested policy；黑箱也没有 secret-use scope。 | task-token 运行拒绝任何 secret ref；发布前拒绝 schedule/tool/nested/agent/network 等依赖临时 policy 的节点，并要求 exact-content 当前 mandatory pass。该门禁仅约束 autonomous task credential，不改变用户对 Quick/Guided warning 的交互发布决策。全局 secret 解析同时要求 exact owner，nested create 在落盘前拒绝环与深度超过 16。 | 是；原黑箱最小权限和发布安全验收保持，客户发布产品规则未扩大。 | `security-audit-artifact.json`、`architecture-boundary.json`、12 项安全闭环测试与独立审计。 |
| `V0413-DEV-004` | T01C 崩溃与并发恢复加固 | `V04-13-T01C` | 合同要求任一进程重启后 relay/ack 续传、取消与 reconnect 可恢复，但初始路线没有完整枚举 exchange response-loss、跨 platform worker desired-state 竞争和取消终态事件尾部。 | bridge schema v3 增加 terminal drain checkpoint；pair/reconnect 使用加密 prepared bearer outbox 与认证状态恢复；assignment desired state 用 CAS；cancel 409 只接受严格绑定终态；取消后用 connection bearer 有界 drain/commit/strict ack，task bearer 保持撤销。 | 是；不改变公开目标或角色，仅把原 restart/idempotency/ack 验收落实到所有发现的崩溃窗口。 | `t01c/security-and-boundary-summary.json`、51 项 bridge 回归、独立真实 ASGI 和最新独立进程 HTTP 证据。 |
| `V0413-DEV-005` | T01D 可靠性与证据存在性加固 | `V04-13-T01D` | 合同要求完整 DeveloperResponse 的 commit/evidence 必须存在，并要求长会话压缩、重启等待、reader cursor、租约和幂等在故障窗口中保持语义；初始路线未规定生产证据 backing、单快照 inbox、超过 100 个对象的有界表示或 ack 之后的精确恢复来源。 | 生产装配用完整且可达的 Git commit 与同一 tree 内内容寻址 `gitblob` 验证 evidence，Git I/O 移出事件循环；developer inbox 使用同一 SQLite snapshot；daemon 持久化远端副作用 intent/result 与 collaboration deadline；压缩增加有界 report/decision/claim/workflow 索引、只读 history replay、按 exact state digest 的多应用 workflow 分页和事务 provenance；current claim 以创建顺序为准，工作流恢复仅认配对成功的公开工具 envelope。 | 是；没有改变审批权、角色、状态机、三个工具上限或验收，只把原存在性、恢复、压缩和零丢失条件落实到发现的反例。 | `t01d/security-and-boundary-summary.json`、真实 `create_app` HTTP 正反测试、32 项压缩/恢复反例、100 次故障循环与独立只读审计。 |

## 未解决、阻塞与延期

| 任务 ID | 事项 | 状态 | 强制 / 可选 | 原因 | 下一步 / 决策权 |
| --- | --- | --- | --- | --- | --- |
| `V04-13-T01E-J` | 后续六项强制任务尚未实现。 | 已接受 | 强制 | 必须遵守依赖顺序，不能以合作协议候选冒充界面、题包、资格测试或企业结果闭环。 | 用户要求 T01D 提交后暂停；恢复时从 E 继续，随后 F/G/H/I/J 按合同闭环。 |

本节中的强制任务会阻止版本归档。只有用户批准阶段合同修订，并且原始意图仍被保留，才能删除或替换这些任务。

## 证据债务

| 证据债务 ID | 任务 ID | 目标等级 | 已达到等级 | 状态 | 不可用依赖 | 声明上限 | 负责人 / 承接目标 | 复查触发条件 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `V0413-ED-001` | `V04-13-T01H` | 冻结 Paperless/InvenTree 真实宿主和三个隐藏 seed | 设计与静态环境审查 | 环境准备中 | 当前 Docker daemon 不可达，题包、宿主栈与隐藏 seed 尚未创建 | 不能声明真实企业题目已运行或通过 | T01F/H | Docker daemon 可用且 environment-ready 预检生成时 |
| `V0413-ED-002` | `V04-13-T01C` | 用户操作的 Studio pairing/build/reconnect/cancel 浏览器路径 | 前端源码、TypeScript、生产构建、API、真实 ASGI 与独立进程 HTTP | `blocked_by_environment` | browser runtime 未暴露可用 browser | 不声明 Studio browser pass；T01C 仅在 deterministic/ASGI/HTTP scoped evidence floor 闭环 | T01E/I | browser runtime 可用时重跑相同隔离路径和 desktop/mobile/a11y 证据 |

证据债务只记录高于合同闭环底线的验证目标。它不能隐藏未完成的强制行为，也不能把尚未获得的证据写成已经通过。

## 意图覆盖

| 来源意图 ID | 阶段前 | 阶段后 | 证据 | 剩余差距 |
| --- | --- | --- | --- | --- |
| `PRODUCT-001` | AgentRuntime 与 legacy Builder 在平台进程内存在。 | 独立本地莉莉丝 daemon/CLI 能持续对话和重启恢复；隔离进程仅凭 16 项 HTTP 合同完成最小 Build/修复，平台现能安全创建严格 assignment、续传 relay/ack 并在不可用后沿同一 ID 恢复。 | T01A 源码/CLI/live/recovery、T01B process-boundary/contract 与 T01C bridge/process 证据。 | H 的真实企业题仍待实现。 |
| `PRODUCT-005` | 有内部修复机制，没有用户审批的跨开发补能闭环。 | 已实现正式题限定的持久报告、补证、用户批准/题内 auto-forward、隔离 developer lease/response、莉莉丝复验和 claim 独立结论协议；DeveloperResponse 必须绑定真实 commit/evidence。 | T01D 源码、147 项聚焦、真实 HTTP 与独立审计。 | E 的用户/开发界面、G 的机器资格包和 H 的真实动态补能仍待实现。 |
| `PRODUCT-008` | 三类产品界面存在，没有本地莉莉丝全局协作监控。 | Studio 已有本地莉莉丝 assignment 状态桥；后端现可提供单快照 channel/report/timeline/inbox、审批和因果导出，但全局合作监控页面与浏览器证据仍属 E/I。 | T01C 前端契约/build、T01D collaboration API/隔离证据与 scoped browser evidence debt。 | E 的合作监控 UI/developer CLI 和 I 的完整视觉/浏览器闭环尚待实现。 |
| `PRODUCT-015` | 有受控局部实验，没有莉莉丝真实企业工作流独立结果。 | T01D 已提供应用/草稿精确绑定、claim 冻结/失效、独立 verifier 角色和关闭后例外写入的确定性协议；题包、归档、隐藏 oracle 和 36 条企业题仍是后续强制闭环。 | T01D claim/事务/HTTP 证据与 F/H/J 合同。 | F 的题包/verifier 进程和 H 的真实三 seed 企业实验尚待执行。 |
| `PRODUCT-016` | REST/OpenAPI 自动 Connector 已完成受控资格。 | 莉莉丝已有 role-filtered 黑箱合同、scoped HTTP 工具、通用 failure envelope 和平台 assignment 接耦；正式真实题仍须暴露并恢复通用缺口。 | T01B contract/process 与 T01C bridge/process 证据及 H 合同。 | H 真实题尚待实现。 |
| `ARCH-003` | 能力放置原则已有报告约束。 | 共用 agent core、独立本地 storage/service/API、HTTP-only 黑箱客户端、平台 daemon bridge，以及独立 collaboration models/service/storage/API 已按进程与数据职责落地；题包和 verifier 继续保持独立边界。 | T01A-D 源码、架构扫描、进程/HTTP/SQLite 证据。 | E-J 其余界面、题包、实验和发布模块尚待实现。 |
| `EVAL-003` | 有生成式验收和合同测试。 | 资格测试、隐藏 oracle 和全分母已进入强制验收。 | F/G/H/J 合同。 | 尚待实现和运行。 |

来源任务集、阶段合同和本表之间，任何来源意图都不允许无故消失。

## 实验与产品状态更新

| 台账 / 界面 | 更新 | 证据 |
| --- | --- | --- |
| 本地莉莉丝 | T01A 独立 daemon/CLI 与真实模型重启、T01B assignment-scoped HTTP contract/client、T01C 平台自动创建/投递/恢复 BuildAssignment 均完成 scoped Closure Audit。浏览器路径仍保留诚实证据债务。 | `docs/evidence/v0.4.13/t01a/`、`t01b/`、`t01c/`。 |
| 临时合作管道 | T01D 持久协议、审批/隔离、租约/回复、恢复、claim 和因果导出已完成确定性闭环；普通任务在 assignment、prompt、工具、OpenAPI 和错误投影中不可发现。监控 UI 与 Q01-Q23 bundle 仍属 E/G。 | `docs/evidence/v0.4.13/t01d/`。 |
| EXP-LILIES-001 | 题目和 oracle 合同已批准，尚未创建运行包。 | T01F/H。 |

## 历史设计

| 历史设计 | 最终状态 | 证据 |
| --- | --- | --- |
| 无 | v0.4.13 expanded plan 仍是当前 active design。 | `docs/current-design/v0.4.13_lilies_local_agent_and_collaboration_pipeline_expanded_plan.md`。 |

## 工作记录归档

| 归档 | 内容 |
| --- | --- |
| `docs/evidence/v0.4.13/t01a/` | T01A 聚焦/全量结果、独立 CLI transcript、真实模型、恢复、架构边界与 SHA manifest；不包含 API key、access token 或未脱敏 pairing code。 |
| `docs/evidence/v0.4.13/t01b/` | T01B 合同/授权/安全/artifact、独立 HTTP Build/修复、精确回归、只读审计与 SHA manifest；不包含 task token、API key、随机资源 ID 或平台数据库。 |
| `docs/evidence/v0.4.13/t01c/` | T01C 确定性/真实 ASGI/独立进程 HTTP、terminal relay/ack、restart/unavailable/reconnect、安全边界、Studio 环境债务和只读审计；不包含 bearer、pairing code、task token、envelope key 或平台数据库。 |
| `docs/evidence/v0.4.13/t01d/` | T01D 147 项聚焦、1243 项全仓、100 次重连/幂等/租约故障、真实 HTTP commit/gitblob 正反路径、32 项压缩/恢复反例、安全边界和独立只读审计；不包含 bearer、token、平台数据库、私有推理或隐藏 oracle。 |

## 下一阶段任务集

| 任务 ID | 来源意图 ID 列表 | 任务 | 为什么现在做 | 闭环目标 | 强制 / 可选 |
| --- | --- | --- | --- | --- | --- |
| `V04-14-T01` | `PRODUCT-016`, `ARCH-008`, `SCENARIO-011` | v0.4.13 完成后按既定顺序扩展 Home Assistant WebSocket；其后才是 ThingsBoard MQTT 与 Actual SDK/CLI，功能正确后才授权原流程/替换流程性能比较。 | 用户要求 v0.4.13 不能吞掉或改写后续非 HTTP 顺序。 | 保持接口自动对齐从 REST 扩展到事件协议，同时仍由平台发现、映射和验证，不允许实验人员预写 adapter。 | 强制 |

本表是下一阶段任务的唯一权威来源。每项任务必须使用稳定的任务 ID 和来源意图 ID；workingon 和 current-design 不能另行定义下一阶段任务。

## 归档提交

- 提交:
- 闭环验证器:
- 意图覆盖验证器:
- 当前 current-design 是否干净:
- 当前 workingon 是否干净:

## 自动演进交接

- 是否继续: 否；用户要求 T01D 刷新证据并提交后暂时暂停
- 当前任务 ID: `V04-13-T01E`
- 下一版本: `v0.4.14`
- 第一任务 ID: `V04-14-T01`
- 恢复所依据的阶段报告: `docs/stage-reports/v0.4.13_lilies_local_agent_and_collaboration_pipeline.md`
- 停止原因: T01A-D 已按合同和独立证据完成；用户于 2026-07-23 要求提交 T01D 后暂停。恢复时必须从当前强制任务 T01E 继续，不得把暂停写成 deferred、blocked 或版本完成
