# v0.2.17 Platform Harness Tool Egress Policy

## 1. Stage Goal

在 Automatic Evolution Mode 下，从 `v0.2.16` 的 Next-stage Task Pool 选择首项任务：把 WebSearch / HTTP MCP 这类工具级网络出站纳入 Platform Harness egress policy。

## 2. Completed Items

| Item | Evidence | Status |
| --- | --- | --- |
| AgentRuntime WebSearch egress | `AgentRuntime._enforce_tool_network_policy()` | completed |
| AgentRuntime HTTP MCP egress | MCP server URL hostname enforcement | completed |
| WorkflowRuntime Tool block egress | `WorkflowRuntime._enforce_tool_network_policy()` | completed |
| Regression coverage | `test_platform_harness_tool_egress_policy_blocks_websearch_tool` | completed |
| Design archive gate applied | `docs/historical-designs/v0.2.17_design_platform_harness_tool_egress_policy_v1.md` | completed |

## 3. Verification Evidence

Focused policy tests:

```bash
.venv/bin/python -m pytest tests/test_workflow.py::test_platform_harness_tool_egress_policy_blocks_websearch_tool tests/test_workflow.py::test_platform_harness_network_egress_policy_blocks_http_requests -q
```

Result:

- `2 passed, 1 warning`

Backend regression:

```bash
.venv/bin/python -m compileall -q platform/backend/src/agent_platform tests
.venv/bin/python -m ruff check platform/backend/src/agent_platform/runtime.py platform/backend/src/agent_platform/workflow_runtime.py tests/test_workflow.py
.venv/bin/python -m pytest -q
```

Result:

- compile passed
- ruff passed
- `64 passed, 1 warning`

Paid/live model test:

- Not required. This stage is deterministic Platform Harness policy enforcement.

## 4. Incomplete Items

| Item | Reason | Next Step |
| --- | --- | --- |
| Stdio MCP egress | No declared hostname | Treat as sandbox/process policy |
| Sandbox/container network firewall | Requires container runtime controls | Later Platform Harness stage |
| UI policy controls | Backend only | Add after policy model stabilizes |

## 5. Next-stage Tasks

1. Add Natural-language Draft Patch preview UI with preview-confirm-apply flow.
2. Plan worker lease / durable execution semantics as a later phase.
3. Consider benchmark history UI after frontend priority is confirmed.
4. Design secret store and secret reference injection after policy boundary stabilizes.
5. Design sandbox/container egress controls for stdio tools.

## 6. Intellectual Asset Candidates

| Candidate | Evidence Chain | Decision |
| --- | --- | --- |
| Network egress hard boundary | v0.2.16 HTTP block + v0.2.17 tool-level egress | Candidate; wait for sandbox/container egress design |

## 7. Historical Designs

Archive readiness:

- Previous stage design archive audited: `v0.2.16` has historical design record.
- Corresponding version/state document exists: yes.
- Version or stage marker: `v0.2.17_platform_harness_tool_egress_policy`.

| Historical design path | Source current-design | Final status | Evidence |
| --- | --- | --- | --- |
| `docs/historical-designs/v0.2.17_design_platform_harness_tool_egress_policy_v1.md` | `docs/current-design/design_platform_harness_tool_egress_policy_v1.md` | implemented | `docs/workingon/work_platform_harness_tool_egress_policy_2026_07_09.md` |

## 8. Archive Commit

- Auto commit required: yes.
- Staged paths verified with `git diff --cached --name-status`: required before commit.
- Commit message: `docs: archive v0.2.17 platform harness tool egress`.
- Commit hash: report from `git log` after archive commit.

## 9. Automatic Evolution Handoff

- Automatic Evolution Mode active: yes.
- Continue to next version: yes, unless user pauses.
- Next version: `v0.2.18`.
- Next task source: this stage report next-stage task pool.
- First workingon file to create/update: likely `docs/workingon/work_natural_language_draft_patch_preview_ui_2026_07_09.md`.

