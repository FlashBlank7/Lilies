# v0.2.40 Builder Provider Timeout Boundary

## 1. Stage Goal

Fix the Builder/provider timeout task boundary surfaced by v0.2.39 paid/live validation.

This stage closes a backend + Platform Harness boundary slice: model stream stalls and provider timeout errors are now observable and classified. It does not close the original E05 Template reuse-depth quality question.

## 2. Full Task Set Disposition

Source: `docs/stage-report-archives/v0.2.x/v0.2.39_template_reuse_expandability_contract.md`

| Prior next-stage task | Disposition in v0.2.40 | Evidence |
| --- | --- | --- |
| Fix Builder/provider timeout task boundary | Completed | Runtime code, Builder Harness metadata, deterministic tests |
| Continue E05 success-condition validation after timeout handling | Deferred to v0.2.41 | Timeout boundary is now fixed; next stage should run bounded E05 validation |
| Run E08 workflow-internal gate vs sidecar monitor/passmode comparison | Deferred | separate Harness experiment stage |
| Continue deferred Platform Harness product tasks with explicit closure level | Deferred | separate product/platform boundary stage |
| Run actual E02 human-panel review if a human reviewer pool is available | Deferred | no human reviewer pool in this execution context |
| Broaden E04 failure classes | Deferred | separate repair-policy experiment |
| Add more complex plan-first cases | Deferred | optional product-strategy evidence |

## 3. Completed Items

- Added a deterministic timeout boundary to `AgentRuntime._collect_stream()`.
- Added `<event_prefix>.timeout` stream events for model stream timeout.
- Added `<event_prefix>.failed` stream events for `ProviderError`.
- Converted model stream timeout into retryable `ProviderError`.
- Added Builder failure classification for provider/model errors.
- Wrote structured failure metadata into Platform Harness task metadata and `build.needs_attention` events.
- Added deterministic regression tests for runtime timeout behavior and Builder/Harness provider-timeout metadata.

## 4. Code And Artifact Changes

- `platform/backend/src/agent_platform/runtime.py`
- `platform/backend/src/agent_platform/builder.py`
- `tests/test_runtime.py`
- `tests/test_workflow.py`
- `docs/experiment-status/v0.2_experiment_status.md`
- `docs/historical-designs/v0.2.40_design_model_stream_timeout_boundary_v1.md`
- `docs/historical-designs/v0.2.40_design_builder_timeout_harness_metadata_v1.md`
- `docs/workingon-archives/v0.2.40/work_v0.2.40_builder_provider_timeout_boundary.md`
- `docs/workingon-archives/v0.2.40/implementation_v0.2.40_builder_provider_timeout_boundary.md`

## 5. Verification

Focused deterministic tests:

```bash
.venv/bin/python -m pytest tests/test_runtime.py::test_collect_stream_timeout_emits_retryable_provider_error tests/test_workflow.py::test_builder_records_provider_timeout_in_harness_metadata -q
```

Result: `2 passed, 1 warning`.

Full backend regression:

```bash
.venv/bin/python -m pytest -q
```

Result: `101 passed, 1 warning`.

Static compile:

```bash
.venv/bin/python -m compileall platform/backend/src/agent_platform tests scripts
```

Result: passed.

## 6. Paid/Live Boundary

No new paid/live provider call was run in v0.2.40.

Reason:

- v0.2.39 already produced paid/live DeepSeek timeout evidence during the E05 shallow arm.
- v0.2.40 changes the deterministic runtime and Platform Harness boundary around that observed failure.
- Forcing a new real provider timeout would be nondeterministic and would not add confidence to the code path.

Next paid/live action:

- v0.2.41 should run bounded E05 success-condition validation after this timeout boundary fix.

## 7. Historical Designs

Archived with this stage:

- `docs/historical-designs/v0.2.40_design_model_stream_timeout_boundary_v1.md`
- `docs/historical-designs/v0.2.40_design_builder_timeout_harness_metadata_v1.md`

## 8. Workingon Archive

- `docs/workingon-archives/v0.2.40/work_v0.2.40_builder_provider_timeout_boundary.md`
- `docs/workingon-archives/v0.2.40/implementation_v0.2.40_builder_provider_timeout_boundary.md`

## 9. Incomplete Or Deferred Items

- Original E05 remains open: reuse-depth quality and success-condition stability still need paid/live validation after this fix.
- UI does not yet expose structured timeout metadata.
- E08 workflow-internal gate vs sidecar monitor/passmode comparison remains open.
- Platform Harness product tasks remain open beyond this backend/platform boundary slice.
- E02 real human-panel timing remains open.
- E04 broader failure-class comparison remains open.

## 10. Engineering Closure

Claimed closure level: `backend slice + platform boundary slice`.

Actual closure:

- Completed: runtime timeout boundary, provider failure event, Builder failure classification, Platform Harness task metadata, deterministic tests, full regression.
- Not completed: UI surfacing, broad provider resiliency redesign, E05 post-timeout paid/live conclusion.

Application marker: `engineering prerequisite completed / original E05 not closed`.

## 11. Next-stage Tasks

The next stage must select from the full remaining task set:

1. Continue E05 success-condition validation after timeout handling:
   - rerun or re-evaluate shallow/deep with the same evidence structure;
   - include the now-structured timeout failure evidence if a provider timeout occurs;
   - do not mark original E05 closed until multiple cases or explicit scope limits are recorded.
2. Run E08 workflow-internal gate vs sidecar monitor/passmode comparison.
3. Continue deferred Platform Harness product tasks with explicit closure level.
4. Optionally run actual E02 human-panel review if a human reviewer pool becomes available.
5. Optionally broaden E04 failure classes.
6. Optionally add more complex plan-first cases before making a product-default E01 decision.

## 12. Automatic Evolution Handoff

Automatic Evolution Mode should continue to `v0.2.41`.

Recommended next version: `v0.2.41_e05_success_condition_after_timeout_boundary`.

Reason: v0.2.40 completed the deterministic timeout boundary that v0.2.39 exposed. The next credible step is to return to E05 and validate Template reuse-depth success conditions with this boundary in place.
