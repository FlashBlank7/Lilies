V04-13-T01D deterministic verification
Captured at (UTC): 2026-07-23T00:59:33Z
Baseline commit: 91d68006de539df050b84c3791894b8dc81aaa63

1. Collaboration focused suite

Command:
  uv run --extra dev --frozen python -m pytest -q tests/test_v04_13_collaboration_*.py

Result:
  147 passed, 2 warnings, 0 failed in 25.87s

Coverage includes strict schemas and access matrices; formal-only activation;
credential/header isolation; deterministic report routing; explicit
observed -> evidence_collecting -> platform-validation revisions; manual and
task-local auto-forward approval; report withdrawal; durable seq/cursors/SSE;
developer inbox isolation, single-snapshot projection and leases;
DeveloperResponse, task amendment, environment response and independent Lilies
rerun; reachable-commit and same-tree gitblob evidence resolution through the
real create_app HTTP path; application-bound claim freeze/invalidation/result;
post-close verifier completion; causal export; OpenAPI non-discoverability;
daemon side-effect recovery, deadline handling, semantic compaction and
projection redaction; bounded read-only history replay before a durable
acknowledgement; exact-sender verifier-claim isolation before SQL LIMIT;
schema-max current claim and workflow recall; provenance-bound approval
replayability; paired-success-only transcript normalization; late workflow-run
origin rejection; fail-closed conflicting inner/outer contract digests; and
SQLite migration. Multi-application workflow recall uses a paginated archive
index plus an exact state digest instead of the transcript tail, and current
claim ordering follows claim creation rather than a later update to an older
claim.

The focused suite includes deterministic fault tests with:
  - 100 subscriber overflow/reconnect rounds and contiguous durable recovery;
  - 100 developer-lease retry/renew/expiry/stale-owner recovery rounds;
  - 100-way identical report submission across independent stores/services;
  - concurrent revise, approval, release, and close receipt replay;
  - same-key payload drift rejection without duplicate side effects;
  - missing commit, missing evidence blob, and mismatched evidence digest leave
    the report implementing and preserve the active lease for retry;
  - 32 compaction/recovery counterexample tests, including 121 reports, 121
    claims, 121 workflow states, 200 decisions, a 10000-character exact
    business goal, and 100 maximum-evidence attempts under the 30000-character
    model-summary ceiling;
  - 200 approval-shaped records in both a no-channel session and a formal but
    unprovenanced transcript remain inline with zero decision omission;
  - schema-max 500 test-run plus 500 business-run identifiers are exactly
    recoverable for the current claim and an exact selected workflow state
    without adding a fourth collaboration tool;
  - a target application with 500 summarized run IDs remains exactly pageable
    after a later successful inspection of a second application;
  - claim1 -> claim2 -> claim1 invalidation retains claim2 as the current claim;
  - a strict Git `ls-tree` parser rejects prefix/spoof object identities rather
    than accepting a line that merely contains the expected object ID.

2. Storage and security transaction subset

Command:
  uv run --extra dev --frozen python -m pytest -q \
    tests/test_v04_13_collaboration_storage_transactions.py \
    tests/test_v04_13_collaboration_storage_security.py

Result:
  21 passed, 0 failed in 2.05s

3. Durable retry logical-clock regression

Command:
  uv run --extra dev --frozen python -m pytest -q \
    tests/test_v04_09_durable_daily_collection.py

Result:
  9 passed, 1 warning, 0 failed in 5.14s

The retry/lease test uses a synthetic logical clock, so wall-clock scheduling
cannot make an already-advanced test timestamp appear to move backwards while
the stale-owner and revision-fencing assertions remain intact.

4. Full repository regression

Initial command:
  uv run --extra dev --frozen python -m pytest -q

Collection result:
  Existing repository test tests/test_scenario_capability_report_revision.py
  imports python-docx, but pyproject.toml does not declare it in the dev extra.
  Collection stopped with ModuleNotFoundError: docx before tests ran. No lock or
  dependency file was changed because the untracked uv.lock belongs to the user.

Re-run with an ephemeral test-only dependency:
  uv run --extra dev --with python-docx --frozen python -m pytest -q

Result:
  1243 passed, 85 xfailed, 2 warnings, 0 failed in 242.33s

The xfails are the repository's existing expected cases. The two warnings are
the existing Starlette TestClient deprecation and duplicate fallback OpenAPI
operation ID warning. The undeclared python-docx test dependency is a truthful
repository baseline/dependency debt for the later release gate; it is not
reported as a passing default dev-extra collection.

The final focused and full runs were executed only after the shared working
tree was stable. The aggregate SHA-256 over the 17 T01D source/test file hashes
is `a0dc30ee4b26b8b849c6f220d2e7405c3bc90f9e8da843f92b273e25c1b550cd`;
the individual hashes were unchanged across the final full run. Earlier runs
taken while another process was still editing the same files, and the red
counterexamples that drove replay, provenance, multi-application archive
selection, and claim-ordering fixes, were excluded from closure evidence.

5. Static and evolution checks

Commands:
  uv run --extra dev --frozen python -m ruff check \
    platform/backend/src/agent_platform/api.py \
    platform/backend/src/agent_platform/config.py \
    platform/backend/src/agent_platform/collaboration_api.py \
    platform/backend/src/agent_platform/collaboration_models.py \
    platform/backend/src/agent_platform/collaboration_service.py \
    platform/backend/src/agent_platform/collaboration_storage.py \
    platform/backend/src/agent_platform/lilies_collaboration_client.py \
    platform/backend/src/agent_platform/lilies_collaboration_tools.py \
    platform/backend/src/agent_platform/lilies_identity.py \
    platform/backend/src/agent_platform/lilies_service.py \
    platform/backend/src/agent_platform/lilies_storage.py \
    platform/backend/src/agent_platform/workflow_storage.py \
    tests/test_v04_13_collaboration_*.py
  git diff --check
  uv run --extra dev --frozen python scripts/validate_evolution_control.py

Results:
  Ruff: All checks passed.
  git diff --check: passed.
  evolution control validation passed.
