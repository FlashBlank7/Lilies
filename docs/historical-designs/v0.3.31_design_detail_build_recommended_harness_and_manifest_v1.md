# design_v0331_detail_build_recommended_harness_and_manifest

状态：completed

## Source

- Source stage report: `docs/stage-reports/v0.3.30_detail_build_action_state_explainer.md`
- Source tasks:
  - `Extend recommended-action harness`
  - `Preserve regression lane manifest`
  - `Preserve stage-report authority`

## Problem

Recommended build action mapping touches model-start safety and needs deterministic coverage.

## Boundary

- No browser dependency.
- No backend writes, builds, tests, runs, publish, restore, draft mutation, or model calls.
- Live mode may only read `/health`.

## Solution

- Add v0.3.31 audit script with recommendation fixtures, source/i18n/style markers, and no-write safety.
- Add focused tests.
- Update the current release gate manifest and relax older exact pass-count markers.

## Acceptance Criteria

- Focused v0.3.31 tests pass.
- Current release gate includes v0.3.31.
- Live evidence is read-only and records no forbidden endpoints.

## Final State

- Added `scripts/v03_31_detail_build_recommended_action.py`.
- Added `tests/test_v03_31_detail_build_recommended_action.py`.
- Updated `docs/testing/regression_lanes.json` so the current gate starts at v0.3.31 and expects 166 passes.
- Relaxed v0.3.30 manifest marker to the stable current-lane id so later pass-count changes do not break archived tests.
