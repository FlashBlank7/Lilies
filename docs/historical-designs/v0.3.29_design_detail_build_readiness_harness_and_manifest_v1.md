# design_v0329_detail_build_readiness_harness_and_manifest

状态：completed

## Source

- Source stage report: `docs/stage-report-archives/v0.3.x/v0.3.28_safe_draft_landing_dismissal.md`
- Source tasks:
  - `Extend detail build readiness harness`
  - `Preserve regression lane manifest`
  - `Preserve stage-report authority`

## Problem

Detail Build tab readiness affects model-start safety and needs deterministic regression coverage.

## Boundary

- No browser dependency.
- No backend writes, builds, tests, runs, publish, restore, draft mutation, or model calls.
- Live mode may only read `/health`.

## Solution

- Add v0.3.29 audit script with readiness fixtures, source/i18n/style markers, and no-write safety.
- Add focused tests.
- Update the current release gate manifest and relax older exact pass-count markers.

## Acceptance Criteria

- Focused v0.3.29 tests pass.
- Current release gate includes v0.3.29.
- Live evidence is read-only and records no forbidden endpoints.

## Final State

- Added `scripts/v03_29_detail_build_readiness.py`.
- Added `tests/test_v03_29_detail_build_readiness.py`.
- Updated `docs/testing/regression_lanes.json` so the current gate starts at v0.3.29 and expects 154 passes.
- Relaxed v0.3.28 manifest marker to the stable current-lane id so later pass-count changes do not break archived tests.
