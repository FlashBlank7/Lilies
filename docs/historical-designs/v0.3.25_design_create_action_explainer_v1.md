# design_v0325_create_action_explainer

状态：completed

## Source

- Source stage report: `docs/stage-reports/v0.3.24_requirement_readiness_summary.md`
- Source tasks:
  - `Start v0.3.25_create_action_state_explainer`
  - `Add create action explainer`

## Problem

The create card has readiness feedback and two actions, but users still need a single explanation of what the current action state means.

## Boundary

- Use local state only.
- No backend changes.
- Do not alter button safety behavior.

## Solution

- Add a create action explainer below readiness.
- Reflect requirement length, readiness, busy state, and build confirmation.

## Acceptance Criteria

- Explainer updates from current local state.
- Copy is bilingual.
- No model/backend call is introduced.

## Final State

- Implemented in `platform/frontend/app/page.tsx` as `createActionState` plus `data-create-action-state`.
- Bilingual copy is implemented in `platform/frontend/lib/i18n.ts`.
- Styling is implemented in `platform/frontend/app/globals.css`.
- Verification is recorded by `scripts/v03_25_create_action_state.py` and `tests/test_v03_25_create_action_state.py`.
