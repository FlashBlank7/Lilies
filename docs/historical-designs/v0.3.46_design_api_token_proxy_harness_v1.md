# design_v0346_api_token_proxy_harness

Status: completed

## Source

- Source stage report: `docs/stage-reports/v0.3.45_try_run_input_error_recovery_ready_state.md`
- User interrupt:
  - `咋每次跑到一半要api token？`
  - `修复`

## Problem

The token proxy fix is easy to regress because the runtime symptom appears only after protected routes or streams run. It needs source-level evidence and a release-gate test.

## Boundary

- Static/source harness plus bounded live `/health` evidence only.
- No model calls.
- No mutation endpoints during live evidence.

## Proposed Solution

- Add `scripts/v03_46_api_token_proxy_env_resolution.py`.
- Add `tests/test_v03_46_api_token_proxy_env_resolution.py`.
- Require markers for token priority, `.env` upward search, query stripping, server-side-only imports, and regression manifest growth.
- Generate working evidence under `docs/workingon/`, then archive it at version close.

## Acceptance Criteria

- Focused v0.3.46 tests pass.
- Current v0.3.x release gate includes v0.3.46 and passes with updated expected count.
- Live evidence calls only `GET /health`.
- `docs/current-design/` and `docs/workingon/` are clean after archive.

## Final Evidence

- Harness: `scripts/v03_46_api_token_proxy_env_resolution.py`
- Tests: `tests/test_v03_46_api_token_proxy_env_resolution.py`
- Manifest: `docs/testing/regression_lanes.json`
- Evidence: `docs/workingon-archives/v0.3.46/api_token_proxy_env_resolution_v0.3.46.json`
