# design_worker_runner_cli_v1

Historical metadata:

- Source stage: `docs/stage-reports/v0.2.27_worker_runner_cli_and_handler.md`
- Original path: `docs/current-design/design_worker_runner_cli_v1.md`
- Final status: implemented
- Archived as: `docs/historical-designs/v0.2.27_design_worker_runner_cli_v1.md`
- Implementation evidence: `docs/workingon-archives/v0.2.27/work_v0.2.27_worker_runner_cli_and_handler.md`
- Verification evidence: worker helper import test passed; full backend regression passed.
- Remaining risk: script registered but not packaged/installed in this run.

## 1. Goal

Add an operator-usable worker runner entrypoint.

## 2. Module Boundary

Backend/package:

- `platform/backend/src/agent_platform/worker_runner.py`
- `pyproject.toml`

## 3. Implementation Plan

1. Add `run_worker_once()` helper that initializes services outside FastAPI lifespan.
2. Add `main()` with argparse options for worker id, lease seconds, kind, limit, once/loop, and poll seconds.
3. Add `agent-platform-worker` project script.
4. Print JSON run results for operator visibility.

## 4. Acceptance Criteria

- Entrypoint imports without starting FastAPI. Completed.
- One-shot runner can be called from tests. Completed.
- Script name is registered in `pyproject.toml`. Completed.

## 5. Implementation Result

Status: implemented.

Changed files:

- `platform/backend/src/agent_platform/worker_runner.py`
- `pyproject.toml`

Verification:

- `test_platform_worker_runner_helper_imports` passed.
- Full backend pytest passed: `84 passed, 1 warning`.

Remaining risk:

- The CLI entrypoint is implemented but not packaged/installed in this run.

## 6. Referenced Intellectual Assets

- `docs/intellectual-assets/asset_platform_harness_task_monitor_boundary.md`

## 7. No Next-stage Authority

This design expands one accepted v0.2.27 task only.
