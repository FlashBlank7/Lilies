# design_v0329_detail_build_requirement_readiness

状态：completed

## Source

- Source stage report: `docs/stage-reports/v0.3.28_safe_draft_landing_dismissal.md`
- Source tasks:
  - `Start v0.3.29_detail_build_requirement_readiness`
  - `Add detail build readiness summary`

## Problem

Users can route from safe draft to the Build tab, but that tab has a plain requirement textarea before the model-team start guard.

## Boundary

- Use local requirement text only.
- Do not call backend/model APIs.
- Preserve the existing two-step build-intent guard.

## Solution

- Add detail build requirement readiness calculation.
- Render a readiness summary under the Build tab textarea.
- Reuse the existing four signals: audience, outcome, acceptance, and detail length.

## Acceptance Criteria

- Readiness appears in the Build tab before team-start guard.
- Ready and needs-detail states are visible.
- No backend/model call is introduced.

## Final State

- Implemented in `platform/frontend/app/applications/[id]/page.tsx` as `detailBuildRequirementReadiness`.
- Rendered in the Build tab with `data-detail-build-readiness="summary"`.
- Reuses existing requirement-readiness copy and visual states.
- Verification is recorded by `scripts/v03_29_detail_build_readiness.py` and `tests/test_v03_29_detail_build_readiness.py`.
