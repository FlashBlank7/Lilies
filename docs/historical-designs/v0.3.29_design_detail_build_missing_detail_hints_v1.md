# design_v0329_detail_build_missing_detail_hints

状态：completed

## Source

- Source stage report: `docs/stage-report-archives/v0.3.x/v0.3.28_safe_draft_landing_dismissal.md`
- Source tasks:
  - `Add detail build missing-detail hints`

## Problem

If the build requirement is weak, users need concrete hints before confirming model-team startup.

## Boundary

- Hints are local and advisory.
- Do not disable the existing team-start button beyond current behavior.
- Do not add a new build path.

## Solution

- Show the same signal list used by the create card.
- Mark each signal ready or needing detail.
- Keep the list compact inside the Build tab.

## Acceptance Criteria

- Missing customer, outcome, acceptance, and detail signals are visible.
- Signals update from the Build tab requirement textarea.
- Existing build guard remains unchanged.

## Final State

- Added four local signal cards inside the Build tab.
- Signals update from the existing Build tab requirement state.
- Existing `build-intent-guard` remains unchanged and follows the readiness summary.
- Verification is archived in `docs/workingon-archives/v0.3.29/detail_build_readiness_v0.3.29.json`.
