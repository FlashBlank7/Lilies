# design_v0330_detail_build_team_start_boundary

状态：completed

## Source

- Source stage report: `docs/stage-reports/v0.3.29_detail_build_requirement_readiness.md`
- Source tasks:
  - `Preserve team-start safety boundary`

## Problem

Action explanation must not make model-team startup easier than the existing two-step guard.

## Boundary

- No new submit button.
- No automatic build calls.
- Existing build button and `build-intent-guard` remain authoritative.

## Solution

- Explainer is text-only.
- Confirm-team state tells users the explicit guarded button starts the team.
- Harness verifies no direct build trigger is added.

## Acceptance Criteria

- Source keeps existing `build-intent-guard`.
- Explainer uses `data-detail-build-action-state`.
- Live evidence calls only `/health`.

## Final State

- Existing `build-intent-guard` and `data-build-action="detail-start-builder-team"` remain authoritative.
- Explainer is text-only and introduces no new submit/build path.
- Live evidence calls only `GET /health`.
