# design_v037_detail_guidance_persona_harness

状态：active

## Source

- Source stage report: `docs/stage-report-archives/v0.3.x/v0.3.6_runtime_product_health_triage.md`
- Source tasks:
  - `Extend persona harness to inspect detail guidance markers`
  - `Preserve runtime health and smoke cleanup boundaries`

## Problem

v0.3.6 persona evidence verified reachability and draft structure. v0.3.7 must verify that the detail page now exposes comprehension affordances, while preserving runtime health and smoke cleanup boundaries.

## Boundary

No browser-click claim unless an approved browser runtime is available. The harness can use source markers, HTTP-rendered route checks, backend API checks, and smoke cleanup.

## Solution

Add a v0.3.7 harness that:

- checks runtime health,
- verifies detail guidance source markers,
- creates a `v0.3.7-smoke` safe draft,
- opens the draft and rendered detail route,
- verifies no `/builds` calls,
- cleans the smoke app through the v0.3.5 cleanup endpoint.

## Acceptance Criteria

- Runtime health still passes.
- Detail guidance and next-action markers pass.
- Smoke draft is cleaned.
- No-build safety passes.

## Evidence Requirements

- `scripts/v03_7_detail_guidance_persona.py`.
- `tests/test_v03_7_detail_guidance_persona.py`.
- v0.3.7 JSON evidence in `workingon/`.
