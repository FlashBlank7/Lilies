# v0.2.131 E08 Remaining Sidecar Architecture Reselection Design

Status: completed

## Source

- Source stage report: `docs/stage-report-archives/v0.2.x/v0.2.130_e08_external_process_manager.md`
- Source task: `Re-select remaining E08 sidecar architecture slice`
- Current version: `v0.2.131_e08_remaining_sidecar_architecture_reselection`

## Problem

v0.2.130 closed local external worker process lifecycle management. The next version must select the remaining E08 architecture slice without reopening completed queue, supervision, worker handler, control, or process management work.

## Selection Boundary

The selector must treat these architecture slices as completed and excluded:

- `external_process_manager`
- `distributed_queue_semantics`
- `production_worker_supervision`
- required worker task-kind execution coverage
- durable heartbeat registry
- handler catalog coverage
- stdio/container egress allowlist
- local secret KMS/rotation contract
- policy controls API, Studio controls, and operator runbook

The remaining concrete architecture candidate is:

- `external_kms_provider_integration`

## Decision Contract

- Generate deterministic JSON and Markdown evidence.
- Select `external_kms_provider_integration`.
- Hand off `v0.2.132_e08_external_kms_provider_integration`.
- Keep full Platform Harness sidecar completion unclaimed.
- Treat `docs/stage-report-archives/v0.2.x/v0.2.130_e08_external_process_manager.md` as the task source.
- Do not use `docs/workingon/` as a planning source.

## Completion Criteria

- Selector script exists and writes evidence.
- Tests assert completed slices are excluded.
- Tests assert the next selected slice is external KMS provider integration.
- Stage report records this as a selection stage, not an implementation stage.
- Active design and workingon directories are clean after archive.
