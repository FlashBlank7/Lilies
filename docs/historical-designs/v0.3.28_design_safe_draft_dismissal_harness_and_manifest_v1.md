# design_v0328_safe_draft_dismissal_harness_and_manifest

状态：completed

## Source

- Source stage report: `docs/stage-reports/v0.3.27_safe_draft_landing_handoff.md`
- Source tasks:
  - `Extend dismissal harness`
  - `Preserve regression lane manifest`
  - `Preserve stage-report authority`

## Problem

Dismiss and URL cleanup logic can regress silently because browser-based verification is unavailable.

## Boundary

- No browser dependency.
- No backend writes, builds, tests, runs, publish, restore, draft mutation, or model calls.
- Live mode may only read `/health`.

## Solution

- Add v0.3.28 audit script with URL cleanup fixtures, source/i18n/style markers, and no-write safety.
- Add focused tests.
- Update the current release gate manifest and relax older exact pass-count markers.

## Acceptance Criteria

- Focused v0.3.28 tests pass.
- Current release gate includes v0.3.28.
- Live evidence is read-only and records no forbidden endpoints.

## Final State

- Added `scripts/v03_28_safe_draft_dismissal.py`.
- Added `tests/test_v03_28_safe_draft_dismissal.py`.
- Updated `docs/testing/regression_lanes.json` so the current gate starts at v0.3.28 and expects 148 passes.
- Relaxed v0.3.27 manifest marker to the stable current-lane id so later pass-count changes do not break archived tests.
