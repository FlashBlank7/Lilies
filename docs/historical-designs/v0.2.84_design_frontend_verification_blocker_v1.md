# design_v0_2_84_frontend_verification_blocker

## Source

- Source stage report: `docs/stage-report-archives/v0.2.x/v0.2.83_complexity_router_post_shadow_rollout_decision.md`
- Source task: `Restore executable frontend verification when Node is available`
- Version target: `v0.2.84_complexity_router_operator_opt_in_rollout`

## Problem

Frontend verification remains blocked by missing Node/npm, but productization work cannot silently ignore the blocker.

## Boundary

- Retry frontend lint and TypeScript checks.
- Do not claim frontend verification success unless commands actually run.

## Design

- Run `npm run lint || true`.
- Run `node_modules/.bin/tsc --noEmit || true`.
- Record exact results in implementation evidence.

## Acceptance

- Workingon evidence records success or exact blocker output.

## Status

Completed in v0.2.84 as a verified environment blocker.
