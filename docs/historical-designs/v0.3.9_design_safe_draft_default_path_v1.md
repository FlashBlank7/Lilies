# design_v039_safe_draft_default_path

状态：completed

## Source

- Source stage report: `docs/stage-reports/v0.3.8_runtime_connection_status_surface.md`
- Source tasks:
  - `Preserve safe draft as the default low-risk path`

## Problem

v0.3.x introduced safe drafts to avoid unnecessary model calls. A build guard should reinforce that path, not bury it.

## Boundary

No new draft model. Use the existing safe draft function and skeleton behavior.

## Solution

- Keep safe draft button visible beside guarded build action.
- Add copy that clearly separates no-build draft from model/team build.
- Ensure harness checks safe draft markers and no-build endpoint ledger.

## Acceptance Criteria

- Safe draft markers remain present.
- Build guard markers do not replace safe draft markers.
- Harness evidence records no `/builds` calls.

## Evidence Requirements

- Source marker checks.
- No-build live evidence.
