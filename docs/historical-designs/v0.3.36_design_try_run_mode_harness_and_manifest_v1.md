# design_v0336_try_run_mode_harness_and_manifest

状态：completed

## Source

- Source stage report: `docs/stage-report-archives/v0.3.x/v0.3.35_try_result_output_preview.md`
- Source tasks:
  - `Extend Try run mode harness`
  - `Preserve regression lane manifest`

## Problem

Run mode visibility must be protected by deterministic tests and must enter the current v0.3.x release gate.

## Boundary

- Static/source harness plus bounded live `/health` evidence only.
- No model calls.
- No run/build/test/version/draft API mutation during live evidence.

## Solution

- Add `scripts/v03_36_try_run_mode_visibility.py`.
- Add `tests/test_v03_36_try_run_mode_visibility.py`.
- Write live evidence to `docs/workingon/try_run_mode_visibility_v0.3.36.json`, then archive it at version close.
- Update `docs/testing/regression_lanes.json` to prepend the v0.3.36 test file and raise expected passes from 190 to 196.
- Update the v0.3.35 harness to check the stable current-gate id instead of the old exact pass count.

## Acceptance Criteria

- Focused v0.3.36 tests pass.
- Current v0.3.x gate passes at 196 expected tests.
- Live evidence calls only `GET /health`.
- `docs/current-design/` and `docs/workingon/` are clean after archive.

## Final State

- Added `scripts/v03_36_try_run_mode_visibility.py`.
- Added `tests/test_v03_36_try_run_mode_visibility.py` with six deterministic tests.
- Updated `docs/testing/regression_lanes.json` so the current release gate starts at v0.3.36 and expects 196 passing tests.
- Updated the v0.3.35 harness to check the stable current-gate id instead of the old exact pass count.
- Live evidence calls only `GET /health`, with no model, build, run, test, version, restore, or draft mutation endpoint.
