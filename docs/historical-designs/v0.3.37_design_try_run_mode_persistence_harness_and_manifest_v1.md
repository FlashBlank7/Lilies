# design_v0337_try_run_mode_persistence_harness_and_manifest

状态：completed

## Source

- Source stage report: `docs/stage-report-archives/v0.3.x/v0.3.36_try_run_mode_visibility.md`
- Source tasks:
  - `Extend Try run mode persistence harness`
  - `Preserve regression lane manifest`

## Problem

Persistence behavior needs deterministic tests and must enter the current v0.3.x release gate.

## Boundary

- Static/source harness plus bounded live `/health` evidence only.
- No model calls.
- No run/build/test/version/draft API mutation during live evidence.

## Solution

- Add `scripts/v03_37_try_run_mode_persistence.py`.
- Add `tests/test_v03_37_try_run_mode_persistence.py`.
- Write live evidence to `docs/workingon/try_run_mode_persistence_v0.3.37.json`, then archive it at version close.
- Update `docs/testing/regression_lanes.json` to prepend the v0.3.37 test file and raise expected passes from 196 to 202.
- Update the v0.3.36 harness to check the stable current-gate id instead of the old exact pass count.

## Acceptance Criteria

- Focused v0.3.37 tests pass.
- Current v0.3.x gate passes at 202 expected tests.
- Live evidence calls only `GET /health`.
- `docs/current-design/` and `docs/workingon/` are clean after archive.

## Final State

- Added `scripts/v03_37_try_run_mode_persistence.py`.
- Added `tests/test_v03_37_try_run_mode_persistence.py` with six deterministic tests.
- Updated `docs/testing/regression_lanes.json` so the current release gate starts at v0.3.37 and expects 202 passing tests.
- Updated the v0.3.36 harness to check the stable current-gate id instead of the old exact pass count.
- Live evidence calls only `GET /health`, with no model, build, run, test, version, restore, or draft mutation endpoint.
