# design_v0330_detail_build_action_explainer

状态：completed

## Source

- Source stage report: `docs/stage-reports/v0.3.29_detail_build_requirement_readiness.md`
- Source tasks:
  - `Start v0.3.30_detail_build_action_state_explainer`
  - `Add detail build action explainer`

## Problem

The detail Build tab now shows readiness, but users still need to know what the team-start action will do next.

## Boundary

- Local state only.
- No new build path.
- Preserve existing build-intent guard and button.

## Solution

- Add `detailBuildActionState`.
- Show add-detail, improve, arm-team, confirm-team, and busy states.
- Render the explainer before deadline and build guard controls.

## Acceptance Criteria

- Explainer reflects requirement length, readiness, build running state, and guard confirmation.
- Copy is bilingual.
- No backend/model call is introduced.

## Final State

- Implemented in `platform/frontend/app/applications/[id]/page.tsx` as `detailBuildActionState`.
- Rendered in the Build tab with `data-detail-build-action-state`.
- Uses existing action-explainer styling with a detail-specific marker.
- Verification is recorded by `scripts/v03_30_detail_build_action.py` and `tests/test_v03_30_detail_build_action.py`.
