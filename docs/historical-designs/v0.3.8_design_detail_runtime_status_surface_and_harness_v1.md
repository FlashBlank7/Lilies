# design_v038_detail_runtime_status_surface_and_harness

状态：active

## Source

- Source stage report: `docs/stage-reports/v0.3.7_draft_detail_first_run_guidance.md`
- Source tasks:
  - `Add detail-page runtime status marker`
  - `Extend harness to test truthful connection states`
  - `Preserve no-build, runtime health, and smoke cleanup boundaries`

## Problem

The detail page now has better draft guidance, but it still does not visibly say whether the studio is connected to current backend code. If the backend drifts or auth fails, the user may misattribute errors to the workflow.

## Boundary

No browser-click claim. Use source markers, rendered route checks, and API evidence.

## Solution

- Add a compact runtime status chip to the detail header.
- Reuse the home-page classifier.
- Add v0.3.8 harness checks for source/rendered markers, runtime health, no-build safety, and smoke cleanup.

## Acceptance Criteria

- Detail page renders `data-runtime-status`.
- Harness verifies home/detail status markers.
- Runtime health and smoke cleanup are preserved.
- No `/builds` calls are made by v0.3.8 evidence.

## Evidence Requirements

- `scripts/v03_8_runtime_connection_status.py`.
- `tests/test_v03_8_runtime_connection_status.py`.
- v0.3.8 JSON evidence in `workingon/`.
