# design_v0335_try_result_output_harness_and_manifest

状态：completed

## Source

- Source stage report: `docs/stage-reports/v0.3.34_try_result_recovery_affordance.md`
- Source tasks:
  - `Extend Try output preview harness`
  - `Preserve regression lane manifest`

## Problem

Readable output/error preview needs deterministic tests and must enter the current v0.3.x release gate, or raw-JSON-first behavior can return unnoticed.

## Boundary

- Static/source harness plus bounded live `/health` evidence only.
- No model calls.
- No run/build/test/version/draft API mutation during live evidence.

## Solution

- Add `scripts/v03_35_try_result_output_preview.py`.
- Add `tests/test_v03_35_try_result_output_preview.py`.
- Write live evidence to `docs/workingon/try_result_output_preview_v0.3.35.json`, then archive it at version close.
- Update `docs/testing/regression_lanes.json` to prepend the v0.3.35 test file and raise expected passes from 184 to 190.
- Update the v0.3.34 harness to check the stable current-gate id instead of the old exact pass count.

## Acceptance Criteria

- Focused v0.3.35 tests pass.
- Current v0.3.x gate passes at 190 expected tests.
- Live evidence calls only `GET /health`.
- `docs/current-design/` and `docs/workingon/` are clean after archive.

## Final State

- Added `scripts/v03_35_try_result_output_preview.py`.
- Added `tests/test_v03_35_try_result_output_preview.py` with six deterministic tests.
- Updated `docs/testing/regression_lanes.json` so the current release gate starts at v0.3.35 and expects 190 passing tests.
- Updated the v0.3.34 harness to check the stable current-gate id instead of the old exact pass count.
- Live evidence calls only `GET /health`, with no model, build, run, test, version, restore, or draft mutation endpoint.
