# v0.2.103 Manual Refresh and Override Preservation Design

## Source

- Source stage report: `docs/stage-reports/v0.2.102_productization_lane_reselection.md`
- Source task: `Preserve manual refresh and override visibility`
- Current version: `v0.2.103_e05_scheduled_monitoring_hook`

## Problem

Scheduled monitoring must not replace the operator's manual refresh path or hide fixed-depth override options. E05 adaptive default remains reversible only if overrides and manual controls stay visible.

## Boundary

- Manual refresh endpoint remains present.
- Scheduled run-once uses a distinct trigger.
- `available_overrides` must keep `adaptive`, `deep`, `none`, and `shallow`.
- No paid/live model run is required for this deterministic monitoring hook.

## Final Scheme

Verification evidence should inspect the API-level behavior through the existing focused tests and the monitoring helper contract. The hook records deterministic snapshot refreshes over existing evidence.

## Acceptance

- Evidence includes manual refresh preservation.
- Evidence includes fixed-depth override visibility.
- Focused adaptive monitoring tests remain green.

## Final Status

Completed in v0.2.103 archive.
