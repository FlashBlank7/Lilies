# Historical Design Metadata

- Source stage: `v0.2.22_platform_harness_stdio_sandbox_egress`
- Original design path: `docs/current-design/design_platform_harness_stdio_mcp_tests_v1.md`
- Final status: implemented and verified
- Implementation evidence: `docs/workingon-archives/v0.2.22/work_v0.2.22_platform_harness_stdio_sandbox_egress.md`
- Verification: focused stdio MCP guard tests passed; full backend pytest passed
- Remaining risk: no formal experiment was closed by this engineering slice

# design_platform_harness_stdio_mcp_tests_v1

## 1. Goal

Verify stdio MCP egress policy behavior without starting a real untrusted stdio server.

## 2. Module Boundary

Tests:

- `tests/test_runtime.py`
- `tests/test_workflow.py`

Docs/status:

- `docs/workingon/work_v0.2.22_platform_harness_stdio_sandbox_egress.md`
- `docs/experiment-status/v0.2_experiment_status.md`

## 3. Data Flow / Control Flow

1. Construct agents with stdio MCP server metadata.
2. Call runtime policy enforcement or tool execution boundary.
3. Assert restricted policies raise before process start.
4. Assert full/full policy does not raise at policy guard.
5. Run full backend regression.

## 4. Implementation Plan

1. Add focused tests for agent-policy block.
2. Add focused tests for platform-policy block.
3. Add focused test for full/full allow.
4. Update experiment-status to mark stdio MCP guard as an implemented engineering slice, not a formal experiment.

## 5. Acceptance Criteria

- Focused tests pass. Completed.
- Full backend pytest passes. Completed.
- No real external stdio process is required for the block-path tests. Completed.
- Experiment/status ledger distinguishes this guard from full sandboxed stdio MCP execution. Completed.

## 6. Implementation Result

Status: implemented.

Changed files:

- `tests/test_runtime.py`
- `docs/experiment-status/v0.2_experiment_status.md`

Verification:

- Focused stdio MCP guard tests passed: `3 passed`.
- Full backend pytest passed: `73 passed, 1 warning`.

Remaining risk:

- No formal experiment is closed by this engineering slice.

## 7. Referenced Intellectual Assets

- `docs/intellectual-assets/asset_platform_harness_task_monitor_boundary.md`

## 8. No Next-stage Authority

This design expands one accepted v0.2.22 task only. It must not select or guide next-stage work.
