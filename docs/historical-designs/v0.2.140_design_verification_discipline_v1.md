# v0.2.140 Verification Discipline

Status: completed

## Source Stage Report

- `docs/stage-report-archives/v0.2.x/v0.2.139_e10_studio_governed_memory_operator_ui.md`
- Next-stage task: Maintain executable verification discipline.

## Problem

The completion audit is document-heavy, but it still needs executable checks and archive cleanliness. The audit should parse current status docs, verify referenced evidence exists, and fail loudly if required sections drift.

## Tasks

1. Add a generated audit script under `scripts/`.
2. Add focused pytest for the audit script.
3. Generate raw JSON and summary evidence.
4. Validate the stage report template and active workspace cleanup before commit.

## Acceptance

- Focused pytest passes.
- Audit script writes JSON and summary.
- Stage report template validation passes.
- `docs/current-design/` and `docs/workingon/` are README-only after archive.

## Final State

- Focused pytest passed.
- Audit script generated JSON and summary.
- Archive cleanup and stage template validation are required final gates.
