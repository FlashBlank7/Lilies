# v0.2.117 Full Sidecar Boundary Preservation Design

## Source

- Source stage report: `docs/stage-reports/v0.2.116_e08_workflow_run_worker_offload_handler.md`
- Source task: `Preserve full-sidecar boundary`
- Current version: `v0.2.117_e08_remaining_sidecar_slice_reselection`
- Status: completed

## Problem

Completing workflow_run and scheduler workers creates real progress, but not full Platform Harness sidecar productization. Remaining gaps must stay visible.

## Remaining Boundary

The selector keeps these gaps explicit:

- worker-owned `test_suite` handler;
- worker-owned `builder_build` handler;
- worker-owned `benchmark` handler;
- worker-owned `draft_patch_preview` handler;
- production worker supervision;
- distributed queue semantics;
- worker liveness alerting;
- external KMS provider integration.

## Acceptance

- The selected slice is one implementation target, not a broad full-sidecar claim.
- Remaining gaps are preserved in the stage report.
- `e08_full_sidecar_completion_claimed` remains false.

## Final Status

Completed in v0.2.117.

Evidence:

- `docs/workingon/decision_v0.2.117_e08_remaining_sidecar_slice_reselection_summary.md`
