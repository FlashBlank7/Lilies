# design_v0350_runtime_quality_gate

Status: completed

## Source

- Source stage report: `docs/stage-report-archives/v0.3.x/v0.3.49_japanese_learning_customer_journey.md`
- Source task: Add run-result quality checks.

## Problem

The release gate currently proves that the UI and scenario markers exist. It does not yet prove that a no-model Japanese-learning workflow can run and return a learning-card result shape.

## Boundary

- Add deterministic local tests only.
- Do not call Builder Team or paid model APIs.
- Do not call live external video sites.

## Proposed Solution

- Add a v0.3.50 harness and pytest wrapper.
- Add an in-process FastAPI workflow run test that creates a deterministic Japanese-learning workflow and asserts the final answer contains the required learner-result sections.
- Update the current v0.3.x release gate pass count.

## Acceptance Criteria

- v0.3.50 focused tests pass.
- Current release gate includes the new test file.
- Runtime output quality checks cover the learner-readable result shape.

## Final State

- Completed in `v0.3.50`.
- Added v0.3.50 evidence script and pytest wrapper.
- Added an in-process FastAPI/TestClient workflow run that creates and runs a no-model Japanese-learning workflow and asserts the final `answer` contains the required learning-card sections.
- Updated current release gate expected pass count from `279` to `288`.

## Final Evidence

- `scripts/v03_50_bounded_japanese_learning_runtime_validation.py`
- `tests/test_v03_50_bounded_japanese_learning_runtime_validation.py`
- `docs/testing/regression_lanes.json`
- Focused test: `9 passed, 1 warning`
- Current release gate: `288 passed, 1 warning`
