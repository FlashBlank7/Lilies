# design_v0338_try_run_duplicate_start_harness_and_manifest

状态：completed

## Source

- Source stage report: `docs/stage-report-archives/v0.3.x/v0.3.37_try_run_mode_persistence.md`
- Source tasks:
  - `Extend Try duplicate-start harness`
  - `Preserve regression lane manifest`

## Problem

Duplicate-start behavior affects cost and state clarity, so it needs deterministic coverage and must enter the current v0.3.x release gate.

## Boundary

- Static/source harness plus bounded live `/health` evidence only.
- No model calls.
- No run/build/test/version/draft API mutation during live evidence.

## Solution

- Add `scripts/v03_38_try_run_duplicate_start_guard.py`.
- Add `tests/test_v03_38_try_run_duplicate_start_guard.py`.
- Write live evidence to `docs/workingon/try_run_duplicate_start_guard_v0.3.38.json`, then archive it at version close.
- Update `docs/testing/regression_lanes.json` to prepend the v0.3.38 test file and raise expected passes from 202 to 208.
- Update the v0.3.37 harness to check the stable current-gate id instead of the old exact pass count.

## Acceptance Criteria

- Focused v0.3.38 tests pass.
- Current v0.3.x gate passes at 208 expected tests.
- Live evidence calls only `GET /health`.
- `docs/current-design/` and `docs/workingon/` are clean after archive.

## Final State

- Added `scripts/v03_38_try_run_duplicate_start_guard.py`.
- Added `tests/test_v03_38_try_run_duplicate_start_guard.py` with six deterministic tests.
- Updated `docs/testing/regression_lanes.json` so the current release gate starts at v0.3.38 and expects 208 passing tests.
- Updated the v0.3.37 harness to check the stable current-gate id instead of the old exact pass count.
- Live evidence calls only `GET /health`, with no model, build, run, test, version, restore, or draft mutation endpoint.
