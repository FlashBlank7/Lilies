# design_v0332_try_run_sample_harness_and_manifest

状态：completed

## Source

- Source stage report: `docs/stage-report-archives/v0.3.x/v0.3.31_detail_build_recommended_action.md`
- Source tasks:
  - `Extend Try tab sample harness`
  - `Preserve regression lane manifest`

## Problem

The Try tab usability fix must be protected by deterministic tests and must enter the current v0.3.x release gate. Otherwise the UI can regress while the product appears green.

## Boundary

- Static/source harness plus bounded live `/health` evidence only.
- No model calls.
- No run/build/test/version/draft API mutation during live evidence.
- Keep archived v0.2 historical diagnostic failures outside the current release gate.

## Solution

- Add `scripts/v03_32_try_run_sample_input.py`.
- Add `tests/test_v03_32_try_run_sample_input.py`.
- Write live evidence to `docs/workingon/try_run_sample_input_v0.3.32.json`, then archive it at version close.
- Update `docs/testing/regression_lanes.json` to prepend the v0.3.32 test file and raise expected passes from 166 to 172.
- Update previous marker checks to avoid coupling v0.3.31 to the old exact pass count.

## Acceptance Criteria

- Focused v0.3.32 tests pass.
- Current v0.3.x gate passes at 172 expected tests.
- Live evidence calls only `GET /health`.
- `docs/current-design/` and `docs/workingon/` are clean after archive.

## Final State

- Added `scripts/v03_32_try_run_sample_input.py`.
- Added `tests/test_v03_32_try_run_sample_input.py` with six deterministic tests.
- Updated `docs/testing/regression_lanes.json` so the current release gate starts at v0.3.32 and expects 172 passing tests.
- Updated the v0.3.31 harness to check the stable current-gate id instead of the old exact pass count.
- Live evidence calls only `GET /health`, with no model, build, run, test, version, restore, or draft mutation endpoint.
