# design_v0334_try_result_recovery_harness_and_manifest

状态：completed

## Source

- Source stage report: `docs/stage-report-archives/v0.3.x/v0.3.33_try_run_result_interpretation.md`
- Source tasks:
  - `Extend Try result recovery harness`
  - `Preserve regression lane manifest`

## Problem

The safe focus affordance needs deterministic tests and must enter the current v0.3.x release gate, or future changes can turn recovery guidance back into a passive or unsafe experience.

## Boundary

- Static/source harness plus bounded live `/health` evidence only.
- No model calls.
- No run/build/test/version/draft API mutation during live evidence.

## Solution

- Add `scripts/v03_34_try_result_recovery_affordance.py`.
- Add `tests/test_v03_34_try_result_recovery_affordance.py`.
- Write live evidence to `docs/workingon/try_result_recovery_affordance_v0.3.34.json`, then archive it at version close.
- Update `docs/testing/regression_lanes.json` to prepend the v0.3.34 test file and raise expected passes from 178 to 184.
- Update the v0.3.33 harness to check the stable current-gate id instead of the old exact pass count.

## Acceptance Criteria

- Focused v0.3.34 tests pass.
- Current v0.3.x gate passes at 184 expected tests.
- Live evidence calls only `GET /health`.
- `docs/current-design/` and `docs/workingon/` are clean after archive.

## Final State

- Added `scripts/v03_34_try_result_recovery_affordance.py`.
- Added `tests/test_v03_34_try_result_recovery_affordance.py` with six deterministic tests.
- Updated `docs/testing/regression_lanes.json` so the current release gate starts at v0.3.34 and expects 184 passing tests.
- Updated the v0.3.33 harness to check the stable current-gate id instead of the old exact pass count.
- Live evidence calls only `GET /health`, with no model, build, run, test, version, restore, or draft mutation endpoint.
