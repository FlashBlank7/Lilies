# Historical Design Metadata

- Source stage: `v0.2.20_platform_harness_worker_lease`
- Original design path: `docs/current-design/design_platform_harness_worker_lease_visibility_tests_v1.md`
- Final status: implemented and verified
- Implementation evidence: `docs/workingon-archives/v0.2.20/work_v0.2.20_platform_harness_worker_lease.md`
- Verification: frontend typecheck and production build passed; focused/full backend tests passed
- Remaining risk: browser screenshot QA remains a separate UI verification task

# design_platform_harness_worker_lease_visibility_tests_v1

## 1. Goal

Make worker lease state observable and verify the slice with focused deterministic tests.

## 2. Module Boundary

Frontend typing/visibility:

- `platform/frontend/lib/platform.ts`
- `platform/frontend/app/applications/[id]/page.tsx`

Backend tests:

- `tests/test_workflow.py`

Docs evidence:

- `docs/workingon/work_v0.2.20_platform_harness_worker_lease.md`
- `docs/experiment-status/v0.2_experiment_status.md`

## 3. Data Flow / Control Flow

1. Backend returns task records with optional lease fields.
2. Frontend task monitor typing accepts and displays lease state.
3. Tests cover direct Harness behavior and API behavior.
4. Workingon records command evidence and remaining risk.

## 4. Implementation Plan

1. Add optional worker lease fields to frontend `PlatformTaskRecord`.
2. Show worker lease metadata in the monitor card when present.
3. Add focused tests for conflict rejection, persistence, expiry reconciliation, late success rejection, and API endpoints.
4. Run full backend regression and frontend typecheck.
5. Update experiment-status to mark Platform Harness worker lease as an implemented capability, not an experiment closure.

## 5. Acceptance Criteria

- Monitor typing and UI can show `worker_id`, `lease_expires_at`, and `lease_version`. Completed.
- Focused worker lease tests pass. Completed.
- Full pytest passes. Completed.
- Frontend typecheck passes. Completed.
- Experiment/status ledger distinguishes implementation progress from formal experiment closure. Completed.

## 6. Implementation Result

Status: implemented.

Changed files:

- `platform/frontend/lib/platform.ts`
- `platform/frontend/lib/i18n.ts`
- `platform/frontend/app/applications/[id]/page.tsx`
- `tests/test_workflow.py`

Verification:

- Focused worker lease tests passed: `3 passed, 1 warning`.
- Full backend pytest passed: `67 passed, 1 warning`.
- Frontend typecheck passed.
- Frontend production build passed.

Remaining risk:

- Browser visual screenshot QA remains a separate UI verification task.

## 7. Referenced Intellectual Assets

- `docs/intellectual-assets/asset_platform_harness_task_monitor_boundary.md`

## 8. No Next-stage Authority

This design expands one accepted v0.2.20 task only. It must not select or guide next-stage work.
