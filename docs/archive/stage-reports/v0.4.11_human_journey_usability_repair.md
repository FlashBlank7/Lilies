# v0.4.11 人类使用路径可用性修复

## 阶段信息

| 字段 | 值 |
| --- | --- |
| 模板版本 | `2.0` |
| 版本 | `v0.4.11` |
| 项目总纲 | `docs/evolution-control/PROGRAM_CHARTER.md` |
| 来源报告 | `docs/lilies_agent_scenario_capability_boundary_v0_4_x_latest.docx` |
| 来源阶段报告 | `docs/stage-reports/v0.4.10_connector_embedding_governed_writeback.md` |
| 阶段类型 | 修复 / 产品 / 前端 / 后端 / 运行时 / 验收 / 回归 |
| 闭环等级 | 多角色产品可用性修复，具有本地真实浏览器和全仓回归证据 |
| 阶段范围说明 | 用户反馈同时涉及应用创建、Builder 进度、工作流编辑、验收修复、客户运行恢复、移动端结果、自动化状态、Connector 隔离和重启行为。六项强制任务把这些问题组成一个完整的多角色修复版本；只修其中一个症状，用户路径仍然是不完整的，也不足以推进版本。 |

## 总体目标对齐

| 字段 | 值 |
| --- | --- |
| 总体目标 | 让能力边界报告中已经实现的机制，在模拟客户、维护者和验收操作员的真实使用方式下变得可用、可信。 |
| 本阶段贡献 | 通过完整人类使用路径重新验证既有能力，并修复“机制已经存在，但普通用户仍然无法完成任务”的断层。 |
| 声明边界 | 本阶段只声明由真实浏览器、API 请求、重启检查、源码测试和全量回归证明的本地产品行为；不声明外部客户生产部署、生产可靠性，也不声明未来绝不会再出现缺陷。 |
| 总体阻塞判断 | 六项任务都可以在当前仓库和本地运行环境中独立修复，不存在会阻塞本阶段的外部环境依赖。 |

## 来源任务集

| 任务 ID | 来源意图 ID 列表 | 上一阶段报告中的来源任务 | 本阶段处置 | 设计 / 证据 | 授权 / 原因 |
| --- | --- | --- | --- | --- | --- |
| `V04-11-T01` | `ARCH-007`, `ARCH-008`, `EVAL-002`, `EVAL-003`, `EVOL-003`, `EVOL-006`, `GOV-004`, `PRODUCT-001`, `PRODUCT-005`, `PRODUCT-006`, `PRODUCT-007`, `PRODUCT-008`, `SCENARIO-002`, `SCENARIO-003` | 模拟不同角色的人类使用正在运行的平台，修复暴露的问题，并把达到版本复杂度的可用性修复连同可复现证据归档。<!-- lock:Simulate distinct human roles using the running platform, repair the bugs they expose, and archive a version-sized usability repair with reproducible evidence. --> | 已接受，强制<!-- lock:accepted mandatory --> | 六项锁定的修复任务、聚焦回归、四个浏览器自动化脚本、全量测试分类和带校验和的归档。 | 用户在 v0.4.10 闭环后直接要求进行问题修复并归档。<!-- lock:Direct user repair and archive instruction after v0.4.10 closure. --> |

## 阶段合同

- 合同状态: 已锁定
- 合同修订: `1`
- 合同锁文件: `docs/evolution-control/stage-contracts/v0.4.11.json`
- 合同指纹: `sha256:c2fcb10d095f44a5072b651051fed13bf6c4d56bea1bfb5f10fa5fc3e2ff712d`
- 合同授权: 用户直接授权模拟人类使用平台、修复发现的问题，并归档完成后的当前版本。<!-- lock:direct user authorization to simulate human platform use, repair discovered bugs, and archive the completed current version -->
- 合同基线提交: `7ac4ab594b73d04baf325bb277d5ef7ed36dccfe`
- 合同变更权限: 强制范围或验收标准的变更必须由用户批准

### 强制任务

| 任务 ID | 来源意图 ID 列表 | 涉及界面 / 角色 | 验收标准 | 必需证据 | 状态 |
| --- | --- | --- | --- | --- | --- |
| `V04-11-T01A` | `PRODUCT-001`, `PRODUCT-008` | 需求补全 / Builder Team / 应用持久化 / 首页<!-- lock:requirement intake / Builder Team / application persistence / homepage --> | 从已经完成的工作流方案启动 Builder Team 时，必须创建或复用一个持久化应用及草稿，展示准确构建任务的实时进度，经得起跳转与刷新，并在应用列表中显示客户能理解的用途，不能静默消失或直接倾倒内部需求文本。<!-- lock:Starting Builder Team from a completed workflow proposal creates or reuses one durable application and draft, exposes live progress for the exact build operation, survives navigation and refresh, and presents the application with a customer-readable purpose instead of silently disappearing or dumping internal requirement text. --> | 应用持久化和展示说明测试、Builder 进度操作测试、选项式需求补全回归、API 行为测试，以及返回首页后仍能找到应用的维护者浏览器路径。<!-- lock:Application persistence and display-description tests, Builder progress operation tests, option-based requirement intake regression tests, API behavior tests, and a maintainer browser journey that returns to the homepage and finds the application. --> | 已实现并验证 |
| `V04-11-T01B` | `PRODUCT-001`, `PRODUCT-006`, `PRODUCT-008` | Engineer Studio / 工作流画布 / 自然语言工作流编辑<!-- lock:Engineer Studio / workflow canvas / natural-language workflow editing --> | 打开和选择工作流积木不能让前端崩溃；一键整理画布和 WASD 移动画布必须可用；右键拖动框选积木只作为参考上下文，不能限制修改范围；基于模型的自然语言编辑必须能修改整个工作流，并在预览或应用前验证每一项操作。<!-- lock:Opening and selecting workflow blocks does not crash the frontend; canvas arrange and WASD navigation remain usable; right-drag references are captured without limiting edit scope; and model-backed whole-workflow editing can perform supported structural and semantic changes while validating every emitted operation before preview or apply. --> | 积木点击和画布变换回归、模型工作流编辑测试、前端检查与构建，以及覆盖选积木、框选参考、一键整理、WASD、编辑预览、应用和可读流程摘要的 Engineer Studio 浏览器路径。<!-- lock:Node-selection and transform regression tests, model workflow-edit tests, frontend lint/build, and an Engineer Studio browser journey covering block selection, reference capture, arrange, WASD, edit preview, apply, and stable readable workflow summary. --> | 已实现并验证 |
| `V04-11-T01C` | `EVAL-003`, `PRODUCT-005`, `PRODUCT-007` | 验收门禁 / 修复预览 / 证据生命周期<!-- lock:acceptance gate / repair preview / evidence lifecycle --> | 真实验收失败后必须提供可执行修复；长内容不能把预览操作按钮挤出可见区域；应用修复后必须自动重跑真实测试，并进入真实的通过或失败终态；Markdown 积木标识不能损坏，也不能要求用户进行没有解释的手动刷新。<!-- lock:A failed real acceptance run exposes an actionable repair, keeps preview actions visible before long content, applies the selected workflow repair, automatically reruns the real tests, and reaches a truthful passing or failing terminal state without corrupting Markdown block identifiers or requiring an unexplained manual refresh. --> | 验收修复上下文和工作流编辑测试、可用性源码合同、证明“应用修复 + 自动重跑”两次请求的浏览器网络证据、预览/运行/通过截图，以及零失败请求和零控制台错误。<!-- lock:Acceptance repair context and workflow-edit tests, usability source contracts, two-request browser network evidence for apply plus automatic rerun, preview/running/passed screenshots, and no failed browser requests or console errors. --> | 已实现并验证 |
| `V04-11-T01D` | `EVAL-002`, `PRODUCT-008` | Customer Runtime / 输入校验 / 失败恢复 / 移动端结果<!-- lock:Customer Runtime / input validation / failure recovery / mobile result --> | 客户打开失败运行时，必须看到客户可理解的恢复界面而不是内部错误；缺少输入时不能发送运行请求，并要聚焦无效字段；补全输入后错误应消失；同步双击只能创建一次运行；恢复后的运行必须成功；结构化结果在桌面和 390 像素移动端都要可读，不能显示原始序列化 JSON。<!-- lock:A customer opening a failed run sees a customer-readable recovery state rather than internal errors; retry with missing input sends no run request and focuses the invalid field; correcting input clears the error; synchronous double activation creates only one run; the recovered run succeeds; and structured results render readably without raw serialized JSON on desktop or a 390-pixel mobile viewport. --> | 运行界面源码合同、带请求计数和无障碍状态的失败恢复浏览器路径、带布局断言的移动端客户路径、可读结果截图、前端检查与构建，以及后端运行状态测试。<!-- lock:Runtime source-contract tests, failed-run recovery browser journey with request counts and accessibility state, mobile customer journey with layout assertions, readable-result screenshot, frontend lint/build, and backend run-state tests. --> | 已实现并验证 |
| `V04-11-T01E` | `ARCH-007`, `ARCH-008`, `GOV-004`, `SCENARIO-002`, `SCENARIO-003` | 自动化状态 / Connector 操作 / 租户与应用隔离<!-- lock:automation status / Connector operations / tenant and application isolation --> | 自动化界面必须区分未配置、未发布草稿和已启用定时任务；Connector 绑定、策略、授权、执行、回调、演练和查询必须限制在当前应用与租户内；空的运维界面不能伪装成故障；既有持久化调度和受治理 Connector 行为不能回归。<!-- lock:Automation distinguishes not configured, unpublished draft, and active schedule states; Connector bindings, policies, authorizations, executions, callbacks, exercises, and related queries remain scoped to the selected application and tenant; empty operational surfaces do not masquerade as failures; and the existing durable scheduling and governed Connector behavior remains intact. --> | 调度状态边界测试、持久化采集测试、Connector 跨应用和跨租户测试、维护者自动化和集成浏览器界面、治理状态检查、完整后端回归和重启健康证据。<!-- lock:Schedule boundary tests, durable collection tests, Connector cross-application and cross-tenant tests, maintainer automation and integration browser views, governance status checks, full backend regression, and restart health evidence. --> | 已实现并验证 |
| `V04-11-T01F` | `EVOL-003`, `EVOL-006`, `PRODUCT-005`, `PRODUCT-008` | 人类使用路径脚本 / 回归策略 / 发布证据 / 归档<!-- lock:human journey harness / regression policy / release evidence / archive --> | 可复用浏览器路径必须模拟客户、维护者、验收修复和失败运行恢复；每条路径都要检查布局、网络、控制台、状态变化和清理；当前发布门禁必须包含新增回归；全量测试不能有当前失败；归档必须有校验和；干净重启后的健康接口必须报告 v0.4.11。<!-- lock:Reusable browser journeys model customer, maintainer, acceptance-repair, and failed-run recovery behavior; each journey asserts layout, network, console, state transition, and cleanup invariants; the current release gate includes the new regressions; the full suite has no current failures; artifacts are checksummed; and runtime health reports v0.4.11 after a clean restart. --> | 四个可执行浏览器路径脚本、归档后的 JSON 与截图、当前发布门禁、完整 pytest JUnit 分类、改动范围 Ruff、前端检查与构建、脚本语法检查、git diff 检查、演进控制验证、产物摘要和重启后的健康响应。<!-- lock:Four executable browser journey scripts, archived JSON and screenshots, current release gate, full pytest JUnit classification, targeted Ruff, frontend lint/build, script syntax checks, git diff check, evolution-control validation, artifact digests, and post-restart health response. --> | 已实现并验证 |

### 可选任务

| 任务 ID | 来源意图 ID 列表 | 涉及界面 / 角色 | 验收标准 | 必需证据 | 状态 |
| --- | --- | --- | --- | --- | --- |
| `无` | `无` | 无 | 无 | 无 | 无 |

## 阶段目标

修复客户、工作流维护者和验收操作员像真人一样使用平台时暴露的端到端断层，并保存可执行证据，证明这些路径可以跨越浏览器、API、持久化和重启边界正确恢复。

## 已完成工作

| 任务 ID | 事项 | 状态 | 证据 |
| --- | --- | --- | --- |
| `V04-11-T01A` | 修复选项式 AI 需求补全；规范模型生成的选择动作和需求维度；保证应用创建持久化；在发送事件前保存 Builder 进度；发布前强制完成能力构建合同和验收覆盖。 | 已实现并验证 | `tests/test_v04_00_ai_requirement_intake.py`、`tests/test_builder_live_progress.py`、`tests/test_v04_05_capability_build_contract.py` 和 `browser/paid-builder-runtime-{journey,diagnostic}.json`。 |
| `V04-11-T01B` | 稳定积木选择和画布变换状态；保留一键整理、WASD 和右键框选参考；把自然语言编辑从狭窄的确定性语法扩展到经过验证的整个工作流操作。 | 已实现并验证 | `browser/maintainer-journey.json`、积木点击回归、模型工作流编辑测试、前端检查和生产构建。 |
| `V04-11-T01C` | 让失败验收真正可修复：预览操作保持可见，应用修复后只触发一次自动真实重跑，用终态替换陈旧状态，并保留积木标识。 | 已实现并验证 | `browser/acceptance-journey.json`、三张验收修复截图，以及当前发布门禁中的验收修复和证据生命周期测试。 |
| `V04-11-T01D` | 修复失败运行恢复、必填输入聚焦、重复运行抑制、模型提示词输入传递、语义验收断言和结构化结果渲染。Customer Runtime 现在展示完整业务结果，优先使用结构化数据而不是重复传输包装，并展示完整步骤日志。 | 已实现并验证 | 失败恢复、移动端和付费模型运行路径 JSON，`tests/test_human_usability_regressions.py`，以及 `tests/test_workflow.py` 中的运行时测试。 |
| `V04-11-T01E` | 重新验证真实的自动化状态、持久化调度和 Connector 应用/租户隔离，同时保持 Studio 与 Governance 空状态可理解。 | 已实现并验证 | 维护者浏览器证据、当前发布门禁中的调度与 Connector 测试、完整 JUnit 回归和重启健康检查。 |
| `V04-11-T01F` | 扩展真人路径脚本，增加结果标题和不安全输出断言，在清理前保存诊断快照，运行一次受限真实付费模型的“需求到运行”路径，分类全量历史测试，并形成带校验和的证据归档。 | 已实现并验证 | 五条浏览器路径、`current-release-gate.json`、`full-suite-classification.json`、`static-verification.json`、`runtime-health.json` 和 `SHA256SUMS`。 |

## 验证

| 任务 ID | 检查 | 结果 | 证据 / 准确命令 |
| --- | --- | --- | --- |
| `V04-11-T01A` | 需求补全、Builder 持久化与进度、能力闭环、验收覆盖和自动发布回归。 | 通过 | 当前发布门禁 `159 passed, 0 failed`；付费路径进入 `ready`，创建一个持久化应用，发布版本 1，其中包含 3 个可编辑积木和 1 个强制验收用例，返回应用成功，最后清理全部冒烟数据。 |
| `V04-11-T01B` | Engineer Studio 浏览器路径、积木点击与编辑回归、TypeScript 检查和优化生产构建。 | 通过 | `browser/maintainer-journey.json`；`npm run lint`；Next.js 16.2.9 的 `npm run build`。 |
| `V04-11-T01C` | 真实失败、修复预览、应用、自动重跑和最终验收状态。 | 通过 | `browser/acceptance-journey.json` 记录两次成功的 `/tests/run` 请求，最终状态为“通过”，没有控制台错误和失败请求。 |
| `V04-11-T01D` | 失败恢复、移动端展示、完整结构化结果、安全断言和模型输入传递。 | 通过 | 恢复、移动端和付费运行路径；付费运行输出六个业务区块和五条步骤日志，没有序列化包装标题、不安全自行维修词、横向溢出、控制台错误或失败请求。 |
| `V04-11-T01E` | 调度/Connector 边界、重启、路由可用性和前端可用性。 | 通过 | 当前发布门禁和完整 JUnit 分类；`runtime-health.json` 记录运行时 `v0.4.11`、实现提交 `7851713`、`current_code_ready=true`、后端 `ok` 和重启后的前端 HTTP 200。 |
| `V04-11-T01F` | 完整发布证据和归档完整性。 | 通过 | `.venv/bin/python -m pytest tests -q --junitxml=.tmp/lilies-v0411-final.xml` 得到 `771 passed, 85 xfailed, 0 failed`；合同基线后改动的 27 个 Python 文件全部通过 Ruff；四个 Node 脚本语法检查、前端检查与构建、compileall、`git diff --check` 和归档摘要回验全部通过。 |

## 闭环审计

- 审计上下文: 使用新的审查上下文，根据阶段合同反向检查，而不是照抄实现总结
- 结论: 通过
- 缺失的强制任务: 无
- 不受支持的声明: 在本地产品声明边界内没有；外部客户生产部署、生产 SLO、安全认证和未来绝无缺陷仍然明确不作声明
- 版本规模门禁: 通过

| 强制任务 ID | 验收结果 | 证据有效 | 审计结论 |
| --- | --- | --- | --- |
| `V04-11-T01A` | 通过 | 是 | 最终付费模型重放直接证明“方案到应用”路径；确定性测试另外覆盖模糊需求的选项问题、持久化、进度顺序、能力闭环和发布门禁。 |
| `V04-11-T01B` | 通过 | 是 | 维护者路径在同一浏览器会话中操作相互影响的画布控件；模型编辑仍然面向整个工作流，每个补丁都经过结构和图校验。 |
| `V04-11-T01C` | 通过 | 是 | 网络证据证明“应用修复 + 一次自动重跑”；最终状态来自已经完成的真实测试响应，不是前端乐观状态。 |
| `V04-11-T01D` | 通过 | 是 | 失败恢复、重复启动、响应式布局、完整结果、安全反向断言分别得到验证；付费模型输出仍然具有供应商随机性，不能被写成生产保证。 |
| `V04-11-T01E` | 通过 | 是 | 既有调度和 Connector 边界没有被可用性改动破坏，跨范围访问仍然被拒绝，重启后的平台公开预期路由。 |
| `V04-11-T01F` | 通过 | 是 | 本阶段跨越需求补全、Builder、Studio、验收、Runtime、自动化、治理、持久化、测试和重启，超过版本规模底线，并具有可复现的机器证据。 |

## 偏移记录

| 偏移 ID | 类型 | 任务 ID | 原合同 | 调整后的路线 | 验收是否保持 | 授权 / 证据 |
| --- | --- | --- | --- | --- | --- | --- |
| `V0411-DEV-001` | 流程时序 | `V04-11-T01` | 实现前先冻结阶段合同。 | 用户问题先被交互式复现并修复，随后才确认这批问题已经达到一个版本的复杂度；合同在归档前锁定已经发现的完整闭环集合。 | 是；没有删除任何强制症状或证据门禁，闭环仍要求在合同基线后独立重跑。 | 用户直接要求优先完成能力报告目标和真人问题修复；Git 历史明确保存了较晚建立合同的偏移。 |

## 未解决、阻塞与延期

| 任务 ID | 事项 | 状态 | 强制 / 可选 | 原因 | 下一步 / 决策权 |
| --- | --- | --- | --- | --- | --- |
| `无` | 本阶段没有未完成的强制或可选任务。 | 已完成 | 可选 | 六项锁定任务都通过了证据门禁。 | 新工作必须由用户重新授权并建立新的阶段合同。 |

本节中的强制任务会阻止版本归档。只有用户批准阶段合同修订，并且原始意图仍被保留，才能删除或替换这些任务。

## 证据债务

| 证据债务 ID | 任务 ID | 目标等级 | 已达到等级 | 状态 | 不可用依赖 | 声明上限 | 负责人 / 承接目标 | 复查触发条件 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `V0411-DEBT-001` | `V04-11-T01F` | 全仓 Ruff 零告警基线 | 合同基线后改动的 27 个 Python 文件全部通过 Ruff；全仓诊断仍有 397 条历史问题。 | 已记录但不阻塞 | 本可用性修复版本没有授权清理全部历史代码质量债务。 | 本阶段只声明改动范围 Ruff 通过，绝不声明全仓 Ruff 已经通过。 | 只有用户授权后，才由未来的维护版本承接。 | 用户授权专门的全仓质量版本，或某条历史告警进入当前真实执行路径。 |

证据债务只记录高于合同闭环底线的验证目标。它不能隐藏未完成的强制行为，也不能把尚未获得的证据写成已经通过。

## 意图覆盖

| 来源意图 ID | 阶段前 | 阶段后 | 证据 | 剩余差距 |
| --- | --- | --- | --- | --- |
| `PRODUCT-001` | 机制已经实现，但真人编辑和构建暴露回归。 | 本地工作流构建路径已实现并验证。 | 付费“方案到运行”重放、Builder 进度与持久化测试、维护者 Studio 路径。 | 外部客户生产行为仍不作声明。 |
| `PRODUCT-005` | 有修复机制，但真实操作员路径不完整。 | 可执行修复和自动真实重跑已实现并验证。 | 验收浏览器路径和当前发布门禁。 | 生产事故恢复仍不作声明。 |
| `PRODUCT-006` | 有结构控制，但画布和编辑稳定性回归。 | 积木选择、导航、参考框选和整个工作流编辑已实现并验证。 | 维护者路径、源码回归、前端检查和构建。 | 当前声明边界内没有剩余缺口。 |
| `PRODUCT-007` | 有证据生命周期，但自动恢复结果不明确。 | 测试绑定证据和自动重跑终态已实现并验证。 | 验收网络记录、能力覆盖门禁和 JUnit 回归。 | 分布式或生产证据耐久性仍不作声明。 |
| `PRODUCT-008` | 三类界面已存在，但客户和维护者路径暴露展示与恢复缺口。 | Builder 入口、Studio、Runtime、自动化、集成和首页都经过本地浏览器验证。 | 五条浏览器路径、响应式截图和重启健康检查。 | 尚未进行外部客户可用性研究。 |
| `ARCH-007` | 持久化调度已实现。 | 真实自动化状态和持久化调度回归得到重新验证。 | 调度测试、维护者界面、全量回归和重启。 | 生产调度 SLO 不作声明。 |
| `ARCH-008` | Connector 边界已实现。 | 可用性修复后重新验证应用和租户隔离。 | Connector 测试和维护者集成/治理界面。 | 客户真实系统集成不作声明。 |
| `EVAL-002` | 有分层验收状态。 | 客户可理解的失败恢复和完整结果展示已实现并验证。 | 恢复、移动端和付费 Runtime 路径。 | 生产规模验收不作声明。 |
| `EVAL-003` | 已实现生成式验收。 | 能力关联断言、安全反向检查、修复预览和自动重跑已实现并验证。 | Builder 门禁、验收路径和付费诊断快照。 | 一次受限付费重放不能证明统计意义上的模型可靠性。 |
| `GOV-004` | 受治理 Connector 控制已实现。 | 应用/租户过滤和真实空状态得到重新验证。 | Connector 发布门禁和 Governance 浏览器证据。 | 生产合规认证不作声明。 |
| `SCENARIO-002` | 持久化采集场景已实现。 | 调度状态和持久化采集行为没有回归。 | 当前发布门禁和完整 JUnit 回归。 | 外部真实站点运行不作声明。 |
| `SCENARIO-003` | 嵌入场景已实现。 | 没有发生跨应用或跨租户泄漏。 | Connector 隔离测试和集成界面。 | 客户部署不作声明。 |
| `EVOL-003` | 确定性闭环机制已实现。 | 已实际使用锁定合同、独立重跑、JUnit 分类、重启证据、闭环审计和带校验和归档。 | 合同基线 `7ac4ab5`、本报告和 `docs/workingon-archives/v0.4.11/`。 | 本阶段没有剩余缺口。 |
| `EVOL-006` | 版本复杂度规则已实现。 | 本阶段由六项跨界面强制任务组成，不是只完成一份设计或一个症状。 | 两个实现提交、五条浏览器路径、159 项发布门禁和 856 项全量分类。 | 本阶段没有剩余缺口。 |

来源任务集、阶段合同和本表之间，任何来源意图都不允许无故消失。

## 实验与产品状态更新

| 台账 / 界面 | 更新 | 证据 |
| --- | --- | --- |
| 人类使用路径可用性修复 | 在本地产品边界内已实现并验证 | 六项强制任务、五条浏览器路径、当前发布门禁和完整 JUnit 分类。 |
| 付费模型“需求到运行”重放 | 一次受限真实供应商运行通过 | 需求变成能力关联的工作流方案；Builder 发布包含 3 个积木和 1 个强制验收的工作流；Customer Runtime 返回六个业务区块和五条步骤日志；清理删除冒烟应用。 |
| 运行时版本 | 以实现提交 `7851713768618828a398bc6b14328e6754bf0c72` 归档为 `v0.4.11` | 重启后 `/health` 为 `ok`、`current_code_ready=true`，前端 HTTP 200。 |
| 仓库质量基线 | 改动范围干净；全仓基线仍不干净 | 27 个改动 Python 文件通过 Ruff；`full-ruff-diagnostic.json` 如实记录 397 条历史问题。 |

## 历史设计

| 历史设计 | 最终状态 | 证据 |
| --- | --- | --- |
| 无 | 本修复阶段来自已经复现的人类使用问题和锁定验收合同，不来自推测性设计。 | 浏览器路径、测试和流程偏移记录。 |

## 工作记录归档

| 归档 | 内容 |
| --- | --- |
| `docs/workingon-archives/v0.4.11/` | 带校验和的验收修复、失败运行恢复、维护者、移动端客户和付费“需求到运行”证据；诊断快照；发布/全量/静态验证；重启健康检查和声明边界说明。 |

## 下一阶段任务集

| 任务 ID | 来源意图 ID 列表 | 任务 | 为什么现在做 | 闭环目标 | 强制 / 可选 |
| --- | --- | --- | --- | --- | --- |
| `无` | `无` | 本归档修复阶段没有授权下一阶段任务。 | 用户明确要求完成并归档当前版本。 | v0.4.11 归档后停止；未来工作必须来自新的用户指令和阶段合同。 | 无 |

本表是下一阶段任务的唯一权威来源。每项任务必须使用稳定的任务 ID 和来源意图 ID；workingon 和 current-design 不能另行定义下一阶段任务。

## 归档提交

- 提交: `d7f1d97c9cf637af2acfb12652f967662249c13c`；实现提交 `7851713768618828a398bc6b14328e6754bf0c72`
- 闭环验证器: 通过；六项强制任务都有实现、通过的验证、有效证据和独立审计结论
- 意图覆盖验证器: 通过；所有来源与合同意图都保留在其真实证据上限内
- 当前 current-design 是否干净: 是；只有 README
- 当前 workingon 是否干净: 是；只有 README

## 自动演进交接

- 是否继续: 否
- 当前任务 ID: `无`
- 下一版本: `无`
- 第一任务 ID: `无`
- 恢复所依据的阶段报告: `docs/stage-reports/v0.4.11_human_journey_usability_repair.md`
- 停止原因: v0.4.11 已通过锁定的本地产品闭环门禁并完成归档；没有授权下一阶段任务
