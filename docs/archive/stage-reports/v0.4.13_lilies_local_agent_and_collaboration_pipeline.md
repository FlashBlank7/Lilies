# v0.4.13 莉莉丝本地智能体、黑箱平台接耦与临时合作管道

## 阶段信息

| 字段 | 值 |
| --- | --- |
| 模板版本 | `2.0` |
| 版本 | `v0.4.13` |
| 项目总纲 | `docs/evolution-control/PROGRAM_CHARTER.md` |
| 来源报告 | `docs/PRODUCT_NORTH_STAR.md` 与已批准展开计划 |
| 来源阶段报告 | `docs/stage-reports/v0.4.12_openapi_connector_generation_loop.md` |
| 阶段类型 | 产品 / 智能体 / 架构 / 后端 / 前端 / 实验 / 验收 |
| 闭环等级 | 独立通用莉莉丝、角色化授权、可复用跨软件协同开发、黑箱工作流交付、自主或逐条审批补能与真实企业实验完整闭环 |
| 阶段范围说明 | 本阶段由本地 daemon/CLI、角色化 assignment、黑箱合同与最小权限工具、平台 assignment 桥、正式实验管道、跨软件协同开发底座、Developer Studio、不可变题包与隐藏验证器、28 项资格测试、六个按能力族拆分且逐个执行的真实项目、同级独立 LiliesAgent 仓库与原生桌面界面、安全本地发现、独立 Token 观测、Android/Windows 两个独立软件开发压力项目、人格整合和发布归档组成。新增软件项目不进入客户工作流成功分母；只完成 daemon、包装、GUI、通用代码任务、单个软件项目或单个客户项目都不能证明平台已跨企业能力族交付工作流。 |

## 总体目标对齐

| 字段 | 值 |
| --- | --- |
| 总体目标 | 让莉莉丝成为真正独立运行、持续对话且权限由 assignment 角色决定的本地通用智能体：在 Builder/formal 角色中仅通过平台公开合同交付企业工作流，在显式 developer/reviewer 角色中可与 Codex 通过可复用协作底座改进平台或其他授权软件。 |
| 本阶段贡献 | 建立莉莉丝本地会话、跨软件协同开发和正式实验底座。r8 保留同级 `../LiliesAgent/` 的一次有界失败，并依据用户后续指令把 T01H 最终路线改为 fresh isolated Codex fallback：每个最终项目都从空应用、全新环境、assignment 和 session 开始，只读显式白名单公开材料并只调用公开平台 API。当前协调上下文因受保护内容暴露而永久不具备最终 Builder 或 verifier 资格；已发布的复用应用项目一也只算中间结果。Lilies 单次运行继续硬限 1,000,000 Token；Codex usage 有权威计数则精确记录，否则标记 `unknown`/`unavailable`，八阶段、输出和公开调试链仍完整记录。T01H 完成后再按 M→N→I→J 完成剩余 v0.4.13。 |
| 声明边界 | T01A-G 与 T01K-L 只证明各自已归档范围。旧项目 2–6、失败的 Lilies 重跑和当前复用应用项目一都只保留为历史或中间证据，不能替代 r8 的 fresh-final 六项目分母；只有同一不可变发布版本依次通过公开 debug 和三个受保护 Seed 才算单项目通过。当前协调上下文已暴露受保护内容，不能作为最终 Builder、verifier 或独立 Closure reviewer。T01M-N 固定 `enterprise_denominator=false`。当前不声明六项目 r8 完成、Android/Windows 完成、客户生产、多租户 IAM、安全认证或跨设备同步。 |
| 总体阻塞判断 | 当前没有已确认的外部阻塞。r8 当前强制任务是 T01H：先由 fresh isolated Codex fallback 从六个全新空应用和客户环境完成最终重跑与统一报告；若公开运行暴露真实通用平台缺口，则先结束 Builder 角色，由独立 developer 做最小通用补能，再从受影响项目的全新应用、环境和隔离 Builder context 重跑。之后依次完成 T01M、T01N、T01I、T01J。 |

充值前安全补充：用户要求确认后台消费并监控各环节 Token 后，已冻结
EXP-LILIES-001 revision 10（parent 9）。它不改变企业题、无人值守协作权限、
预算、宿主副作用或 oracle，只增加默认失败关闭的 Provider 出口、T01H
`--enable-model-egress` 单次放行和每 5 秒私有 Token/成本快照。只读审计时
模型进程、启动自动消费者均为 0，`MODEL_EGRESS_ENABLED=false`，没有发起
模型请求；该充值前判断已由后续显式 r10 运行取代。两次运行均只在
`--enable-model-egress` 子进程组内消费，并由 Token 监控记录。r13 继续保持
默认断路，只在 seed 101 正式运行时单次放行；终态没有可恢复 assignment。

## 来源任务集

| 任务 ID | 来源意图 ID 列表 | 上一阶段报告中的来源任务 | 本阶段处置 | 设计 / 证据 | 授权 / 原因 |
| --- | --- | --- | --- | --- | --- |
| `V04-13-T01` | `PRODUCT-001`, `PRODUCT-005`, `PRODUCT-008`, `PRODUCT-015`, `PRODUCT-016`, `PRODUCT-017`, `ARCH-003`, `EVAL-003` | 让莉莉丝作为按 assignment 授权的独立通用智能体运行；验证黑箱 Builder 交付，提供可跨软件复用且支持逐条审批或自主交接的协同开发底座，并完成由真实项目缺口推动平台补能的独立验证企业实验。<!-- lock:Run Lilies as an independent general local agent whose authority comes from each assignment; verify black-box Builder delivery, expose a reusable cross-software collaborative-development substrate with manual or autonomous handoff, and complete an independently verified enterprise workflow experiment that repairs the platform from actual project gaps. --> | 已接受，强制<!-- lock:accepted mandatory --> | `docs/current-design/v0.4.13_lilies_local_agent_and_collaboration_pipeline_expanded_plan.md`、本阶段十四项强制任务及其机器证据。 | 用户于 2026-07-24 批准角色化通用智能体、跨软件莉莉丝—Codex 协同开发和任务级逐条审批/自主合作，同时保持真实项目平台实验为当前优先级。<!-- lock:User approval on 2026-07-24 of role-scoped general-agent authority, reusable cross-software Lilies-Codex collaborative development, and task-level manual or autonomous collaboration, while retaining the real-project platform experiment as the immediate priority. --> |
| `V04-13-T01-PORTFOLIO` | `PRODUCT-001`, `PRODUCT-010`, `PRODUCT-011`, `PRODUCT-012`, `PRODUCT-013`, `PRODUCT-014`, `PRODUCT-015`, `PRODUCT-016`, `PRODUCT-017`, `SCENARIO-004`, `SCENARIO-005`, `SCENARIO-006`, `SCENARIO-007`, `SCENARIO-008`, `SCENARIO-009`, `SCENARIO-010`, `SCENARIO-011`, `EVAL-003` | 用六个真实项目组合替代 revision 数量进度：按不同能力族逐个执行客户项目，每次不可变迭代和失败都归属项目，通用平台补能另列且不进入客户分母。<!-- lock:Replace revision-count progress with a six-real-project portfolio: execute distinct customer projects one by one, keep every immutable iteration and failure under its project, and keep reusable platform capability work in a separate non-customer denominator. --> | 已接受，强制<!-- lock:accepted mandatory --> | `docs/experiments/lilies-collaboration/portfolio-v04-13-t01h.json`、六个项目 manifest、独立 capability lane 及组合验证器。 | 用户于 2026-07-25 批准按不同能力族组织约六个真实项目、逐个执行、迭代归入各项目，并将可复用平台补能单独登记。<!-- lock:User approval on 2026-07-25 to continue with approximately six real projects organized by distinct capability families, execute them one by one, keep iterations under each project, and track reusable platform supplementation separately. --> |
| `V04-13-T01-CODEX-BUILDER` | `EVAL-003`, `PRODUCT-001`, `PRODUCT-010`, `PRODUCT-011`, `PRODUCT-012`, `PRODUCT-013`, `PRODUCT-014`, `PRODUCT-015`, `PRODUCT-016`, `PRODUCT-017` | 由全新隔离 Codex 黑箱上下文替代莉莉丝执行剩余六项目，只经 task-scoped 平台公开接口搭建和修复工作流；Builder 与平台开发分离，硬件、网络、模型供应和软件缺口如实分账，能通用补能的先补再重跑，不能补的保留，并为每个项目单独出具完成与补能报告。<!-- lock:Replace Lilies as the remaining six-project Builder with a fresh isolated Codex black-box context that constructs and repairs workflows only through task-scoped public platform interfaces; separate platform development from Builder execution, classify hardware, network, model-supply and software gaps truthfully, supplement general platform capabilities when feasible, retain unresolved gaps, and issue one outcome and supplementation report for each project. --> | 已由后续路线取代，历史保留<!-- lock:superseded preserved --> | contract revision 4、隔离 Codex Builder runner、公共 API transcript、六项目报告与独立 oracle。 | 用户于 2026-07-26 明确要求 Codex 替代莉莉丝完成六题，只使用平台接口搭建；平台限制能补则补，不能补则保留缺口，并区分硬件限制和软件功能缺失，逐项目报告。<!-- lock:User approval on 2026-07-26 to have Codex replace Lilies for the six tasks, use only platform interfaces to build the workflows, supplement platform limitations when possible, retain gaps that cannot be supplemented, distinguish hardware limits from real software capability gaps, and report each project's completion and platform changes. --> |
| `V04-13-T01-SUPPLEMENTATION-LEDGER` | `EVAL-003`, `PRODUCT-001`, `PRODUCT-005`, `PRODUCT-015`, `PRODUCT-016`, `PRODUCT-017` | 让 Codex 主导的六项目补能循环具备可审计因果链：每个平台缺口必须由隔离 Builder 报告，每项通用开发补能及验证必须绑定该报告，受影响项目必须重跑；三段记录全部保存在 collaboration history 后项目才可通过。<!-- lock:Make the Codex-led six-project supplementation loop causally auditable: every platform gap must be reported by the isolated Builder, every generic developer supplementation and verification must bind that report, and the affected project must rerun with all three records retained in collaboration history before the project may pass. --> | 已接受，强制<!-- lock:accepted mandatory --> | contract revision 5、Builder report、developer supplementation evidence、affected-project rerun/reprobe 与 bounded history replay。 | 用户于 2026-07-26 明确要求修改原 T01H 任务目标，由 Codex 通过平台接口替代莉莉丝，并确保今后所有合作补能记录均被保存。<!-- lock:User approval on 2026-07-26 that the original T01H task goal must be revised for Codex to replace Lilies through platform interfaces and that all future collaboration and supplementation records must be saved. --> |
| `V04-13-T01-STANDALONE` | `ARCH-003`, `EVAL-003`, `GOV-002`, `PRODUCT-001`, `PRODUCT-017` | 将解耦后的莉莉丝移入 `agent/` 下与平台同级的 `../LiliesAgent/` 独立仓库，拥有自己的发行包、daemon/CLI、状态、测试、Git 历史、macOS 原生桌面应用和 Token 观测；平台只能安全发现并显式配对，不能导入源码、共享私密状态或把发现当成授权。<!-- lock:Extract the decoupled Lilies agent into the sibling ../LiliesAgent repository under the agent directory, give it its own distribution, daemon and CLI, state, tests, Git history, native macOS desktop application and token observability, and let the platform discover it locally without importing its code, sharing private state, or treating discovery as authorization. --> | 已接受，强制<!-- lock:accepted mandatory --> | expanded plan 修订 23、独立仓库边界、桌面应用、安全发现、默认出口与 Token 证据。 | 用户于 2026-07-26 要求把解耦后的莉莉丝做成 agent 目录下同级独立项目并提供类似 Codex/Claude Code 的界面，随后明确项目必须位于当前平台仓库上一级。<!-- lock:User instructions on 2026-07-26 to make decoupled Lilies a standalone sibling project under the agent directory with a Codex- or Claude Code-like software interface, followed by the explicit location clarification that the project must be created one level above the current platform repository. --> |
| `V04-13-T01-CROSS-PLATFORM-DEVELOPMENT` | `EVAL-003`, `GOV-002`, `PRODUCT-001`, `PRODUCT-017` | 解耦完成后，通过分别冻结的 DevelopmentAssignment 让莉莉丝完成一个真实 Android 应用和一个真实 Windows 应用；两个项目的迭代、证据、预算与 Token 分开保存且不进入企业工作流分母。<!-- lock:After standalone decoupling, use Lilies through separately frozen DevelopmentAssignments to implement one real Android application and one real Windows application as distinct projects, preserving each project's iterations, evidence, budgets and token usage outside the enterprise-workflow denominator. --> | 已接受，强制<!-- lock:accepted mandatory --> | 两个独立项目合同、莉莉丝开发 transcript、真实构建与运行验收。 | 用户于 2026-07-26 要求解耦后的莉莉丝分别承接 Android 与 Windows 软件开发项目。<!-- lock:User instruction on 2026-07-26 to have the decoupled Lilies undertake separate Android and Windows software-development projects after decoupling. --> |
| `V04-13-T01-LILIES-RERUN` | `EVAL-003`, `GOV-002`, `PRODUCT-001`, `PRODUCT-010`, `PRODUCT-011`, `PRODUCT-012`, `PRODUCT-013`, `PRODUCT-014`, `PRODUCT-015`, `PRODUCT-016`, `PRODUCT-017`, `SCENARIO-004`, `SCENARIO-005`, `SCENARIO-006`, `SCENARIO-007`, `SCENARIO-008`, `SCENARIO-009`, `SCENARIO-010`, `SCENARIO-011` | 由同级解耦 LiliesAgent 从空应用重跑六个真实项目，公开记录工作链、输出、Token 与阶段耗时，并让同一发布版本分别通过公开 debug 和三个受保护 Seed。<!-- lock:Rerun the six locked real projects with the decoupled sibling LiliesAgent as a fresh public-interface Builder, record its durable observable work chain, outputs, token use and per-phase elapsed-time shares under a 1,000,000-token cap per Builder run, independently verify one public debug case and three protected seeds per project, and finish this portfolio before T01M, T01N, T01I and T01J while preserving all prior attempts and evidence. --> | 已由 r8 取代，失败与历史保留<!-- lock:superseded preserved --> | r7 组合 runner、一次有界 Lilies 失败、可查询的公开事件、Token 与八阶段报告。 | 用户截至 2026-08-01 的最新指令要求使用同级 LiliesAgent 重跑全部项目、单次 1M Token 硬停、公开可追踪但不暴露私有推理，并在继续 M/N/I/J 前完成组合。<!-- lock:User instructions through 2026-08-01 to use the decoupled Lilies in the sibling LiliesAgent project to rerun every project, expose a queryable Codex-like observable task session without private reasoning, cap each single Builder run at 1,000,000 tokens, report the debugging path, outputs and phase-time shares, and complete the portfolio before the remaining v0.4.13 work. --> |
| `V04-13-T01-LILIES-CODEX-FALLBACK` | `EVAL-003`, `GOV-002`, `PRODUCT-001`, `PRODUCT-010`, `PRODUCT-011`, `PRODUCT-012`, `PRODUCT-013`, `PRODUCT-014`, `PRODUCT-015`, `PRODUCT-016`, `PRODUCT-017`, `SCENARIO-004`, `SCENARIO-005`, `SCENARIO-006`, `SCENARIO-007`, `SCENARIO-008`, `SCENARIO-009`, `SCENARIO-010`, `SCENARIO-011` | Lilies 一次有界失败后，由 fresh isolated Codex fallback 只用显式白名单公开材料和 task-scoped 公共平台 API 重跑六项目；保留 Lilies 失败、全部中间 attempt、fresh-final 边界和 H→M→N→I→J 顺序。<!-- lock:After one bounded sibling LiliesAgent attempt fails, continue the six-project rerun with fresh isolated Codex fallback Builders using only explicitly allowlisted public materials and task-scoped public platform APIs; preserve the failed Lilies attempt, every intermediate attempt, and the fixed H-to-M-to-N-to-I-to-J order while keeping fresh final applications and independent debug-plus-three-protected-seed acceptance. --> | 已接受，强制<!-- lock:accepted mandatory --> | r8 六个 fresh-final 应用/环境/assignment/session、公开 API transcript、角色与暴露资格记录、同版本 debug + 三受保护 Seed、八阶段与项目报告。 | 用户要求 Lilies 失败后由 Codex 继续扮演 Builder，只使用真实业务可获得信息和平台公开 API，优先最小通用补能并完成六项目后按固定顺序继续。<!-- lock:User instructions through 2026-08-02 to use Lilies first, then have Codex continue as Builder after the bounded Lilies attempt failed, use only public platform APIs and information available in a real workflow-building engagement, preserve all attempts, supplement only minimal generic platform gaps, and complete T01H before T01M, T01N, T01I and T01J. --> |

## 阶段合同

- 合同状态: 已锁定
- 合同修订: `8`
- 合同锁文件: `docs/evolution-control/stage-contracts/v0.4.13-r8.json`
- 合同指纹: `sha256:2235ae1c7fb8c7f34bf1f0d5f05fc9a23e117109c928d69d2d4d2545d7978417`
- 合同授权: User instructions through 2026-08-02 that, after the bounded sibling ../LiliesAgent Builder attempt failed, Codex must continue as the fallback Builder using only public platform APIs and information available in a real workflow-building engagement, prioritize minimal generic platform supplementation, preserve the failed Lilies attempt and all later attempts, rerun all six projects from fresh final applications and customer-environment generations with observable debugging, outputs and eight-phase timing, verify one public debug case and three protected seeds against each same immutable published version, and then continue V04-13-T01H, V04-13-T01M, V04-13-T01N, V04-13-T01I and V04-13-T01J in that order; the 1,000,000-token cap applies to each Lilies Builder run and unavailable Codex usage is recorded as unknown rather than estimated.
- 合同基线提交: `pending`
- 合同变更权限: 强制范围或验收标准的变更必须由用户批准

### 强制任务

| 任务 ID | 来源意图 ID 列表 | 涉及界面 / 角色 | 验收标准 | 必需证据 | 状态 |
| --- | --- | --- | --- | --- | --- |
| `V04-13-T01A` | `PRODUCT-001`, `ARCH-003` | 本地莉莉丝内核 / daemon / CLI<!-- lock:local Lilies core / daemon / CLI --> | 独立 loopback 莉莉丝 daemon 与 CLI 提供持久多轮会话、最小 scope 配对、重启恢复、持久权限与 ack、预算和取消；不得导入平台 WorkflowStorage/Builder 或共享平台数据目录/token，并补齐 pair code、status、stop 和私密凭证 provision/revoke 协议。<!-- lock:A standalone loopback Lilies daemon and CLI provide persistent multi-turn sessions, scoped pairing, restart recovery, durable permissions and acknowledgements, budgets and cancellation without importing platform workflow services or sharing the platform data directory or token; pairing-code creation, status, stop, and private credential provision and revoke protocols are included. --> | 严格 schema、会话/turn/权限/配对/ack/恢复测试，独立子进程 CLI transcript，三轮受限真实模型运行和重启证据。<!-- lock:Strict-schema, session, turn, permission, pairing, acknowledgement, and recovery tests plus independent-process CLI transcripts, a bounded three-turn live-model run, and restart evidence. --> | 已完成 |
| `V04-13-T01B` | `PRODUCT-001`, `PRODUCT-016`, `ARCH-003` | 平台黑箱合同 / scoped HTTP 工具 / 审计<!-- lock:platform black-box contract / scoped HTTP tools / audit --> | 版本化公开合同和任务级 HTTP 工具让隔离莉莉丝进程只能对 assignment 白名单应用执行创建、单步草稿编辑、测试、运行、trace/artifact 读取和发布；强制 scope（含 publish）、应用隔离、撤销/过期、幂等 payload 冲突、审计关联、合同漂移、role-filtered schema 与内部 endpoint 拒绝。<!-- lock:A versioned public platform contract and task-scoped HTTP tools let an isolated Lilies process create, incrementally edit, test, run, inspect artifacts and traces, and publish only assigned applications; publication scope, application isolation, expiry and revocation, idempotent payload conflict detection, audit correlation, contract drift, role-filtered schemas, and internal-endpoint denial are enforced. --> | 合同摘要/漂移、403/404 scope、跨应用、幂等、审计、trace 脱敏、artifact 路径安全及独立 HTTP 进程最小 Build/修复证据。<!-- lock:Contract digest and drift, scope denial, cross-application, idempotency, audit, trace-redaction, artifact-path-safety, and separate-process HTTP-only minimal-build-and-repair evidence. --> | 已完成 |
| `V04-13-T01C` | `PRODUCT-001`, `PRODUCT-008` | 平台配对 / BuildAssignment / Studio 状态桥<!-- lock:platform pairing / BuildAssignment / Studio bridge --> | 平台与 daemon 安全配对并私密 provision assignment credential；严格 BuildAssignment 不含明文秘密或 oracle，关联 application/build/assignment/session，持久 relay/ack 事件，在任一进程重启后续传；daemon 不可用时明确失败，绝不静默 fallback 或预建最终草稿。<!-- lock:The platform securely pairs with the daemon and privately provisions assignment credentials; a strict BuildAssignment without plaintext secrets or oracle data links application, build, assignment, and session, relays persisted acknowledged events across either-process restart, fails explicitly when unavailable, and never silently falls back or prebuilds the final draft. --> | API/存储/凭证、assignment 幂等、事件重放、断线重连、双向定位、取消、daemon unavailable 和 Studio 浏览器路径。<!-- lock:API, storage, credential, assignment-idempotency, event-replay, reconnect, bidirectional-lookup, cancellation, daemon-unavailable, and Studio-browser evidence. --> | 已完成 |
| `V04-13-T01D` | `PRODUCT-005`, `PRODUCT-008`, `PRODUCT-015` | 正式合作管道 / 审批 / Codex 隔离 / claim<!-- lock:formal collaboration pipeline / approval / Codex isolation / claims --> | 仅正式题接收实验报告与验证 channel；独立表和严格状态机实现报告创建/补证/修订、确定性分类路由、用户审批/题内 auto-forward、权限隔离、reader cursor、幂等消息、Codex lease/response、题包 amendment、环境 response、莉莉丝 re-probe、独立 claim 冻结/失效/验证和关闭后 verifier 例外；未审批正文对 developer 完全不可见。<!-- lock:Only formal tasks receive the experiment report-and-verification channel; dedicated tables and strict state machines implement report creation, evidence supplementation and revision, deterministic routing, user approval and task-local auto-forward, permission separation, reader cursors, idempotent messages, Codex leases and responses, task amendments, environment responses, Lilies reprobe, independently stored claim freeze invalidation and verification, and verifier-only post-close completion while unapproved content remains invisible to developers. --> | 数据库迁移、状态/CAS、访问控制、100 次幂等/重连/溢出/租约故障注入和不含日志扫描的因果导出。<!-- lock:Database-migration, state and CAS, access-control, one-hundred-iteration idempotency, reconnect, overflow, and lease-fault tests plus causal export without log scanning. --> | 已完成 |
| `V04-13-T01E` | `PRODUCT-005`, `PRODUCT-008` | Developer Studio / 用户审批 / developer CLI<!-- lock:Developer Studio / user approval / developer CLI --> | 全局 `/developer/collaboration` 在桌面和移动端展示题目、莉莉丝可观察上下文、工具/合同/草稿/run/trace、完整报告因果链、处理人和下一动作；权限与能力审批分离，审批前 developer inbox 无可推断正文，developer CLI 支持 inbox/lease/respond，Customer Runtime 不暴露开发内容或私有思维链。<!-- lock:The global developer collaboration workspace explains task, observable Lilies context, tools, contracts, drafts, runs, traces, report causality, ownership, and next actions on desktop and mobile; permission and capability approval remain separate, preapproval developer views reveal no report content, a developer CLI supports inbox lease and response, and Customer Runtime exposes neither development content nor private reasoning. --> | 前端源码/typecheck/build、桌面/移动浏览器截图、键盘/溢出/reduced-motion、网络/控制台、审批幂等与 developer 隔离证据。<!-- lock:Frontend source, typecheck, build, desktop and mobile browser screenshots, keyboard, overflow, reduced-motion, network, console, approval-idempotency, and developer-isolation evidence. --> | 已完成 |
| `V04-13-T01F` | `PRODUCT-015`, `EVAL-003` | 题包 / 环境锁 / 归档 / 隐藏验证器<!-- lock:task packages / environment lock / archive / hidden verifier --> | 不可变 revision 题包、环境 preflight、允许动作、预算、过滤 workspace、运行归档和独立 verifier 可重放；正式 Builder 与 developer 工作区排除 `.git`、平台 data 和 protected/oracle，claim 绑定冻结 hash，旧 revision 修改、oracle 泄漏、缺健康证据和未授权替代验证均被拒绝。<!-- lock:Immutable task revisions, environment preflight, allowed actions, budgets, filtered workspaces, run archives, and an independent verifier are replayable; formal Builder and developer workspaces exclude git metadata, platform data, and protected oracle content, claims bind frozen hashes, and old-revision mutation, oracle leakage, missing health evidence, and unauthorized substitute validation are rejected. --> | schema/digest/权限/mount、环境健康、archive manifest/replay、oracle 泄漏反向测试和只读 verifier 进程证据。<!-- lock:Schema, digest, permission, mount, environment-health, archive-manifest, replay, oracle-leak negative, and read-only-verifier-process evidence. --> | 已完成 |
| `V04-13-T01G` | `PRODUCT-005`, `PRODUCT-017`, `ARCH-003`, `EVAL-003` | 管道资格 Q01-Q28 / 可复用协同开发 / 故障注入<!-- lock:pipeline qualification Q01-Q28 / reusable collaborative development / fault injection --> | `PIPE-Q01` 至 `Q28` 全部实际运行且无 mandatory xfail：Q01-Q23 保持正式实验边界，普通会话协作不可发现，reconnect/idempotency/lease/concurrency 各 100 次零丢失、零重复副作用；显式协同开发 assignment 可针对任意授权软件工作区分配莉莉丝与 Codex 角色，选择人工或自主交接，持久恢复工作和复验，并通过不依赖 workflow application/Builder 的平台中立 API/CLI 运行；自主执行不得扩大工作区、命令、网络、副作用、预算或隐藏证据授权，所有资格结果均不进入企业成功率分母。<!-- lock:PIPE-Q01 through Q28 all run without mandatory xfails: Q01-Q23 preserve the formal experiment boundary, ordinary sessions cannot discover collaboration, and one hundred reconnect, idempotency, lease, and concurrency iterations lose no messages and duplicate no side effects; an explicit collaborative-development assignment can target an arbitrary authorized software workspace, assign Lilies and Codex task roles, select manual or autonomous handoff, persist restart-safe work and review, and operate through a platform-neutral API and CLI without a workflow application or Builder dependency, while autonomous execution never widens declared workspace, command, network, side-effect, budget, or hidden-evidence authority and all qualification results remain outside the enterprise-success denominator. --> | 机器资格包记录每个 Q、iteration、counter、命令、digest、API/浏览器结果、角色和授权、非 Lilies fixture 源码 diff/测试、持久自主 dispatch 历史、受限真实莉莉丝—Codex 交接及 `enterprise_denominator=false`。<!-- lock:A machine qualification bundle records every Q case, iteration, counter, command, digest, API result, browser result, role and authority grant, non-Lilies fixture source diff and test result, durable autonomous-dispatch history, bounded live Lilies-Codex handoff, and an explicit enterprise_denominator=false marker. --> | 已完成 |
| `V04-13-T01H` | `EVAL-003`, `GOV-002`, `PRODUCT-001`, `PRODUCT-010`, `PRODUCT-011`, `PRODUCT-012`, `PRODUCT-013`, `PRODUCT-014`, `PRODUCT-015`, `PRODUCT-016`, `PRODUCT-017`, `SCENARIO-004`, `SCENARIO-005`, `SCENARIO-006`, `SCENARIO-007`, `SCENARIO-008`, `SCENARIO-009`, `SCENARIO-010`, `SCENARIO-011` | 六个真实客户项目 / Lilies 一次有界尝试 / fresh isolated Codex 公开 API fallback / 可观察重跑遥测 / 因果补能账本 / 独立分母 / 通用能力 lane<!-- lock:six real customer projects / bounded Lilies-first attempt / fresh isolated Codex public-API fallback Builder / observable rerun telemetry / causal supplementation ledger / independent denominators / generic capability lane --> | 先完整保留同级 LiliesAgent 的一次有界失败，再由 fresh isolated Codex fallback 逐项目从全新空应用、客户环境、BuildAssignment、session 和 task credential 开始。最终 Builder 只能读取显式白名单中的公开需求包、积木手册、公开 schema、通用 Builder 指南与 task-scoped 公共 API，只能经这些 API 搭建、测试、运行、读取公开 trace/artifact 和发布；不得读取源码、Git、数据库、developer workspace、protected input、隐藏 Seed、oracle 或 expected/actual 差异。任何接触源码或受保护内容的上下文永久不能计作最终 Builder；当前协调上下文和已发布的复用应用项目一均明确不合格，后者只算中间 harness/workflow 证据。Lilies 每次运行硬限 1M Token；Codex 有权威计数时精确记录，否则只能标记 unknown/unavailable，禁止估算或写 0。八个阶段互斥覆盖全部耗时，Codex 不适用的发现/配对阶段以零时长 not_applicable 记录。每个项目发布后由独立 task-author verifier 对同一不可变版本运行一次公开 debug 与三个受保护 Seed，中间不得修图；任一失败都必须保留并从全新应用/环境重新尝试。通用缺口只能由公开证据触发，Builder 结束角色后由独立 developer 做最小通用补能，并由新的隔离 Builder 从全新应用重跑。六项目全部通过后才按 H→M→N→I→J 继续。<!-- lock:A bounded Lilies-first attempt with the decoupled sibling ../LiliesAgent is retained with builder_actor=lilies, its exact measured token usage and eight-phase timing, and its terminal failure; that failed attempt does not satisfy any project. EXP-LILIES-001 through EXP-LILIES-006 then run one by one with a fresh isolated Codex fallback Builder context for every final-eligible project attempt. Each final attempt starts with a fresh empty application, fresh customer-environment generation, fresh BuildAssignment, fresh session and task-scoped credential; customer environments run sequentially and stop after evidence capture. The final Builder receives an explicit allowlist containing only the public requirement package, block manual, public schemas, general public Builder guidance and task-scoped public platform contract and APIs, and it creates, incrementally edits, tests, runs, inspects public traces and artifacts, and publishes only through those APIs. It cannot read platform or LiliesAgent source, Git metadata, platform databases, developer workspaces, protected inputs, hidden seeds, expected state, oracle data or expected-versus-actual verifier differences, and the harness cannot prebuild or directly edit the workflow or reveal protected results. Any context exposed to source, protected content, oracle data or verifier differences is permanently ineligible as a final Builder and may act only as a separately authorized developer or coordinator; this includes the current root coordination context after its protected-content exposure incident. The already-published reused-application EXP-LILIES-001 version predates that incident but remains an intermediate harness and workflow result with fresh_empty=false and eligible_for_final=false, so it cannot satisfy final public-debug or protected-seed acceptance. Every attempt records builder_actor as lilies or codex_fallback, the exact context, assignment, session and application identities, public-material allowlist digest and exposure eligibility, while preserving every historical Lilies and Codex attempt, revision, actor, failure and evidence. Each Lilies Builder assignment or session has max_session_tokens=1000000; exceeding the recorded limit or issuing another model request after it fails the attempt. Codex token usage is recorded exactly when an authoritative counter is exposed; otherwise it is recorded as unknown or unavailable and is never estimated or represented as zero. The durable observable record contains public session messages or concise actions, public tool calls and results, explicit errors, workflow mutations, tests, runs, traces, artifacts, host receipts, publication results and timestamps, but never private chain-of-thought. Every elapsed interval belongs exactly once to environment/bootstrap, daemon discovery, explicit pairing, assignment/provision, Builder execution, host-result verification, platform/archive verification, or cleanup/reporting; actor-inapplicable discovery or pairing phases are recorded as zero-duration not_applicable, and phase start, end, duration and percentage sum to 100 percent within rounding. After publication, a task-author-controlled independent verifier runs one public debug case and three protected seed cases against the same immutable published version without repair between them; any failure requires a new retained final attempt from a fresh empty application and environment. Protected inputs, oracle content and expected-versus-actual differences never enter Builder or developer-visible records. The portfolio retains separate immutable denominators and covers supplier-document reconciliation on Paperless and InvenTree, access-controlled cited RAG on BookStack, durable event automation on Home Assistant, versioned ML or DL maintenance triage on ThingsBoard, structured finance artifact delivery on Actual, and forecast-driven constrained replenishment on ERPNext. EXP-LILIES-006 remains the mandatory primary ML-training denominator and must perform chronological split, fit or train, evaluation and backtest, immutable model version, promotion or deployment, inference and a retraining trigger; import-only or forecast-only evidence cannot satisfy it. Each non-technical project report explains task and seed design, business and natural-language workflow logic, intermediate and final outputs, failures and public repair path, tokens and phase shares, capabilities added, unresolved software or model gaps and environment limits. A platform supplement is allowed only after public evidence proves a reusable capability gap; the Builder role ends before supplementation, a separately authorized developer makes the minimal generic change, and a fresh isolated Builder reruns the affected project from a fresh application and environment. The supplement must contain no host-specific endpoint, field, mapping, adapter, wrapper, hidden answer or final graph, pass a non-source generality check, and remain linked as Builder gap report, developer supplementation and verification, and affected-project rerun or reprobe. T01H passes only when all six r8 Codex-fallback final attempts independently pass their public debug case and all three protected seeds with every failed and intermediate attempt retained. Portfolio averages, mock substitution, capability tests, another project evidence, prior Codex or Lilies success, the current reused application, or direct host manipulation cannot mask a failure. Only then may execution continue in the fixed order T01M, T01N, T01I and T01J. --> | r8 锁定组合和六项目 manifest；Lilies 有界失败、精确 Token 与八阶段记录；当前协调上下文的受保护内容暴露事故及最终角色禁用记录；复用应用项目一的 intermediate/fresh_empty/eligible_for_final 标记；每项目 fresh-final 应用/环境/assignment/session/credential 与 fresh isolated Codex context；公开文件/API allowlist、完整公开动作/transcript、禁止协助与暴露资格扫描；Codex exact 或 unknown/unavailable usage；八阶段；同一发布版本 debug + 三受保护 Seed；完整 attempt 分母、非技术报告、fresh independent reviewer 与通用补能三联因果证据。<!-- lock:The locked r8 portfolio and per-project manifests; the bounded Lilies-first failure with builder_actor=lilies, exact authoritative token usage, the 1,000,000-token configuration, terminal status and complete eight-phase spans; an incident record marking the current root coordination context protected-content-exposed and ineligible for final Builder or verifier use without reproducing protected content; the already-published reused-application EXP-LILIES-001 result marked intermediate=true, fresh_empty=false and eligible_for_final=false; and, for every final project attempt, a fresh empty application, customer-environment generation, BuildAssignment, session, task-scoped credential and fresh isolated Codex context with builder_actor=codex_fallback. Required final evidence also includes the explicit public-file and public-API allowlist and digest, deny-by-default workspace and forbidden-assistance scans proving no platform or LiliesAgent source, Git, database, developer workspace, protected input, hidden seed, oracle or expected-versus-actual access; complete public platform API, action, error, mutation, test, run, trace, artifact, host-receipt and publication records with timestamps but no private reasoning; exact Codex token counters when authoritative counters exist and explicit unknown or unavailable values otherwise, never estimates or false zeroes; non-overlapping environment/bootstrap, daemon-discovery, pairing, assignment/provision, Builder-execution, host-verification, platform/archive-verification and cleanup/reporting spans, including zero-duration not_applicable actor phases, with starts, ends, durations and percentages summing to 100 percent within rounding; every frozen package revision, environment, public source and interface digest, budget, draft, test, run, trace, artifact, host receipt, publication, archive, model provider and cost; one public-debug result and three independently executed protected-seed results against the same immutable published version with no repair between cases and protected contents withheld from Builder and developer contexts; complete per-project attempt denominators and non-technical reports covering task and seed design, natural-language business and workflow logic, intermediate and final outputs, failure-to-repair path, token use and phase-time shares; preservation links for all historical Lilies and Codex attempts and failures; a fresh independent closure reviewer that has not seen protected content; and, for every supplementation cycle, the immutable Builder-gap, generic-developer-supplementation and affected-project fresh-rerun triplet with exact report revision, capability provenance, project revision, attempt and digest links, non-source generality verification and bounded history replay. --> | 进行中 |
| `V04-13-T01I` | `PRODUCT-008` | CLI ASCII / 平台背景 / 可访问性<!-- lock:CLI ASCII / platform background / accessibility --> | 功能闭环后加入原创低干扰莉莉丝人格：CLI 宽/窄/NO_COLOR/非 TTY/屏幕阅读器行为正确；平台背景只在允许界面出现，Customer Runtime 无 developer 身份，reduced-motion 静止且不降低可读性或改变功能状态。<!-- lock:After the functional loop, an original low-distraction Lilies persona behaves correctly in wide, narrow, NO_COLOR, non-TTY, and screen-reader CLI modes; the platform background appears only on allowed surfaces, Customer Runtime shows no developer identity, reduced motion is static, and readability and functional state are unchanged. --> | 终端快照、前端源码/typecheck/build、desktop/mobile/reduced-motion/contrast 浏览器证据。<!-- lock:Terminal snapshots, frontend source, typecheck, build, and desktop, mobile, reduced-motion, and contrast browser evidence. --> | 已接受 |
| `V04-13-T01J` | `ARCH-003`, `EVAL-003`, `GOV-002`, `PRODUCT-001`, `PRODUCT-005`, `PRODUCT-008`, `PRODUCT-015`, `PRODUCT-016`, `PRODUCT-017` | 发布门禁 / 中文报告 / 归档<!-- lock:release gate / Chinese report / archive --> | 机器发布门禁必须包含 T01A-I 与 T01K-N 聚焦和全量回归、全范围 Ruff 或诚实基线、前端与独立桌面构建、Docker、浏览器与原生 UI、真实模型、Q01-Q28、通用协作资格包、真实实验全分母、Android 模拟器或设备、真实 Windows 运行、Token/后台出口审计、禁止协助、archive digest 和演进控制；没有当前失败，独立 Closure Audit 与版本规模通过，设计移入 historical 且不把 partial/not_run/environment failure 写成完成。<!-- lock:The machine release gate includes T01A through I and T01K through N focused and full regression, full-scope Ruff or an explicitly truthful baseline, frontend and standalone desktop builds, Docker configuration, browser and native-UI evidence, live-model evidence, Q01-Q28 and reusable-collaboration qualification bundles, real-experiment full-denominator, Android emulator or device and real Windows runtime evidence, token and background-egress audits, forbidden-assistance, archive digests, and evolution-control evidence with no current failure; independent Closure Audit and version size pass, the design moves to historical, and the report never labels partial, not-run, or environment failure as completion. --> | 发布门 JSON、闭环审查、阶段报告、协作/实验/独立智能体/Android/Windows archive manifest 与 hash、所有仓库提交及 validator 输出。<!-- lock:Release-gate JSON, closure review, stage report, collaboration, experiment, standalone-agent, Android and Windows archive manifests and hashes, commits from every repository, and validator outputs. --> | 已接受 |
| `V04-13-T01K` | `ARCH-003`, `EVAL-003`, `GOV-002`, `PRODUCT-001`, `PRODUCT-017` | 同级独立仓库 / daemon 与 CLI / macOS 桌面 / 安全发现 / Token 观测<!-- lock:standalone sibling repository / daemon and CLI / macOS desktop / safe discovery / token observability --> | 解耦后的莉莉丝位于 agent 目录下与平台同级的 `../LiliesAgent/`，拥有自己的 Python distribution、Git 历史、daemon/CLI、私有状态、测试和 macOS SwiftPM 原生应用；不得导入 `agent_platform` 或共享平台数据库/token。平台只读取同一用户、mode 0600、非符号链接的 discovery record，核对 loopback 公共 health 指纹，并要求显式一次性配对码；发现不授权，平台不得读取 daemon bootstrap secret。Provider 默认关闭，启动或空闲不得消费模型 Token，usage 按 session/stage/model 持久统计，未知值不得估算。桌面应用可创建、连接、恢复、停止会话，显示消息、权限和 Token，并跨 daemon 重启恢复。<!-- lock:The decoupled Lilies implementation lives in the sibling ../LiliesAgent repository under the agent directory with its own Python distribution, Git history, daemon and CLI, private state, tests and native macOS SwiftPM application. It runs without importing agent_platform or sharing the platform database or tokens. The platform locates only a same-user, mode-0600, non-symlink discovery record, verifies a loopback public health fingerprint, and requires an explicit one-time pairing code; discovery never grants authority and the platform never reads the daemon bootstrap secret. Provider egress remains disabled by default, no startup or idle workflow consumes model tokens, and persisted usage is reportable by session, stage and model with unknown usage labeled rather than estimated. The desktop application can create, attach, resume and stop sessions, display messages, permissions and token usage, and survive daemon restart. --> | 同级仓库/import 扫描；package、Python、CLI、daemon restart 与 Swift 测试；file mode/owner/no-follow/fingerprint/bootstrap/pairing 负向；真实跨进程 bridge/replay；默认出口与空闲零 Token 审计；stage/model usage fixture；macOS 构建、启动、视觉检查和独立安全复审。<!-- lock:Sibling-repository boundary and import scans; package, Python, CLI, daemon-restart and Swift tests; secure file-mode, owner, no-follow, fingerprint, bootstrap and pairing negative tests; real separate-process platform bridge and event replay; default-egress and idle no-token audit; per-stage and per-model usage fixtures; macOS build, launch and visual inspection; sanitized manifests and independent read-only security review. --> | 已完成 |
| `V04-13-T01L` | `ARCH-003`, `EVAL-003`, `GOV-002`, `PRODUCT-001`, `PRODUCT-017` | 独立 DevelopmentAssignment broker / 自主智能体协作 / 受限软件工具<!-- lock:standalone DevelopmentAssignment broker / autonomous agent collaboration / scoped software tools --> | 独立莉莉丝通过模型与工具循环执行显式 DevelopmentAssignment；role、path、command、network、side-effect、deadline 和 budget grant 均冻结。无人监管只取消逐条交接监控，executor 不能铸造、批准或扩大自己的授权。文件/进程工具有界、可取消、隔离并清理完整进程组；副作用执行前必须持久写入 prepared，重启时不确定副作用进入 reconciliation 而非盲目重放，稳定幂等键不得重复变更。模型调用进入同一 stage/model Token 账本，Provider 关闭时不可调用。<!-- lock:The standalone Lilies project can execute an explicitly created DevelopmentAssignment against an arbitrary authorized software workspace through a model-and-tool loop with frozen role, path, command, network, side-effect, deadline and budget grants. Unattended autonomous handoff removes per-message human supervision but never lets an executor mint, resolve or widen its own authority. File and process tools are bounded, cancellable and isolated; complete process groups are reaped; every side effect has a durable prepared record before execution, restart-ambiguous effects require reconciliation instead of blind replay, and stable idempotency prevents duplicate mutations. Model calls use the same persisted stage and model token ledger and remain impossible while provider egress is disabled. --> | 严格 assignment/grant schema；resolver/executor 分离与 self-approval 拒绝；workspace/state/symlink/command/network/output/cancel/descendant 反例；prepared/running/settled/reconciliation 重启；稳定幂等；跨进程 API/CLI、确定性 provider 的端到端开发 fixture、diff/test/usage 与独立安全复审。<!-- lock:Strict assignment and grant schemas; resolver and executor separation with self-approval denial; workspace escape, state-directory, symlink, command, network, output, cancellation and descendant-process negative tests; prepared, running, settled and reconciliation restart tests; stable-idempotency and duplicate-effect tests; independent-process API and CLI transcript; deterministic-provider end-to-end development fixture with complete tool, diff, test and usage evidence; independent security review. --> | 已完成 |
| `V04-13-T01M` | `EVAL-003`, `GOV-002`, `PRODUCT-001`, `PRODUCT-017` | 独立 Android 软件项目 / 莉莉丝开发 assignment / 模拟器或设备<!-- lock:separate Android software project / Lilies developer assignment / emulator or device --> | T01K/L 后，莉莉丝从单独冻结的 Android 题目和隔离仓库开始，经受限 autonomous DevelopmentAssignment 实现应用，全部迭代归属该项目，并交付源码、测试、可复现 APK 或 application bundle，以及符合冻结 oracle 的真实模拟器或设备 smoke。Codex 直接实现、空工程、静态源码检查或其它项目证据不得替代；结果固定 `enterprise_denominator=false`。<!-- lock:After T01K and T01L, Lilies starts from a separately frozen Android project brief and isolated repository, implements the application through its scoped autonomous DevelopmentAssignment, keeps every iteration under that project, and produces source, tests, a reproducible APK or application bundle, and a real emulator or device smoke result matching the frozen acceptance oracle. Direct Codex implementation, a generated skeleton, static source inspection or another project's evidence cannot count as completion; the result is a software-development pressure test with enterprise_denominator=false. --> | 冻结题面/baseline digest；隔离 Lilies session/assignment/grant；完整 model/token/tool transcript；逐 iteration commit/diff/test；固定 Android toolchain/build；APK/bundle digest；模拟器或设备截图、日志、验收、安全扫描与独立复审。<!-- lock:Frozen project brief and baseline digest; isolated Lilies session, assignment and grant; complete model, token and tool transcript; per-iteration commits, diffs and tests; pinned Android toolchain and reproducible build command; APK or bundle digest; emulator or device screenshots, logs and acceptance results; security scan and independent review. --> | 已接受 |
| `V04-13-T01N` | `EVAL-003`, `GOV-002`, `PRODUCT-001`, `PRODUCT-017` | 独立 Windows 软件项目 / 莉莉丝开发 assignment / Windows runtime<!-- lock:separate Windows software project / Lilies developer assignment / Windows runtime --> | T01K/L 后，莉莉丝从单独冻结的 Windows 题目和隔离仓库开始，经受限 autonomous DevelopmentAssignment 实现应用，全部迭代归属该项目，并交付源码、测试、可复现 Windows 应用工件，以及符合冻结 oracle 的真实 Windows runtime smoke。Codex 直接实现、空工程、仅 macOS 编译或其它项目证据不得替代；结果固定 `enterprise_denominator=false`。<!-- lock:After T01K and T01L, Lilies starts from a separately frozen Windows project brief and isolated repository, implements the application through its scoped autonomous DevelopmentAssignment, keeps every iteration under that project, and produces source, tests, a reproducible Windows application artifact, and a real Windows runtime smoke result matching the frozen acceptance oracle. Direct Codex implementation, a generated skeleton, macOS-only compilation or another project's evidence cannot count as completion; the result is a software-development pressure test with enterprise_denominator=false. --> | 冻结题面/baseline digest；隔离 Lilies session/assignment/grant；完整 model/token/tool transcript；逐 iteration commit/diff/test；固定 Windows toolchain/build；artifact digest；真实 Windows 截图、日志、验收、安全扫描与独立复审。<!-- lock:Frozen project brief and baseline digest; isolated Lilies session, assignment and grant; complete model, token and tool transcript; per-iteration commits, diffs and tests; pinned Windows toolchain and reproducible build command; artifact digest; real Windows screenshots, logs and acceptance results; security scan and independent review. --> | 已接受 |

### 可选任务

| 任务 ID | 来源意图 ID 列表 | 涉及界面 / 角色 | 验收标准 | 必需证据 | 状态 |
| --- | --- | --- | --- | --- | --- |
| `无` | `无` | 无 | 无 | 无 | 无 |

## 阶段目标

让用户能够从平台仓库同级的 `../LiliesAgent/` 启动一个拥有独立 daemon/CLI、私有状态、Token 观测和原生桌面界面的通用莉莉丝；平台只做安全发现和显式配对。当前 r8 先保留一次有界 LiliesAgent 失败，再由 fresh isolated Codex fallback 在六个全新空应用、环境、assignment 和会话中只经公开平台 API 搭建、调试、运行和发布真实工作流，并让同一不可变发布版本依次通过公开 debug 与三个受保护 Seed。当前协调上下文和复用应用项目一不能计作 final。Android、Windows 项目随后分别验证软件开发边界且不进入企业分母；最后完成人格、发布门禁和 v0.4 总报告。

## 已完成工作

| 任务 ID | 事项 | 状态 | 证据 |
| --- | --- | --- | --- |
| `V04-13-T01` | expanded plan 四个审批包已由用户批准；v0.4.12 前置清障和第五次 Closure Audit 已通过。 | 已完成 | approved plan、`closure-review-5-pass.md`、提交 `6360e35`。 |
| `V04-13-T01A` | 抽出平台与本地进程共用的 provider-neutral agent core；新增独立 `lilies` daemon/CLI、本地 API、SQLite/WAL schema v3、X25519 daemon 身份、scope 配对与安全 token 连续性轮换、ACL、持久 ack/权限/checkpoint、跨轮预算/截止时间/取消、私密 assignment credential 和受限 workspace 工具。普通会话不导入平台 WorkflowStorage/Builder/Storage/API，也不暴露 collaboration schema 或私有思维链。 | 已完成 | `platform/backend/src/agent_platform/lilies_*.py`、`agent_core.py`、`agent_runtime_factory.py`、九组聚焦/回归测试和 `docs/evidence/v0.4.13/t01a/manifest.json`。 |
| `V04-13-T01B` | 新增单调版本化的 16 项公开合同、task-scoped token 与 HTTP-only 客户端/工具；强制 scope、assignment/session/application/run 绑定、并发单应用授权、撤销/过期、语义幂等和合同漂移。黑箱运行只允许五项本地文件工具、无网络/secret/MCP，持久化 workspace/递归策略；公开输出剔除私有推理，超大结果返回完整标准错误，artifact 以 opaque UUID、2 MB 总上限和摘要校验的 64 KiB 分块读取。独立莉莉丝进程仅凭合同完成空应用 Build、故意失败、一次修复、验收、发布、合同刷新和业务运行。 | 已完成 | `docs/evidence/v0.4.13/t01b/manifest.json`、独立只读审计 `PASS`。 |
| `V04-13-T01C` | 完成平台与 daemon 的安全 pairing、精确 scope/identity/expiry 绑定、严格四 ID BuildAssignment、私密 credential provision/revoke、崩溃安全 pair/reconnect outbox、跨进程 desired-state CAS、持久 relay/strict ack、取消终态 drain 和 Studio 状态桥。daemon 不可用时保留明确 503 与可恢复 ID；不回退 legacy Builder、不预建草稿。 | 已完成 | `docs/evidence/v0.4.13/t01c/manifest.json`；独立只读 Closure Audit `PASS at the scoped evidence floor`。 |
| `V04-13-T01D` | 完成只对正式题激活且普通会话不可发现的临时合作管道；新增独立 SQLite 表、严格报告/审批/租约/回复/题包修订/环境恢复/re-probe/claim 状态机、持久 reader cursor 与单快照 developer inbox。所有写入使用完整请求摘要、CAS 和幂等 receipt；daemon 能跨重启恢复等待、截止时间和不确定远端副作用。implemented DeveloperResponse 只接受当前 Git 历史中完整可达提交及同一 commit tree 内摘要匹配的 `gitblob` 原始证据；关闭后仅 verifier 可完成既有 claim。压缩恢复增加不推进 durable ack 的有界历史回放、exact-sender verifier claim ACL、schema-max run ID 分页和事务 provenance；工作流状态只接收配对成功的公开工具 envelope，以分页索引和 exact state digest 恢复指定应用而不猜 transcript tail；current claim 按创建顺序确定；121 reports/claims/workflows、200 decisions、10000 字符业务目标及最大证据负载仍在 30000 字符内保持可执行恢复。 | 已完成 | `docs/evidence/v0.4.13/t01d/manifest.json`；147 项聚焦、1243 项全仓、真实 `create_app` HTTP 正反路径和独立只读审计 `PASS`。 |
| `V04-13-T01E` | 新增全局 `/developer/collaboration`：桌面三栏与移动任务/时间线/详情路由以语义卡片展示正式题、可观察消息、上下文压缩、工具摘要/耗时/证据、独立运行权限、报告审批、Codex 租约/完整 DeveloperResponse、莉莉丝复验、claim/独立验证差异，以及关联草稿、测试、run 和安全 trace；服务端派生当前阻塞、负责人、等待原因和下一动作。新增机器可读 `lilies-developer` inbox/lease/renew/release/respond CLI，严格 0600/no-follow 凭证文件与单 JSON/脱敏错误；Customer Runtime 改读专用安全投影，保留业务输出但排除开发协作和私有推理。莉莉丝仍是通用智能体，代码能力由每个任务显式 workspace/scope 决定，单次运行权限不会自行扩大授权。 | 已完成（scoped evidence floor） | `docs/evidence/v0.4.13/t01e/manifest.json`；37 项聚焦、433 项广泛 v0.4.13 回归、25 项前端、1302 项全仓、TypeScript、生产构建、真实 Studio/CLI/Customer Runtime HTTP 投影及第二个新上下文只读复审 `PASS`；浏览器 provider 不可用，诚实保留 `V0413-ED-003`。 |
| `V04-13-T01F` | 新增严格不可变且内容寻址的正式题包、父 revision 链、认证环境 preflight/ready、允许动作与持久预算、按角色过滤的 Lilies/developer/verifier workspace、正式 assignment 反向授权、平台持有的成功/失败/取消/环境失败 archive 和 exact replay。v1.1 claim 绑定 draft/run/artifact/receipt/report/package/ready/archive/verifier-process 全部冻结摘要；独立 verifier 以冻结源码、解释器与六个依赖完整文件清单在 macOS deny-by-default sandbox 中只读运行。新增平台派生的隐藏 seed、稳定 ledger/outcome/verdict 和 0400 资格 bundle；重复 seed、旧成功复活、同版本依赖字节漂移、oracle 多重编码泄漏、task-specific adapter/映射/最终图、缺健康证据、越权替代验证及 `.git`/平台 data/protected 泄漏均失败关闭。证据上限为 controlled-local macOS integration。 | 已完成 | 提交 `d51e062`、`docs/evidence/v0.4.13/t01f/manifest.json`；提交后 22 文件 342 项聚焦、830 项 v0.4.13、1668 项全仓、76 路径 Ruff、compileall、diff check 及全新上下文只读 Closure Audit `PASS`。真实 Paperless/InvenTree、36/36 oracle 与三个实际 seed 仍由 `V0413-ED-001`/T01H 承接。 |
| `V04-13-T01K` | 把莉莉丝迁为平台同级的独立 Git/Python distribution，交付自有 daemon、CLI、私有状态、版本化合同和 SwiftPM macOS 应用；平台只保留安全 discovery 与认证 loopback bridge。Provider 默认关闭；daemon-global receipt、legacy unknown migration 和平台监控按 session/stage/model 记账且不估算未知 usage。 | 已完成（scoped evidence floor） | 两仓库最终提交、`docs/evidence/v0.4.13/t01k/manifest.json`；独立 Python `231 passed, 9 skipped`、平台精确回归 `352 passed`、Swift `93 passed`，可复现 wheel/sdist/app ZIP、隔离 wheel 真实 daemon、真实认证 bridge/replay、5.709 秒空闲零增量、重启后连接/账本复核及全新只读 Closure Audit `PASS`。Computer Use native pipe 不可用，`V0413-ED-004` 明确禁止视觉/交互/键盘/accessibility 通过声明。 |
| `V04-13-T01L` | 在独立莉莉丝中完成冻结 DevelopmentAssignment、四角色分权、无人监管自主模型—工具循环、受限文件/进程能力、持久 prepared/reconciliation/idempotency、完整进程组回收和统一 Token 账本；普通会话不能发现或铸造开发权限，Provider 默认关闭。 | 已完成（scoped evidence floor） | 独立仓库提交 `fd0f1a4`、`docs/evidence/v0.4.13/t01l/manifest.json`；Python `322 passed`、独立合同复核 `147 passed`、跨进程/端到端 `2 passed`、Swift `99 passed`、Release/精确启动通过、付费调用 0，并由全新只读 Closure Audit 判定 `PASS`、mandatory blocker `0`。 |

## 验证

| 任务 ID | 检查 | 结果 | 证据 / 准确命令 |
| --- | --- | --- | --- |
| `V04-13-T01` | 前置 v0.4.12 闭环 | 通过 | `docs/evidence/v0.4.12/closure-review-5-pass.md`。 |
| `V04-13-T01A` | strict schema、core/runtime、存储、权限、配对/轮换、ack、预算、取消、credential、本地 API 与 CLI 聚焦回归 | 通过 | `.venv/bin/python -m pytest -q tests/test_agent_core.py tests/test_runtime.py tests/test_api.py tests/test_v04_13_lilies_models.py tests/test_v04_13_lilies_storage.py tests/test_v04_13_lilies_daemon_and_tools.py tests/test_v04_13_lilies_service.py tests/test_v04_13_lilies_api.py tests/test_v04_13_lilies_cli.py`，`102 passed`。 |
| `V04-13-T01A` | 最终全量后端回归 | 通过 | `.venv/bin/python -m pytest -q`，最终复跑 `885 passed, 85 xfailed`，无当前失败；一次既有 10 ms lease 测试的负载抖动失败及其单测/全量复跑结果如实保存在 `deterministic-tests.txt`。 |
| `V04-13-T01A` | 相关 Python Ruff、diff 与依赖锁一致性 | 通过 | `.venv/bin/ruff check ...`、`git diff --check`、`uv lock --check` 全通过；没有修改用户未跟踪的 `uv.lock`。 |
| `V04-13-T01A` | 最终 X25519 代码独立进程 CLI 生命周期 | 通过 | `cli-lifecycle-transcript.txt`：serve/pair/chat/sessions/status/stop、0700/0600、stop 后删除 daemon.json、第二个 daemon 进程 attach 同一 session。 |
| `V04-13-T01A` | DeepSeek 受限真实模型与重启连续性 | 通过 | `live-model-transcript.txt` / `live-model-summary.json`：`deepseek-v4-flash` 三轮含 `local_time` 与首轮回忆；重启后第四轮回答 `local_time`；总计 1987 tokens、USD 0.00033054、5 model calls、1 tool call。 |
| `V04-13-T01A` | 架构边界、恢复与秘密扫描 | 通过 | `architecture-boundary.json`、`recovery-evidence.json`；禁止导入扫描无匹配，最终 SQLite 消息/事件/安全事件的 thinking/Authorization/api_key/access_token marker 均为 0，manifest SHA 全部复核通过。 |
| `V04-13-T01B` | v0.4.13 聚焦、运行时兼容和全仓回归 | 通过 | `uv run --extra dev --frozen python -m pytest -q tests/test_v04_13*.py tests/test_runtime.py tests/test_workflow.py tests/test_v04_03_regression_time_boundary.py` 为 `303 passed`；兼容性复跑 `42 passed`；最终全仓 `1004 passed, 85 xfailed`，无当前失败。 |
| `V04-13-T01B` | HTTP-only 独立进程最小 Build/失败修复 | 通过 | `uv run --extra dev --frozen python -m pytest -q tests/test_v04_13_lilies_platform_process_boundary.py`，`2 passed`；平台与 LocalLiliesService 为独立进程，Builder 引用被移除且 build rows 为 0。 |
| `V04-13-T01B` | scoped contract、授权/审计、运行隔离、脱敏和 artifact 边界 | 通过 | `contract-and-scope.json`、`security-audit-artifact.json`、`architecture-boundary.json`；最终 digest `sha256:6836b56a5eb76c269e49b4edb9c8078011e4c24955701966290a617735d56279`，合同 schema digest `sha256:578e399402c7d4e589a60ed83f021c1ce3c0d2143c2754f8fdf0c21a850cdece`。 |
| `V04-13-T01B` | Ruff、lock、diff、JSON/secret、manifest 与独立只读审计 | 通过 | `deterministic-tests.txt` 的精确 Ruff 文件集、`uv lock --check`、`git diff --check`、6 项 manifest hash 均通过；`independent-audit.md` 结论 `PASS`，无 mandatory residual。 |
| `V04-13-T01C` | 聚焦、v0.4.13 与全仓回归 | 通过 | bridge `51 passed`；全部 v0.4.13 `294 passed`；全仓 `1096 passed, 85 xfailed`，无当前失败。 |
| `V04-13-T01C` | pairing/reconnect/credential/relay/cancel 安全与真实 ASGI | 通过 | pairing/reconnect response-loss、四 ID、并发取消、completed-wins、terminal commit-before-ack 均有回归；独立真实 ASGI 从 `12/0` 恢复到 exact `12/12` 且不复活 assignment/build。 |
| `V04-13-T01C` | 最新独立进程 HTTP、restart/unavailable/replay/no-prebuild | 通过 | `platform-daemon-http-evidence.json`：两个 assignment、22/22 事件、offline 503 保留四 ID、same-ID resume、2/2 凭证撤销、0600 与 32 文件秘密扫描；sanitized SHA `53668f3ce38da91a3881e77a9ae1fa9df2ac6fa3ae8551ec92aef5b4e4823278`。 |
| `V04-13-T01C` | Studio 源码、TypeScript、生产构建与浏览器证据 | 部分通过 / 证据债务 | 16 项前端契约、2 项 operation-attempt、TypeScript 和 Next production build 通过；浏览器运行时无可用 browser，记录为 `blocked_by_environment`，不声明 browser pass。 |
| `V04-13-T01D` | 合作管道聚焦、事务/安全、真实 HTTP DeveloperResponse、压缩/历史回放/provenance 反例与全仓回归 | 通过 | `docs/evidence/v0.4.13/t01d/deterministic-tests.txt`：协作聚焦 `147 passed`，存储/安全子集 `21 passed`，耐久逻辑时钟 `9 passed`，全仓 `1243 passed, 85 xfailed`；32 项压缩/恢复反例、exact-sender SQL-before-limit replay、exact-state 多应用 workflow recall、按创建顺序的 current claim、公开工具 envelope 来源约束、目标文件 Ruff、`git diff --check` 与演进验证通过。默认 dev extra 缺既有 `python-docx`，全仓使用临时 `--with python-docx` 且未修改用户 `uv.lock`。 |
| `V04-13-T01E` | Studio/CLI/API/安全投影聚焦、广泛与全仓回归 | 通过 | `docs/evidence/v0.4.13/t01e/deterministic-tests.txt`：T01E 聚焦 `37 passed`；v0.4.13 collaboration/local-Lilies 广泛回归 `433 passed`；全仓 `1302 passed, 85 xfailed, 0 failed`；用户报告的自然语言框选修改/回调路径 `34 passed`；当前变更文件 Ruff、compileall 和 `git diff --check` 通过。全目录 Ruff 的 27 项旧基线仅来自未改文件并已如实列出。 |
| `V04-13-T01E` | 前端语义、响应式/可达源码合同、TypeScript 与生产构建 | 通过 | `npm test` 为 `25 passed`；`npm run lint` 通过；`npm run build` 通过并生成 `/developer/collaboration`。覆盖 exact permission ID、移动三层路由、980px 切换、SSE cursor、5 秒可见页 context 刷新、双击 in-flight/idempotency、防私有推理、完整证据/合同/DeveloperResponse/验证差异、modal focus trap、Escape 和 reduced-motion 源码合同。 |
| `V04-13-T01E` | 真实本地 Studio、developer CLI 与 Customer Runtime HTTP 投影 | 通过 | 最终提交 `3ba23ea` 由独立 Uvicorn 进程加载：1 channel、4 reports、13 causal messages、1 failed claim、496 个可观察事件；developer CLI 只见 1 条已批准报告，3 条未授权/非 developer report ID 全不可见；Customer Runtime 的 developer/private/credential 共十二类 marker 均为 0。 |
| `V04-13-T01E` | §15.5 桌面/移动用户浏览器路径、截图、键盘/溢出/reduced-motion、network/console | `blocked_by_environment` / 证据债务 | Chrome 浏览器技能 `getForUrl` 返回 `No browser is available`；按 troubleshooting 查询的 browser list 精确为 `[]`。没有伪造截图或把 HTTP/source/build 冒充 browser pass；完整未运行清单和复查触发器见 `browser-environment.json`。 |
| `V04-13-T01F` | 不可变题包、认证健康、允许动作/预算、filtered workspace、archive/replay、claim 与 oracle 泄漏反例 | 通过 | `docs/evidence/v0.4.13/t01f/deterministic-tests.txt`：证据上限为 controlled-local；提交后新增 T01F `342 passed`；完整 v0.4.13 `830 passed`；全仓 `1668 passed, 85 xfailed, 0 failed`。题包字节/权限/旧 revision、health 缺失/失败/未认证/过期、scope widening、`.git`/平台 data/oracle/link/mount、archive byte/mode/index/TOCTOU、connector/report budget、claim 字段与 substitute validation 均有反例。 |
| `V04-13-T01F` | 独立 verifier 进程、运行时冻结和 stable hidden-seed replay | 通过 | `archive-and-verifier.json`、`task-verifier-cli-transcript.txt`：证据上限为 controlled-local macOS；fresh `python -S`、冻结源码/解释器/六个依赖完整 RECORD 文件摘要、deny-network sandbox、单 result sink；同版本依赖字节漂移在 exec 前拒绝。平台 seed resolver、两次受信计算、distinct seed/assignment/archive/claim、失败 reset、旧成功不可复活及 0400 secret-free qualification bundle 均有回归。 |
| `V04-13-T01F` | 静态、证据完整性与独立 Closure Audit | 通过 | 76 个 T01F Python 变更路径 Ruff、compileall、`git diff --check`、八份 evidence JSON/文本与 manifest SHA 全通过；全新上下文从 Program Charter、锁定合同、`327f745..d51e062` diff 和验证证据反向审查，结论 `PASS`，无强制缺口。 |
| `V04-13-T01G` | Q01-Q28、通用协同开发、四类 100 次故障注入、真实双智能体交接与全仓回归 | 通过 | 机器包 28/28 mandatory pass、四 lane 各 100 次零禁止计数、独立 API/CLI 和非平台 Git fixture、Codex 实现/DeepSeek 莉莉丝独立 review、`enterprise_denominator=false`；Audit 1 发现 SQLite sidecar TOCTOU 后完成修复，竞态用例连续 100 次通过，Audit 2 `PASS`。 |
| `V04-13-T01K` | 独立仓库、桌面应用、安全发现、真实 bridge 与 Token/后台出口 | 通过（scoped evidence floor） | `docs/evidence/v0.4.13/t01k/deterministic-tests.txt`：独立 Python `231 passed, 9 skipped`；平台 12 文件精确回归 `352 passed`；Swift `93 passed`；两仓库 Ruff/format/合同/diff 检查通过。 |
| `V04-13-T01K` | 发行、隔离安装、真实启动与跨进程恢复 | 通过 | wheel/sdist 与 macOS ZIP 双构建字节一致；隔离 Python 3.13 wheel 启动真实 daemon 并完成 CLI 配对/usage/安全停止；精确 `.app` 构建、ad-hoc 签名、strict codesign 和进程启动通过，真实 daemon restart 与 bridge event replay 通过。 |
| `V04-13-T01K` | discovery/pairing/秘密、全局 Token、启动与后台出口安全 | 通过 | owner/mode/no-follow/inode/PID/fingerprint/bootstrap/pairing 负向全部失败关闭；认证 daemon-global no-store receipt 在完整 ACL 分页前后稳定，5.709 秒 Token/call/cost/unknown 增量为 0，启动自动消费者与四项 active runtime counter 为 0。123 次旧聚合调用只迁为 unknown，不反推维度或 Token。 |
| `V04-13-T01K` | 独立 Closure Audit 与声明上限 | 通过（scoped evidence floor） | 全新只读上下文从 r6 锁、两个仓库提交 diff 和最终证据反向审查，mandatory blocker `0`。原生视觉 provider 两次相同外部失败转为 `V0413-ED-004`；build/launch 不冒充视觉通过，且不声明 Developer ID/notarization、Codex 账户级账单、T01L-N、T01H 或客户生产。 |
| `V04-13-T01L` | 独立 DevelopmentAssignment、受限工具、恢复与自主协作 | 通过（scoped evidence floor） | 独立仓库提交 `fd0f1a4e67511157f805f936f58d5152fe67dc5f`；Python `322 passed`、合同聚焦 `147 passed`、跨进程 CLI 与确定性 diff/test/usage `2 passed`、Swift `99 passed`、Release/精确 app-daemon 启动通过；全新只读 Closure Audit 判定 `PASS`、mandatory blocker `0`。付费模型调用为 0；声明上限为 controlled-local macOS 与 deterministic provider。 |
| `V04-13-T01M-N` | Android 与 Windows 两项强制任务 | 已接受、未完成；按 r8 在 T01H 后执行 | T01L 的 macOS FD-snapshot 进程后端不替代 Android 模拟器/设备或真实 Windows runtime。两个任务继续固定 `enterprise_denominator=false`，必须分别冻结题面、归档莉莉丝开发迭代、真实构建和运行证据。 |
| `V04-13-T01H-J` | r8 Lilies-first/Codex fallback 六项目重跑、Android、Windows、人格与发布仍为当前强制工作。 | 进行中 | Lilies 有界失败、旧项目 2–6、当前复用应用项目一和所有中间结果均保留，但不能替代六个 fresh-final Codex fallback 项目；T01M/N/I/J 尚未完成。 |

## 闭环审计

- 审计上下文: 使用新的审查上下文，根据阶段合同反向检查，而不是照抄实现总结
- 结论: 待定
- 当前审计状态: T01G、T01K 与 T01L scoped Closure Audit 通过；T01H 的 Lilies-first 尝试已失败并保留，复用应用项目一只形成 intermediate evidence；r8 的六个 fresh-final Codex fallback 项目尚未全部执行，因此 T01H 与版本均未闭环，T01M-N/I/J 仍待后续。
- T01G 审计说明: Audit 1 从 19 文件 changed-scope 套件复现 SQLite WAL/SHM sidecar 在 `exists/stat/chmod` 间消失的竞态并判定 `FAIL`；实现改为 `O_NOFOLLOW` + live descriptor `fstat/fchmod` 后，强制消失回归、20 路原子预算、连续 100 次隔离竞态、208 项聚焦、1832 项全仓、Q01-Q28、真实 Codex/DeepSeek 交接和全部 source/hash 重新绑定。第二个全新只读上下文独立复跑 208 项并判定 `PASS`
- T01L 审计说明: 全新只读上下文从 Program Charter、锁定 Stage Contract、独立仓库 `36441b2..fd0f1a4` diff 和最终验证反向审查；确认四角色分权、冻结 grant、FD-only 输入快照、私有空 cwd、prepared/reconciliation/idempotency、独立 watchdog/进程组回收、Provider 断路与 stage/model Token 账本，结论 `PASS`、mandatory blocker `0`
- 缺失的强制任务: T01M-N、T01H-J
- 不受支持的声明: 当前可声明受控本地环境中的既有 T01A-G/K-L scoped 能力、历史项目 2–6 的既有结果、一次有界 Lilies 失败和复用应用项目一的中间工作流/harness 证据；不能声明当前协调上下文是未暴露的最终 Builder/verifier，不能把复用应用项目一写成 fresh-final，也不能声明 r8 任一项目或六项目组合完成、Android/Windows 完成、人格/发布门完成、客户生产、多租户 IAM、安全认证或跨设备同步。
- 版本规模门禁: 待定

| 强制任务 ID | 验收结果 | 证据有效 | 审计结论 |
| --- | --- | --- | --- |
| `V04-13-T01A` | 通过 | 是 | 独立只读审计反向发现并推动修复累计预算/usage、cancel/recovery、durable ack、严格响应、fingerprint 绑定与依赖锁问题；最终实现、102 项聚焦、885 项全量、独立进程和真实模型证据满足合同，claim ceiling 未扩大。 |
| `V04-13-T01B` | 通过 | 是 | 最终代码、303 项聚焦、1004 项全仓、2 项独立进程、合同/安全/artifact 证据和只读审计满足锁定合同；2 MB artifact 上限与 autonomous task-token 发布边界已写入 claim ceiling。 |
| `V04-13-T01C` | 通过 | 是（浏览器为诚实证据债务） | 独立审计发现并推动修复配对/重连响应丢失、expiry 绑定、跨进程取消竞争、completed-wins 与取消终态事件 drain；最终确定性、真实 ASGI、独立进程 HTTP 和受限 Studio 源码证据满足合同的 scoped evidence floor。 |
| `V04-13-T01D` | 通过 | 是 | 独立审计反向检查正式题激活/普通会话不可发现、未审批隔离、状态/CAS、单快照 inbox、100 次重连/幂等/租约故障、daemon 等待与副作用恢复、因果导出、claim/post-close verifier、历史 replay 的 durable-ack/exact-sender ACL、provenance 防伪、公开工具 envelope/迟到 run/digest 冲突防伪、多应用 exact-state archive 和 claim 创建顺序，以及 121 reports/claims/workflows + 200 decisions 压缩与生产 `create_app` 的完整可达 commit + 同树 gitblob 证据门；最终 147 项聚焦和 1243 项全仓无当前失败，claim ceiling 未扩大到 T01E-H。 |
| `V04-13-T01E` | 通过（scoped evidence floor） | 是（浏览器为诚实证据债务） | Audit 1 根据锁定合同确认八项修复无剩余行为 blocker，但因陈旧 `34/430/24` 清单拒绝闭环；刷新为 `37/433/25/1302` 后，第二个新上下文逐项复核六份 SHA、最终提交绑定、Studio/CLI/隔离合同和声明上限并判定 `PASS`。浏览器 provider `[]` 继续限制 claim ceiling。 |
| `V04-13-T01F` | 通过 | 是 | 在 controlled-local evidence floor，全新上下文审计从锁定合同反向检查 revision/schema/digest/permission、认证 health、动作与预算、Lilies/developer/verifier workspace、平台与验证 trust root、成功及所有终态 archive/replay、v1.1 claim、oracle 多重编码泄漏、独立只读进程、同版本运行时字节漂移和 stable seed 防重/失败 reset；提交后 342 项聚焦、830 项 v0.4.13 与 1668 项全仓无当前失败。审计明确不把 local `real_host` 扩张为 T01H 外部宿主或三个实际 seed。 |
| `V04-13-T01G` | 通过 | 是（浏览器为诚实证据债务） | Audit 1 发现并复现 SQLite sidecar permission TOCTOU，拒绝 `207 passed` 的不稳定证据；修复后第二个全新上下文复跑 208 项、验证 Q01-Q28 28/28、四 lane 各 100 次、standalone API/CLI、重启等价的 manual/autonomous history、冻结 grant、真实 Codex 实现与 DeepSeek 莉莉丝独立 review、全链路 `enterprise_denominator=false` 及 source/nested/bundle hash，结论 `PASS`。浏览器 `[]` 仅限制浏览器声明。 |
| `V04-13-T01H` | 进行中（r8 final 分母 0/6） | 部分有效 | 历史 registry、manifest、capability lane、Lilies 失败、旧项目 2–6 和复用应用项目一均完整保留。当前项目一 version 只证明中间工作流/harness 行为，因应用非全新而 `eligible_for_final=false`；当前协调上下文又因受保护内容暴露不能承担最终 Builder 或 verifier。六项都必须由 fresh isolated Codex context 经公开 allowlist/API 重跑，并由同一不可变版本各自通过 debug + 三受保护 Seed。 |
| `V04-13-T01I` | 待执行 | 待定 | 待定 |
| `V04-13-T01J` | 待执行 | 待定 | 待定 |
| `V04-13-T01K` | 通过（scoped evidence floor） | 是（原生视觉为诚实证据债务） | Audit 1 因缺少 daemon-global/startup 权威收据和最终提交绑定保持 `PARTIAL`；补齐全局观测、legacy unknown 迁移、进程识别与最终回归后，全新 Audit 2 逐项核对独立仓库、真实 bridge/replay、安全 discovery/pairing、默认出口、空闲/启动账本、macOS 功能/重启/build/launch 和 artifact hash，mandatory blocker `0`，判定 `PASS`。`V0413-ED-004` 不允许任何视觉层通过声明。 |
| `V04-13-T01L` | 通过（scoped evidence floor） | 是（跨平台、live Provider 和原生交互为诚实证据债务） | 独立仓库提交 `fd0f1a4` 完成冻结 grant、四角色分权、受限文件/进程工具、prepared/reconciliation/idempotency、确定性模型—工具循环与 Token 账本；Python `322 passed`、独立合同复核 `147 passed`、跨进程 CLI/端到端 `2 passed`、Swift `99 passed`、Release/启动通过；全新 Closure Audit `PASS`、mandatory blocker `0`。 |
| `V04-13-T01M` | 已接受（按 r8 在 T01H 后执行） | 待定 | 尚未冻结 Android 项目题面，也未产生莉莉丝开发、APK 或真实模拟器/设备验收证据。 |
| `V04-13-T01N` | 待执行 | 待定 | 未冻结 Windows 项目题面，未产生莉莉丝开发、应用工件或真实 Windows runtime 验收证据。 |

## 偏移记录

| 偏移 ID | 类型 | 任务 ID | 原合同 | 调整后的路线 | 验收是否保持 | 授权 / 证据 |
| --- | --- | --- | --- | --- | --- | --- |
| `V0413-DEV-001` | D0 协议补全 | `V04-13-T01A-F` | expanded plan 定义了目标、角色和主要 API，但遗漏了 CLI 所需 pair-code/status/stop/credential provisioning，以及 report 补证、莉莉丝 re-probe、题包/环境回复和 claim 独立存储入口。 | 在不改变任何角色、审批或验收的前提下，把这些入口写入锁定合同；publish 使用显式 scope；普通会话的 schema/错误不能泄漏管道存在。 | 是；只补齐实现协议，让原时序可执行。 | 实施前只读 gap map 与 expanded plan 已锁定行为。 |
| `V0413-DEV-002` | T01A 实现加固 | `V04-13-T01A` | daemon.json 需要公钥指纹且本地会话/ack/预算必须跨 token 生命周期、取消和重启保持真实。 | 使用仓库内经 RFC 7748 向量验证的 X25519 公钥派生，避免新增依赖与改写用户 lock；过期 bearer 只可与新一次性 code 组合为 continuity proof，不能调用 API，revoked 不可轮换；schema v3 用 `metrics_settled_at` 原子结算 checkpoint，SSE 只信 durable ack。 | 是；安全和耐久语义更严格，未改变公开角色或验收。 | 负向测试、`architecture-boundary.json`、`deterministic-tests.txt` 和只读审计记录。 |
| `V0413-DEV-003` | T01B 黑箱策略持久性加固 | `V04-13-T01B` | task credential 可运行和发布白名单应用，但 immutable version 尚未保存 assignment workspace/tool/network/nested policy；黑箱也没有 secret-use scope。 | task-token 运行拒绝任何 secret ref；发布前拒绝 schedule/tool/nested/agent/network 等依赖临时 policy 的节点，并要求 exact-content 当前 mandatory pass。该门禁仅约束 autonomous task credential，不改变用户对 Quick/Guided warning 的交互发布决策。全局 secret 解析同时要求 exact owner，nested create 在落盘前拒绝环与深度超过 16。 | 是；原黑箱最小权限和发布安全验收保持，客户发布产品规则未扩大。 | `security-audit-artifact.json`、`architecture-boundary.json`、12 项安全闭环测试与独立审计。 |
| `V0413-DEV-004` | T01C 崩溃与并发恢复加固 | `V04-13-T01C` | 合同要求任一进程重启后 relay/ack 续传、取消与 reconnect 可恢复，但初始路线没有完整枚举 exchange response-loss、跨 platform worker desired-state 竞争和取消终态事件尾部。 | bridge schema v3 增加 terminal drain checkpoint；pair/reconnect 使用加密 prepared bearer outbox 与认证状态恢复；assignment desired state 用 CAS；cancel 409 只接受严格绑定终态；取消后用 connection bearer 有界 drain/commit/strict ack，task bearer 保持撤销。 | 是；不改变公开目标或角色，仅把原 restart/idempotency/ack 验收落实到所有发现的崩溃窗口。 | `t01c/security-and-boundary-summary.json`、51 项 bridge 回归、独立真实 ASGI 和最新独立进程 HTTP 证据。 |
| `V0413-DEV-005` | T01D 可靠性与证据存在性加固 | `V04-13-T01D` | 合同要求完整 DeveloperResponse 的 commit/evidence 必须存在，并要求长会话压缩、重启等待、reader cursor、租约和幂等在故障窗口中保持语义；初始路线未规定生产证据 backing、单快照 inbox、超过 100 个对象的有界表示或 ack 之后的精确恢复来源。 | 生产装配用完整且可达的 Git commit 与同一 tree 内内容寻址 `gitblob` 验证 evidence，Git I/O 移出事件循环；developer inbox 使用同一 SQLite snapshot；daemon 持久化远端副作用 intent/result 与 collaboration deadline；压缩增加有界 report/decision/claim/workflow 索引、只读 history replay、按 exact state digest 的多应用 workflow 分页和事务 provenance；current claim 以创建顺序为准，工作流恢复仅认配对成功的公开工具 envelope。 | 是；没有改变审批权、角色、状态机、三个工具上限或验收，只把原存在性、恢复、压缩和零丢失条件落实到发现的反例。 | `t01d/security-and-boundary-summary.json`、真实 `create_app` HTTP 正反测试、32 项压缩/恢复反例、100 次故障循环与独立只读审计。 |
| `V0413-DEV-006` | T01F 可重放性与信任根加固 | `V04-13-T01F` | 合同要求独立 verifier、隐藏验证、动作/预算和 archive 可重放，但未穷举同版本依赖字节漂移、调用方伪造 seed、host/补偿副作用预算、报告补证轮次耗尽、旧成功在后续失败后复活及 developer source promotion 的并发文件系统窗口。 | verification policy 绑定 Python 可执行文件及六个依赖完整安装文件清单；平台 seed resolver 和稳定 ledger 要求 distinct seed/assignment/archive/claim；connector/report 预算使用持久原子 ledger 并进入双份 archive；旧 attempt 精确 replay 复用磁盘 prepared identity 且不能恢复已 reset verdict；developer worker/source promotion 以 sandbox、Git blob、branch/index/inode CAS 和 protected trust-root 排除落实。 | 是；只收紧原不可变、预算、隔离、replay、隐藏验证和越权拒绝验收，没有把 T01H 企业题结果提前纳入 T01F。 | `docs/evidence/v0.4.13/t01f/`、342 项聚焦、独立 Closure Audit `PASS`。 |
| `V0413-DEV-007` | 用户批准的合同修订 2 | `V04-13-T01G-J` | 原合同把“黑箱平台”写成莉莉丝永久边界，把合作限制为正式实验报告管道，auto-forward 也只自动投递而不自动执行。 | 明确莉莉丝与 Codex 均为按 assignment 授权的通用智能体；保留 Builder/formal 黑箱证据边界，新增不依赖 workflow application 的跨软件 DevelopmentAssignment、独立工作区/开发工具/API/CLI 和持久 autonomous dispatcher；formal 与 development 均可在当前任务选择 manual 或 autonomous，扩权仍需用户。 | 是并扩大；没有削弱黑箱企业实验、隐藏 oracle、全分母或用户授权，新增的代码任务结果明确不计产品成功率。 | 用户于 2026-07-24 对三项纠偏逐条确认并要求继续完成；合同修订 2 基线提交 `ef32240df9e030169301fa71e045ee965af2138f`。 |
| `V0413-DEV-008` | T01G 资格与运行时加固 | `V04-13-T01G` | 合同要求平台中立协同开发、冻结授权、真实双智能体交接、原子预算和可重放资格证据，但实现过程中暴露当前 Codex CLI requirements policy/provider discovery 变化、模型结构化枚举轻微漂移和 SQLite WAL/SHM 消失竞态。 | Seatbelt 只增加精确 requirements policy 路径；CONNECT proxy 对未声明 host 保持零字节 fail-closed；模型 patch 以“最终文件只发生目标算术变化”验证，review verdict 仅接受有界前缀且仍由 acceptance check 一致性约束；SQLite permission tightening 改为 `O_NOFOLLOW` + descriptor `fstat/fchmod`。 | 是；不扩大路径、host、side effect、budget 或声明，反而关闭 provider、模型输出和 sidecar TOCTOU 反例。 | `docs/evidence/v0.4.13/t01g/`，Audit 1 `FAIL`、连续 100 次竞态通过、Audit 2 `PASS`。 |
| `V0413-DEV-009` | T01H 题包与真实运行准备 | `V04-13-T01H` | 正式题要求真实 PDF/扫描件、Excel 逐行验证、三隐藏 seed、临时错误/权限错误和宿主身份认证；T01F 的通用 verifier 只支持文件/JSON，既有题包和真实宿主编排尚未生成。 | 新增公式失败关闭、ZIP/XML 边界受限的通用 XLSX oracle；冻结官方 commit/image digest、24 条公开与三个各 36 条隐藏 PDF、118 项 oracle、固定 compose、exact-host 故障代理、scoped account provisioning 和 HMAC environment attestation。随后补齐只走平台 `formal-builds` 的 runner、四项精确 daemon scope、Harness 加密宿主 secret、同 assignment 私有 resume receipt、InvenTree 1.4.2 Basic token 与 SupplierPart/PO-line 初始化、Paperless 消费完成、精确 `view_paperlesstask`/对象权限和锁保护写请求账本。验证拆成平台冻结 claim 的 79 项归档/XLSX oracle 与 task-author 只读凭证生成的 39 项宿主 oracle；后者绑定 0600 最终快照、逐记录写次数、权限/临时故障门，runner 用固定最小环境隔离宿主 verifier，且仅在宿主通过后触发平台 verifier，平台再把结果写入内容寻址的三 seed 稳定 ledger。Docker daemon 恢复后，compose 并发拉取两次 short-read，改用相同 digest 串行路线完成 6/6；真实预飞又发现并修复 InvenTree manage.py 路径、shell profile、worker URL、订单 wildcard 和两个版本端点，按 `parent_revision` 保留 revision 1/2，revision 3 的 7 容器、24/24 debug seed、0600 零写基线、两故障代理和 HMAC 身份均通过。模型授权到位后的三次 r3 正式尝试依次暴露并修复协同开发签名密钥缺口、runner 未回收隔离子进程组、以及 Git formal developer 投影包含而 workspace materializer 排除 dotenv 变体的边界不一致；runner 现保存不可变 attempt chain，签名密钥安全迁移且与其他 secret 隔离，进程组按组终止，Git 投影与新路径许可统一拒绝全部 dotenv 变体。因 verifier process digest 改变，遵循 §17.5 保留 r3 失败并冻结 parent revision 3 的 revision 4。r4 完成 36 条 hidden seed 宿主初始化后，formal source baseline 又发现完整 Git 树错误使用 developer 投影禁令而拒绝已提交的 sealed `protected` 题包路径，且 attempt rollover 错误要求 latest evidence 与当前 revision 相同；现将完整仓库树的规范路径/对象完整性检查与 developer 可见投影的 protected/secret/dotenv 拒绝分离，developer commit diff 仍对受保护路径失败关闭，attempt ID 按其原 revision 验证并允许父 revision 链。r4 失败补录为第四份不可变 attempt，随后冻结 parent revision 4 的 revision 5，未原地改写旧题包。r5 通过 36 条 hidden seed 宿主预飞、formal source preparation，并创建/启动正式 assignment 与持久 collaboration channel，首次跨过所有 pre-assignment 门；其后 task-author runner 将 Studio 的 `{channels,count}` 响应包络误当原始列表而未能启用已授权 auto-forward。该失败补录为第五份不可变 attempt，冻结 parent revision 5 的 revision 6，并只修正通用 response-envelope 读取。r6 通过该门并真实执行 92 次工具、Draft revision 48 与 6 次 workflow run，但 runner 启动 daemon 时仍使用默认 30-turn token ceiling，没有在 session 创建前投影题包冻结的 120-turn build/repair 预算，33 次模型调用后失败且最终宿主快照保持零写。该预算失败作为第六份不可变 attempt 进入企业分母；revision 7 只把冻结的 `max_build_repair_turns` 投影为 task-local daemon 默认值，不改平台能力、adapter、映射或最终图。r7 首次正式尝试在正确冻结预算内达到 103 model/139 tool/37 draft/11 run，但只嵌入 8/24 公开记录。全新状态重试达到 58 model/73 tool/19 draft/4 run，三个精确 workspace-write 权限分别保留为 immutable checkpoint，并生成 24 条公开记录的 JSON/CSV；Builder 随后以 incomplete summary 结束 turn，真实 XLSX、宿主写回、注册工件、发布、capability report、claim 与 oracle 全部缺失。daemon 已按 task-author 终止该失败会话，但 formal collaboration close 保持 pending；runner 也未把 `running/ready` 且无 claim 识别为 incomplete terminal。该 task-author runner 缺口证明需要冻结 revision 8，仅修改终态判断和证据收口，不修改平台能力、adapter、映射或最终图。 | 是；保持真实宿主、36/36、三实际 seed、黑箱 Builder 与无 adapter 验收不变，只补齐可执行题包、正式运行路径和独立证据采集。 | `docs/experiments/lilies-collaboration/EXP-LILIES-001/1/` 至 `/7/`、`scripts/run_v04_13_enterprise_experiment.py`、`docs/evidence/v0.4.13/t01h/preparation.json`、`runtime-environment.json`、10 份 immutable attempt/checkpoint、`runs/observations/seed-101-r7-a2-builder-ready-incomplete.json`、最新 source/runner 聚焦回归、587/587 真实 HEAD 投影一致性诊断、既有 verifier/formal/local bridge/stable/broker 回归和 compose config。 |
| `V0413-DEV-010` | T01H 无人值守 task-local permission | `V04-13-T01H` | r8 的 formal Builder 在安全工作区写入前由 operational permission 停住；旧 runner 随即关闭平台/daemon，人工 resume 路线暴露 connection race 与 SQLite 损坏。下一次 clean run 又因 daemon event 707 含 credential-shaped 字段而被 formal relay 安全边界拒绝，平台看不到后续 permission。 | 根据最新用户“关闭人类监管、智能体自行协作”的明确授权冻结 parent revision 8 的 revision 9。runner 只自动 allow 与 task/revision/assignment/session/request/input digest 全绑定、路径规范且位于 `work`/`artifacts` 的 `workspace_write`/`workspace_patch` 原始输入；不传 updated input。其它工具、越界路径、身份不符和 formal relay 安全拒绝均失败关闭，业务 Human Input、宿主 403、connector、网络、秘密、预算、oracle 与副作用 grant 不变。formal capability report 仍只可由完整报告通过 `auto_forward` 进入 developer inbox，不能偷换 DevelopmentAssignment，也不能预写 adapter/映射/最终图。 | 是；实现路线依据最新用户授权改变，但 T01H 验收、角色隔离和安全边界未扩大。 | `runs/observations/unattended-task-local-authority.json`、revision 8 relay failure attempt、revision 9 immutable package、37 项聚焦与 200 项广泛回归。 |
| `V0413-DEV-011` | T01H Provider 出口与 Token 观测加固 | `V04-13-T01H` | r9 的真实运行有题包预算与最终汇总，但平台/daemon 启动后存在调度恢复路径，旧直接 Provider 脚本也默认可出网；Local Lilies 账本只提供 turn aggregate，充值前无法用一个只读入口同时判定后台风险和按环节查看 Token。 | 冻结 parent revision 9 的 revision 10；Provider 默认失败关闭，平台与本地 daemon 显式投影开关，T01H runner 再要求 `--enable-model-egress` 并只对该子进程组放行。新增只读进程/SQLite 监控、按 stage/model 汇总、未知 usage 调用标记和 run 内 `0600` 终态快照；账本缺失时安全结论为 unknown 且失败关闭，治理页增加 stage 维度。 | 是；企业任务、预算、协作角色、宿主权限、oracle、分母和真实运行要求不变，只减少误启动付费调用并增加可观测性。 | `docs/evidence/v0.4.13/t01h/token-consumption-audit.json`、revision 10 immutable package、最终 115 项受影响回归、v0.4.13 全量 `1053 passed` 后唯一陈旧 health 严格响应断言已修复并由最终套件复跑通过、前端 TypeScript、Provider/缺账/终态快照负向测试。 |
| `V0413-DEV-012` | T01H formal turn 一次性协议续接 | `V04-13-T01H` | r11 seed-101 的 Builder 在服务器尚未接受 durable formal completion evidence 时，以普通 prose 结束模型循环；runner 随后只能把 assignment 判为 incomplete，无法在无人监管同一 turn 内继续原任务。 | 冻结 parent revision 11 的 revision 12；仅对 `formal_experiment` 在无工具最终响应且无服务器完成证据时写入一个确定性 ID、持久、exact-replay、同 turn 的平台协议消息并再进入一次原模型循环。第二次仍无完成证据则停止；customer、已有完成证据和原 permission/collaboration wait 不受影响。 | 是；业务题面逐字节不变，权限、预算、截止时间、验收、宿主副作用、oracle、CAP 准入和失败分母均不变；消息不列举具体缺口。 | revision 12 immutable package；45 项 `lilies_service` 测试、10 项 portfolio 测试、Ruff；独立只读复审 `PASS`。 |
| `V0413-DEV-013` | T01H 无人监管 permission idempotency 有界化 | `V04-13-T01H` | r12 的 exact task-local permission 路径把 task ID、request UUID 和完整 input digest 直接拼接，真实值超过 bridge schema 的 128 字符上限；mock runner 测试未执行公开 schema 校验。 | 保留 r12 失败并冻结 parent revision 12 的 revision 13；将 task ID、revision、assignment、session、permission request 和 input digest 的规范对象做 SHA-256，生成短前缀加 64 位摘要的确定性 key。测试必须真实断言 16–128 字符、全部绑定变化都会改变 key、相同绑定 exact replay，以及既有越权路径继续拒绝。 | 是；只修正已有授权决定的协议编码，不新增 permission、不扩大可写路径、工具、宿主、网络、秘密、预算、oracle、验收或 CAP 准入。 | r12 attempt `sha256:80a0…c13a`、bridge 422 字段级错误、36 条零请求 baseline/final 与私有 Token 快照。 |
| `V0413-DEV-014` | T01H 正式归档意图前置完整性校验 | `V04-13-T01H` | r13 在 Builder turn 中把历史 draft run 与空 artifact/receipt 冻结为 intent；工具仅返回 `awaiting_daemon_completion`，完整公开证据校验延迟到 daemon 完成后，原 turn 已不能修复或提交完整报告。 | 在 bridge assignment lock 内、不可变 reserve 前，由平台推导完整 current-draft test/business run 集，精确匹配当前业务运行登记 inventory，按 media type 数量覆盖每个 required deliverable，拒绝 test-run artifact，并将每个 host receipt 的 execution ID 与 operation ID 成对绑定 Connector write denominator；公开缺口返回稳定 409，同 turn 可用新幂等键重试。daemon 完成后的 durable archiver 重复同一检查。 | 是；只前移并加强平台已有的公开证据完整性边界，不增加项目提示、adapter、mapping、权限、宿主操作、预算、oracle、CAP 路由或验收。 | 首轮只读审查 blocker、修正后第二轮 `PASS`、113 项归档/bridge/service/tool 回归、HTTP 409 同 turn 修复、失败前 `formal_archive_intent_digest=null`、r14 题面 byte-identical。 |
| `V0413-DEV-015` | T01H production archive validator 装配修复 | `V04-13-T01H` | r14 的 validator 单元与 bridge 集成回归均通过，但 production `create_app` 在 `LocalLiliesBridge` 构造前尝试设置其属性；当时对象仍为 `None`，真实 bridge 无 validator，空 artifact/receipt intent 仍被不可变保留。 | 删除无效的提前注入，在 `FormalRunArchiveCoordinator` 已构造后通过 `LocalLiliesBridge` 构造参数注入 callback；production wiring 回归直接断言 hook 非空且调用当前 coordinator。 | 是；只修正既定通用边界的生产依赖装配，不改变项目题面、权限、预算、宿主操作、oracle、CAP 路由或验收。 | r14 attempt `sha256:a21f18…af725`、`runs/observations/seed-101-r14-production-validator-unwired.json`、production wiring 回归、只读审查 `PASS`、r15 题面 byte-identical。 |
| `V0413-DEV-016` | T01H waiting-permission 事件同步屏障 | `V04-13-T01H` | r15 第一次 relay 只到 event 472；daemon 随后在同一事务持久化 permission row、`waiting_permission` 和 event 478。runner 读到 waiting 后未再 relay 就查询 Studio，误判无精确请求。 | 仅在同 assignment 已暴露 `waiting_permission` 后再 relay 一次，随后仍只从 Studio 脱敏事件读取请求；relay、投影、路径、digest、receipt 或 security-boundary 任一失败均不决定权限。 | 是；修复既有无人值守 task-local 权限投影时序，不增加工具、路径、网络、宿主副作用、预算或隐藏证据权限。 | r15 attempt `sha256:a7ce91…b7afe7`、r15 observation、472/478 回归、post-wait security rejection 回归、150 项聚焦、只读审查 `PASS`、r16 题面/验收不变。 |
| `V0413-DEV-017` | T01H oversized permission 最小安全投影 | `V04-13-T01H` | r16 已 relay/ack 第二个 permission event，但 12,873-byte 公开 payload 触发 Studio 8 KB 限制；旧投影把整个 redacted input 降为 digest-only，连安全顶层 path 一并丢失。 | 仅对已成为 digest-only 的 permission 输入保留原值与脱敏值完全一致、类型为字符串且长度不超过 1,000 的顶层 path；bulk content、private 字段、凭据样式、超长/非字符串 path 和普通 tool 投影继续省略。runner 仍独立执行全部 assignment/request/digest、canonical、deny-segment 和 `work|artifacts` 检查。 | 是；只恢复已有授权判断所需的最小脱敏字段，不增加工具、路径、网络、宿主副作用、预算、隐藏证据或 CAP 权限。 | r16 attempt `sha256:67112b…4fca`、r16 observation、161 项组合与相关回归、Ruff、全新只读安全审查 `PASS`、r17 题面逐字节不变。 |
| `V0413-DEV-018` | T01H daemon SQLite 连接生命周期修复 | `V04-13-T01H` | r17 在 relay/ack cursor 1451 后调用本地 Lilies `get_session`，读取 `clients` 表时抛出 `sqlite3.OperationalError: disk I/O error`；平台收到 502 并失败关闭。终止后数据库 `quick_check=ok`、卷剩余 169 GiB。只读诊断发现 46 处 `sqlite3.Connection` 上下文只提交/回滚而不关闭，长跑持续累积 DB/WAL/SHM 文件描述符。 | 保留 r17 客户失败；统一使用事务后必关闭的 generic connection context。connect 后配置失败也必尝试 close；setup/body/commit/rollback 首错与 close 二次失败并存时，close 只附加 note 而不掩盖首错；正常事务后 close 失败如实抛出。不得把任何持久存储错误重试成成功。 | 是；只修正通用资源生命周期，不扩大权限、宿主、预算、oracle、CAP 或客户声明。 | r17 attempt `sha256:9502e6…99557`、r17 observation、128 次 opened=closed、setup/body/commit/close 故障注入、10k/10k daemon/官方监控压力、291 项 Lilies 回归、冻结后 108 项组合、Ruff/diff check、第二轮全新只读复审 `PASS`、r18 package `ea3f…10cf`/`7573…17c2`。 |
| `V0413-DEV-019` | T01H collaboration report 语义完整性诊断 | `V04-13-T01H` | r18 的 capability report revision 3 保留 attempted routes/evidence 但缺 `expected`、`actual`、`manuals_checked` 和 `missing_contract`；revision 5 增加 `actual` 却丢失 attempted routes/evidence，仍不完整。服务已由 `CollaborationReportPayload.completeness_issues()` 精确计算缺口并把状态置为 `needs_more_evidence`，但成功 submit/revise 响应只给状态和报告体，不返回缺失字段名，莉莉丝无法确定性修复。 | 保留 r18 客户失败；仅在成功的 report create/revise 公开响应中附加排序稳定的缺失字段名，不暴露字段值、验证上下文、秘密、URL、宿主 adapter/mapping/wrapper、oracle 或最终图。complete report 返回空列表；auto-forward 与审批、CAP 准入和业务验收不变。 | 是；fresh create/revise、operation/message/late receipt replay 均返回同一有界诊断，存储与路由不变；该诊断不创建 CAP entry，也不能把 incomplete report 变成 complete。 | r18 attempt `sha256:483282…762c6`、r18 observation、53 项聚焦、238 项广泛协作回归、冻结后 153 项组合回归、Ruff/diff check、portfolio/evolution validators、独立只读复审 `PASS`。 |
| `V0413-ROUTE-020` | T01H Builder 主体与四类工作流合同修订 | `V04-13-T01H` | r19 hidden run 在 15,547,265 tokens 后只有一个 JSON artifact 且无宿主/report/oracle；用户观察的 public debug run 在 600 秒内又消耗 9,325,430 tokens、执行 45 次成功 Draft 修改和 60 次 context update，三个 workflow run 全失败，且没有提交 report，实际不存在莉莉丝—Codex信息交接。用户判断莉莉丝不适合作为剩余六题 Builder，并明确要求 Codex 接管、只经平台接口完成和按项目报告。 | 新建 contract revision 4，保留 r3 与全部 Lilies failure。当前已读源码的 Codex 作为 developer，只做通用平台补能；另起 fresh-context Codex Builder，只得到题面、过滤 workspace 和 task-scoped public API，禁止源码/Git/数据库/developer workspace/protected/expected/oracle。六项目继续逐个执行并独立验收；四类工作流固定为 ML 训练、模型调用、RAG、特定系统嵌入，EXP-LILIES-006 承担不可绕过的完整 ML 训练生命周期主分母。 | 用户明确批准目标/Builder 路线修订；六项目、顺序、独立分母、真实宿主、oracle、CAP 禁止宿主专用适配和重跑要求不变。 | contract r4、public debug observation `sha256:73f5…48b7`、用户 2026-07-26 指令、独立合同只读审查。 |
| `V0413-ROUTE-021` | T01H 因果补能账本合同修订 | `V04-13-T01H` | 历史 r10–r17、r19 与 r19-observe 的 collaboration channel 只有激活和 auto-forward 两条控制消息且无 report；r18 仅有一份 `needs_more_evidence` report 及一次修订。用户要求停止追究旧展示、继续由 Codex 代替莉莉丝经平台接口做题，并明确今后补能记录必须保存。 | 新建 contract revision 5；保留全部历史 channel、report、attempt 与失败。每次新补能强制形成 Builder gap report → developer supplementation/test → affected-project rerun/reprobe 三联因果记录，精确绑定 report revision、项目 revision、attempt、actor 与 digest；任何一环缺失不计项目完成。 | 用户明确批准 T01H 目标与强制证据修订；六项目、顺序、独立分母、真实宿主、oracle、CAP 边界、黑箱隔离和最终验收不变。 | contract r5、历史 r10–r19 collaboration 只读计数、用户 2026-07-26 最新指令。 |
| `V0413-ROUTE-022` | 用户批准的 r6 范围与优先级修订 | `V04-13-T01K-N` | r5 的当前顺序是继续 T01H 六项目，再执行人格与发布；本地莉莉丝实现仍位于平台仓库，Android 与 Windows 软件开发不在锁定合同内。 | 新建 contract revision 6：保留 A-I 尤其 T01H 的验收与全部 0/3 失败事实，新增同级 `../LiliesAgent/`、原生 macOS 界面、安全发现与 Token 观测、独立 DevelopmentAssignment，以及两个分别归档的 Android/Windows 项目。执行优先级临时改为 K→L→M→N→恢复 H→I→J。 | 用户明确批准 D2 范围与优先级扩展；没有删除、弱化或完成 T01H，K-N 全部固定 `enterprise_denominator=false`。 | expanded plan 修订 23、用户 2026-07-26 的独立项目/UI/Android/Windows 指令及随后对 `agent/` 同级位置的明确说明、contract r6。 |
| `V0413-ROUTE-023` | r7 LiliesAgent 六项目重跑、可观察工作链与执行顺序 | `V04-13-T01H-N` | r6 仍以 Codex 为 T01H Builder，并把 M/N 放在 H 前；用户随后明确要求启动同级解耦 LiliesAgent，用它从空应用重跑全部六项目，单次运行 1M Token 硬停，保留可查询的公开任务链并报告各阶段耗时，完成后再继续其余 v0.4 工作。 | 新建 contract revision 7；旧 Codex/Lilies 证据和六题分母全部保留，T01H actor 改为 LiliesAgent，验收绑定同一发布版本的 public debug 与三个 protected Seed，顺序改为 H→M→N→I→J。 | 是；这是用户批准的 Builder 路线与优先级变更，没有删除或弱化六题、隐藏验收、M/N/I/J 或发布门。 | 用户截至 2026-08-01 的最新指令、contract r7、统一 runner 与重跑报告。 |
| `V0413-ROUTE-024` | 用户批准的 r8 Lilies-first/Codex fallback 与最终证据隔离 | `V04-13-T01H-N` | r7 只接受同级 LiliesAgent 作为六项目 Builder；该有界尝试以真实失败终止，用户随后要求 Codex 继续扮演 Builder、只使用真实业务可获得信息和平台公开 API，并优先最小通用补能。之后当前协调上下文的过宽搜索暴露了受保护内容，且当前项目一复用了旧应用。 | 新建 contract revision 8：保留 Lilies 失败与全部历史/中间 attempt；每个 final project 改由 fresh isolated Codex fallback 在显式公开文件/API allowlist 下从全新空应用、环境、assignment、session 和 credential 重跑。当前协调上下文永久标为 protected-content-exposed，不能充当 final Builder/verifier；当前复用应用项目一标为 intermediate/ineligible。Codex usage 只记录 exact authoritative counter 或 unknown/unavailable；八阶段对不适用项写零时长 N/A。同一不可变版本的 public debug + 三 protected Seed 和 H→M→N→I→J 不变。 | 是；Builder 路线按用户指令改变，但没有删除或弱化六项目、fresh-final、独立隐藏验收、补能三联因果、M/N/I/J 或发布门，反而收紧了证据资格。 | 用户截至 2026-08-02 的 Lilies 失败后 Codex fallback 指令、失败与复用应用公开证据、受保护内容暴露事故记录及 contract r8。 |
| `V0413-DEV-020` | T01K 原生视觉证据路线 | `V04-13-T01K` | r6 必需证据包含精确打包 macOS 应用的视觉检查；实现已完成原生行为、Swift、真实 daemon restart、精确 build/sign/launch，但 Computer Use native pipe 连续两次以相同外部启动错误失败。 | 依据 `EVOL-007/008` 和 T01C/E precedent，在不改变任何应用行为、验收目标或产品边界的前提下，以确定性行为、真实跨进程、原生构建/签名/启动和独立审计作为 scoped closure floor；视觉/交互/键盘/accessibility 全部保留为 `V0413-ED-004`，provider 状态不变时不再重试。 | 是；只改变不可用外部 evidence provider 的验证路线，明确降低声明上限，不把 build/launch 冒充视觉通过。 | `docs/evidence/v0.4.13/t01k/environment-and-claim-ceiling.json`、两次相同 native-pipe 失败、全新只读 Audit 2 `PASS`。 |
| `V0413-DEV-014` | T01H formal archive intent fail-late | `V04-13-T01H` | r13 莉莉丝明确承认宿主连接和真实输出缺失，却把非当前 draft 的 business/test run 与空 artifact/receipt 冻结为 intent；工具按设计只回 `awaiting_daemon_completion`，当前 completion detector 把该 receipt 当完成信号，daemon 结束后 durable archiver 才拒绝，无法在同一 turn 修复或提交完整报告。 | 保留 r13 失败；在不读取 hidden oracle 的前提下，为正式 archive intent 增加平台拥有的公开证据预检，至少拒绝非当前 draft 的不完整 run set、缺失的必需工件证据和与已授权宿主写入合同不相称的空 receipt，使错误在 intent 不可变冻结前返回莉莉丝。 | 是；只提前已有 durable archiver 的通用失败，不提供项目答案、adapter、映射或最终图，不放宽验收和 CAP 准入。 | r13 attempt `sha256:222025…1302`、10 次 workflow run、final draft 27、零宿主请求/工件/报告/claim、终态 Token 账本与 `formal_archive_rejected`。 |

## 未解决、阻塞与延期

| 任务 ID | 事项 | 状态 | 强制 / 可选 | 原因 | 下一步 / 决策权 |
| --- | --- | --- | --- | --- | --- |
| `V04-13-T01M-N` | Android 与 Windows 两项任务尚未闭环。 | 已接受，等待 T01H 后执行 | 强制 | 没有把空工程或跨平台代码结构当成通过；两个项目的冻结题面、隔离仓库、真实构建与 runtime 验收均未完成。 | 按 r8 先完成 T01H，再执行 Android T01M 和 Windows T01N。环境缺失只能形成证据债务和声明上限，不能把 mandatory task 写成完成。 |
| `V04-13-T01H-J` | r8 Lilies-first/Codex fallback 六项目重跑、Android、Windows、人格与发布尚未闭环。 | 进行中 | 强制 | Lilies 有界失败、旧项目 2–6 与复用应用项目一均保留为历史/中间证据，但不能替代六个 fresh-final Codex fallback 项目；当前协调上下文也不具备最终 Builder/verifier 资格。T01M/N/I/J 尚未完成。 | 先完成 T01H 六项目各自同一发布版本的 public debug + 三个 protected Seed，再按 M→N→I→J 继续。 |

本节中的强制任务会阻止版本归档。只有用户批准阶段合同修订，并且原始意图仍被保留，才能删除或替换这些任务。

## 证据债务

| 证据债务 ID | 任务 ID | 目标等级 | 已达到等级 | 状态 | 不可用依赖 | 声明上限 | 负责人 / 承接目标 | 复查触发条件 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `V0413-ED-001` | `V04-13-T01H` | 六项目各自 fresh-final 同一不可变版本通过公开 debug 与三个受保护 Seed | 同级 LiliesAgent 的有界 attempt 已失败并记录 21,051 authoritative tokens 与八阶段；当前复用应用项目一已发布中间 version，但 `fresh_empty=false`、`eligible_for_final=false`；当前协调上下文已标记 protected-content-exposed | 进行中 | 不存在已确认外部 blocker；缺少六个由 fresh isolated Codex context 在公开 allowlist/API 下完成的 final attempt 与独立验收 | 只能声明 Lilies 失败、复用应用的中间工作流/harness 证据和合同/runner 确定性能力；不能声明任何 r8 项目 final、六项目组合完成或 protected verifier 独立通过 | T01H | r8 lock、公开 allowlist、fresh app/env/assignment/session/context 和暴露资格门全部就绪后逐项目启动；每项失败都新建并保留 final attempt |
| `V0413-ED-002` | `V04-13-T01C` | 用户操作的 Studio pairing/build/reconnect/cancel 浏览器路径 | 前端源码、TypeScript、生产构建、API、真实 ASGI 与独立进程 HTTP | `blocked_by_environment` | browser runtime 未暴露可用 browser | 不声明 Studio browser pass；T01C 仅在 deterministic/ASGI/HTTP scoped evidence floor 闭环 | T01E/I | browser runtime 可用时重跑相同隔离路径和 desktop/mobile/a11y 证据 |
| `V0413-ED-003` | `V04-13-T01E` | §15.5 全部桌面/移动用户浏览器路径、截图、键盘、overflow、reduced-motion、network/console | Studio/API/CLI 确定性与真实 HTTP 投影、前端源码测试、TypeScript 和 production build | `blocked_by_environment` | Chrome 技能 `getForUrl` 返回 `No browser is available`，troubleshooting 后 `agent.browsers.list()` 精确返回 `[]` | 不声明 Studio desktop/mobile、真实用户审批/双击、键盘、布局、动效、console/network 或 Customer Runtime DOM browser pass | T01I/J | browser list 出现至少一个可控 browser |
| `V0413-ED-004` | `V04-13-T01K-L` | 精确打包原生 macOS 应用的视觉、人工交互、键盘和 accessibility 检查 | Swift `99 passed`、真实 daemon restart、Debug/Release `.app` build、精确应用/daemon 身份启动，以及中文主界面、暗紫科技背景、白发闭眼莉莉丝与 reduced-motion 的静态实现 | `blocked_by_environment` | Computer Use native pipe 两次相同 startup failure | 不声明视觉布局、呼吸动效质量、真人交互、键盘、VoiceOver、accessibility 或原生 UX pass；T01K-L 只在各自 scoped evidence floor 完成 | T01J | Computer Use native pipe 可用，或用户提供明确视觉确认 |
| `V0413-ED-005` | `V04-13-T01L` | live DeepSeek DevelopmentAssignment 的开发质量与供应商实账 | 确定性 Provider 的完整模型—工具循环、严格调用前 Token 上界、stage/model 持久账本、关闭出口失败关闭；付费调用和账单均为 0 | `not_required_for_scoped_closure` | 缺少可离线权威计数的冻结 tokenizer 与满足请求上界合同的 live adapter；未授权任何充值消费 | 只声明 deterministic-provider 行为和账本正确性，不声明 live DeepSeek 能完成软件开发、模型质量或真实供应商成本 | T01M-N / T01J | 安装并冻结权威离线 tokenizer 与合规 adapter，且用户另行授权真实模型消费 |
| `V0413-ED-006` | `V04-13-T01L` | 最终进程监督实现所在主机无历史残留 | 最终 watchdog/完整进程组回收回归全部通过且未新增已知残留；四个淘汰的 pre-watchdog 实验进程不可中断，但不是模型工作流且无 Provider 出口 | `environment_residue` | macOS 内核态不可中断进程只能通过主机重启清理 | 不把这四个历史进程写成最终实现产生的新后台工作流，也不声明当前主机进程环境完全洁净 | 环境维护 | 主机重启后复核 PID、Provider 出口与 Token 账本 |
| `V0413-T01K-DIST-001` | `V04-13-T01K` | Developer ID、notarization、公共 Gatekeeper 与跨机器分发 | wheel/sdist/app ZIP 双构建字节一致；本机 ad-hoc 签名与 strict codesign 通过 | higher-level evidence not claimed | 未提供 Developer ID 身份和 notarization 授权 | 不声明 Developer ID、notarized/public Gatekeeper-ready 或跨机器分发 | T01J | 提供 Developer ID 凭据并明确授权 notarization workflow |

证据债务只记录高于合同闭环底线的验证目标。它不能隐藏未完成的强制行为，也不能把尚未获得的证据写成已经通过。

## 意图覆盖

| 来源意图 ID | 阶段前 | 阶段后 | 证据 | 剩余差距 |
| --- | --- | --- | --- | --- |
| `PRODUCT-001` | AgentRuntime 与 legacy Builder 在平台进程内存在。 | 既有本地莉莉丝 daemon/CLI 能持续对话和重启恢复；T01K 进一步交付同级独立 Git/Python distribution、自有 daemon/CLI/私有状态、SwiftPM 应用、安全 discovery/认证 bridge 和全局 Token 观测；T01L 在同一独立 carrier 中完成冻结授权的 autonomous DevelopmentAssignment、受限工具、恢复/幂等和统一账本。两项均在各自 scoped evidence floor 通过独立审计。 | T01A-C 既有证据、`docs/evidence/v0.4.13/t01k/manifest.json` 与 `docs/evidence/v0.4.13/t01l/manifest.json`。 | M-N 的跨平台项目和 H 的真实企业题仍待完成；T01K-L 不支持原生真人交互、live Provider 或客户生产声明。 |
| `PRODUCT-005` | 有内部修复机制，没有用户审批的跨开发补能闭环。 | 已实现正式题限定的持久报告、补证、用户批准/题内 auto-forward、隔离 developer lease/response、莉莉丝复验和 claim 独立结论协议；全局 Studio 能解释阻塞/负责人/证据/下一步，用户审批与运行权限分离；T01G 又完成平台中立 manual/autonomous 协同开发、重启恢复、冻结授权、独立 review 与资格闭环。 | T01D/E 证据；T01G 28/28 Q、四类各 100 次、standalone API/CLI、真实 Codex/DeepSeek handoff 和 Audit 2 `PASS`。 | H 的真实企业动态补能仍待实现；T01E 用户浏览器层保留 `ED-003`。 |
| `PRODUCT-008` | 三类产品界面存在，没有本地莉莉丝全局协作监控。 | 已实现全局 `/developer/collaboration` 的语义监控工作区、移动三层路由、状态解释、证据/因果链、审批、daemon/预算信号和安全 Customer Runtime 分界；浏览器不持有 developer 凭证，私有思维链不进入投影。 | T01C assignment 桥、T01D collaboration API、T01E Studio/CLI/安全投影源码与测试、真实本地 HTTP、TypeScript/build 及 `ED-003`。 | I 的完整人格视觉和真实 desktop/mobile/reduced-motion 浏览器闭环尚待实现。 |
| `PRODUCT-010` | 企业、个人和工程压力测试的分母规则已进入 Product North Star，但真实项目尚未组合化执行。 | T01H registry 固定四个企业项目和两个单独报告的泛化项目，禁止混合分母；旧项目 2–6、Lilies 失败和复用应用项目一作为历史/中间证据保留，r8 fresh-final 分母仍为 0/6。 | portfolio `cohort_policy`、六个 manifest、历史项目报告与 r8 合同。 | 六个项目都须由 fresh isolated Codex fallback 从全新应用/环境重新验收，组合尚不能关闭。 |
| `PRODUCT-011` | 数据、文件、schema、质量和 lineage 能力为未验收意图。 | 六项目分别冻结文档、知识、事件、telemetry、财务表和计划数据的 typed 输入与独立 oracle。 | 六个项目 manifest 与组合验证器。 | 选择与合同不等于运行；仍须由生成工作流通过真实数据 oracle。 |
| `PRODUCT-012` | ML/DL 生命周期能力未完成真实项目验收。 | ThingsBoard 项目明确版本模型、特征、评测、threshold、人工 review、alarm 和 drift；ERPNext 项目补充版本预测模型。 | EXP-LILIES-004/006 manifest。 | 模型导入/训练、部署、监控和真实结果均未运行。 |
| `PRODUCT-013` | 企业 RAG 意图未完成真实项目验收。 | BookStack 项目明确权限继承、内容更新/删除、embedding、rerank、引用、拒答和 `forbidden_chunk_count == 0` oracle。 | EXP-LILIES-002 manifest 与官方接口证据。 | revision 1、冻结语料、权限矩阵和真实 RAG 结果尚未形成。 |
| `PRODUCT-014` | Excel/受治理 API 工件能力只有局部机制与项目 1 未完成证据。 | 项目 1、5、6 分别冻结 Excel/财务工件/计划记录、receipt、幂等和回读验收。 | EXP-LILIES-001/005/006 manifest。 | 三项目客户工件和宿主交付均未通过。 |
| `PRODUCT-015` | 有受控局部实验，没有莉莉丝真实企业工作流独立结果。 | T01D 已提供应用/草稿精确绑定和独立 claim 协议；T01F 已实现不可变题包、认证环境、允许动作/预算、filtered workspace、完整终态 archive/replay、冻结 v1.1 claim、只读 verifier、oracle 泄漏门和 stable hidden-seed ledger。 | T01D claim/事务/HTTP 证据；T01F 提交 `d51e062`、342/830/1668 回归和独立审计。 | H 的真实 Paperless/InvenTree、36 条 oracle 与三个实际 seed 企业实验仍待执行；不能用 controlled-local `real_host` 抬高。 |
| `PRODUCT-016` | REST/OpenAPI 自动 Connector 已完成受控资格。 | 莉莉丝已有 role-filtered 黑箱合同、scoped HTTP 工具、通用 failure envelope 和平台 assignment 接耦；正式真实题仍须暴露并恢复通用缺口。 | T01B contract/process 与 T01C bridge/process 证据及 H 合同。 | H 真实题尚待实现。 |
| `PRODUCT-017` | 独立莉莉丝 core、正式报告管道和 developer CLI 已存在，但黑箱边界被错误写成永久能力限制，协作又硬绑 formal/application，auto-forward 不会自主执行。 | 已实现按 assignment 角色授权、跨软件 DevelopmentAssignment、manual/autonomous 双轴策略、独立工作区/工具/API/CLI、持久 dispatcher、扩权暂停、独立 review 和 stop/archive；T01K-L 已完成 scoped closure。r8 进一步把 Lilies 失败后 Codex fallback、Builder/developer 角色切换和暴露资格写入 final 证据边界。 | T01G Q24-Q28、T01K/T01L evidence、Lilies 失败证据与 contract r8。 | M-N 尚未完成；T01H fresh-final 分母为 0/6。 |
| `ARCH-003` | 能力放置原则已有报告约束。 | 共用 agent core、正式 collaboration 与独立 collaborative-development modules 已分离；T01K 又验证独立发行拥有 daemon/CLI/state/provider/桌面，平台只拥有 discovery、bridge/client，跨仓库不 import 或共享数据库/token；T01L 已把 executor、tool、watchdog 与 reconciliation carrier 落在独立仓库。 | T01A-G 架构证据、T01K import scan/发行/真实 loopback bridge/安全复审，以及 T01L `manifest.json`。 | M-N 的跨平台 runtime 与 H-J 仍未闭环。 |
| `EVAL-003` | 有生成式验收和合同测试。 | T01F 已实现可重放题包与独立 verifier；T01G 完成 Q01-Q28；T01K-L 完成各自 scoped closure。旧项目 2–6 的受保护 Seed 证据和项目一中间结果均保留，但不能替代 r8 fresh-final 六项目。 | `docs/evidence/v0.4.13/t01f/`、`t01g/`、`t01k/`、`t01l/`、T01H 历史 evidence 与 contract r8。 | 六个 fresh-final 项目、M-N 和 J 发布门仍待完成；最终 verifier 与 Closure reviewer 必须使用未暴露受保护内容的新上下文。 |
| `GOV-002` | 平台已有 input/output/cached/reasoning Token、费用与预算观测，T01H 又增加 Provider 默认断路和 stage/model 快照。 | T01K 的独立 daemon 以全局 receipt 记录 attempted/recorded/unknown、session/stage/model 与成本；历史 123 次聚合调用只标 unknown，不估算。平台监控合并 ACL 明细与全局 unattributed remainder，真实启动/空闲出口关闭且零增量。T01L 又把 DevelopmentAssignment 每次模型调用按 stage/model 预留并持久结算，未知用量强制 reconciliation，确定性 fixture 精确记录 24 与 87 tokens，付费调用为 0。 | `runtime-bridge-and-observability.json`、`security-and-token-audit.json`、T01L `verification.json` 与最终 Token monitor 回归。 | M-N 的项目级账本仍待分别闭环；不覆盖 Codex/OpenAI 账户级账单或 live Provider 成本。 |
| `SCENARIO-004` | 真实检测/诊断场景未验收。 | ThingsBoard 项目锁定 telemetry、模型、threshold、人工 review、alarm 和 drift oracle。 | EXP-LILIES-004 manifest。 | revision 1 与真实模型运行未开始。 |
| `SCENARIO-005` | 预测与 typed delivery 场景未验收。 | Actual 项目已交付强类型 JSON/XLSX 和官方 CLI 写回；ERPNext 项目已交付预测、计划和草稿回执三类 JSON。 | EXP-LILIES-005/006 manifest、报告与受保护 Seed evidence。 | 生产租户、长期运行和客户正式签收仍属证据债务。 |
| `SCENARIO-006` | 权限与引用 RAG 场景未验收。 | BookStack 项目通过内容权限、版本、引用、新鲜度与禁止来源集合定义独立验收。 | EXP-LILIES-002 manifest。 | 真实语料和 held-out query 运行未开始。 |
| `SCENARIO-007` | 预测、优化和计划场景未验收。 | ERPNext 项目已完成时间切分训练/回测、不可变版本、提升/部署、推理、重训提示、MOQ/批量/容量/预算规划、不可行解释、人工 review 和幂等草稿写回。 | EXP-LILIES-006 manifest、version 1、公开 3/3、三个 Seed 48/48 与新应用 16/16。 | 声明仅覆盖受控本地周期数据和 bounded solver，不外推到生产预测精度或大规模求解。 |
| `SCENARIO-008` | 文档抽取、校验、对账与写回题已冻结但未通过。 | 项目一公开题包、历史失败和当前复用应用中间 version 均保留；后者已证明部分公开业务链和 acceptance harness，但因不是全新空应用而不进入 r8 final 分母。 | 项目一题包、公开平台 transcript、中间 version eligibility 与 T01H evidence。 | r8 fresh-final 项目一尚未由新的隔离 Codex context 从空应用完成 debug + 三 protected Seed。 |
| `SCENARIO-009` | 持续监控与受控调整场景未验收。 | Home Assistant 和 ThingsBoard 项目已分别通过事件恢复/去重与模型漂移/审批/回滚 Seed；ERPNext 又验证预测漂移只给出重训建议而不在线训练。 | EXP-LILIES-003/004/006 manifest、报告与 Seed evidence。 | 不声明长期生产 SLO 或真实工厂模型精度。 |
| `SCENARIO-010` | 个人/小团队泛化未独立报告真实结果。 | Home Assistant 与 Actual 被明确放入单独 cohort，绝不混入企业分母。 | portfolio `cohort_policy`。 | 两项目结果尚未形成。 |
| `SCENARIO-011` | 真实宿主增补/替换资格已有早期设计，没有组合级客户结果。 | 六项目均声明真实宿主、官方 interface、augmentation/replacement 边界和独立 oracle；接口适配仍由平台负责。 | 六个 manifest、历史宿主资格表和 portfolio validator。 | 每个项目仍须零实验者 adapter 运行；正确性通过后才能比较性能。 |

来源任务集、阶段合同和本表之间，任何来源意图都不允许无故消失。

## 实验与产品状态更新

| 台账 / 界面 | 更新 | 证据 |
| --- | --- | --- |
| T01H 六项目组合 | 已锁定六个不同能力族项目，并将项目 ID 与题包 revision 分离：EXP-LILIES-001 的 1–13 是项目 1 的迭代；EXP-LILIES-002 至 006 各自从 revision 1 开始。执行严格逐个进行，企业项目 1/2/4/6 与泛化项目 3/5 分开统计；Home Assistant WebSocket → ThingsBoard MQTT → Actual SDK/CLI 顺序不变；通用补能进入 `CAP-LILIES` 非客户分母。 | `docs/experiments/lilies-collaboration/portfolio-v04-13-t01h.json`、六个 `project.json`、`platform-capability-gaps.json` 与 `scripts/validate_real_project_portfolio.py`。 |
| 本地莉莉丝 | T01A 独立 daemon/CLI 与真实模型重启、T01B assignment-scoped HTTP contract/client、T01C 平台自动创建/投递/恢复 BuildAssignment 均完成 scoped Closure Audit。浏览器路径仍保留诚实证据债务。 | `docs/evidence/v0.4.13/t01a/`、`t01b/`、`t01c/`。 |
| 正式管道与通用协同开发 | T01D 持久正式协议、审批/隔离、租约/回复、恢复、claim 和因果导出已完成确定性闭环；T01E 全局监控工作区、developer CLI、权限分离和 Customer Runtime 安全投影已完成 scoped closure；T01G 已完成平台中立 DevelopmentAssignment、manual/autonomous 交接、冻结授权、持久 dispatcher、独立 API/CLI、Q01-Q28、四类各 100 次故障注入与真实 Codex/DeepSeek 受限交接。普通任务仍不可发现任何协作能力，代码协同结果不进入企业分母。 | `docs/evidence/v0.4.13/t01d/`、`t01e/`、`t01g/`；T01G Audit 1 发现并修复 SQLite sidecar TOCTOU，Audit 2 `PASS`。 |
| 正式题包与隐藏验证 | T01F 的不可变 revision、认证 health、允许动作/预算、filtered workspace、完整 archive/replay、v1.1 claim、独立 verifier 与 stable seed 机制已完成 controlled-local Closure Audit；macOS sandbox 不等于跨平台/生产隔离。 | `docs/evidence/v0.4.13/t01f/`。 |
| EXP-LILIES-001 | 同级 LiliesAgent 的最后一次有界重跑以 assignment failure 结束，精确记录 21,051 tokens、八阶段且无工作流写入；随后 Codex 经公开平台 API 在复用应用上修正结果汇聚、发布 immutable version 并推动 acceptance harness 的真实接口闭环，但该应用不是 fresh empty，明确标记 `intermediate=true`、`eligible_for_final=false`。当前协调上下文又因受保护内容暴露不能承担 final Builder/verifier；r8 fresh-final 尚未开始。 | Lilies failure receipt、公开 API transcript、复用应用 version/eligibility、harness tests、incident record 与 contract r8。 |
| EXP-LILIES-002–006 | 旧 Codex 结果、各自题包、应用、公开测试、真实宿主回读、三个受保护 Seed、逐项目报告和补能证据全部保留为历史；它们不替代 r8 要求的 fresh isolated Codex fallback、全新应用/环境和同版本 debug + 三 Seed final 重跑。 | 旧逐项目 evidence、各 project manifest 与 contract r8。 |
| r6 独立莉莉丝与跨平台开发 | T01K-L 已在各自 scoped evidence floor 完成；T01M/N 均已接受但按 r8 在 T01H 后执行。四项固定 `enterprise_denominator=false`。 | `docs/evolution-control/stage-contracts/v0.4.13-r6.json`、T01K/L manifests 与 contract r8。 |

## 历史设计

| 历史设计 | 最终状态 | 证据 |
| --- | --- | --- |
| 无 | v0.4.13 expanded plan 仍是当前 active design。 | `docs/current-design/v0.4.13_lilies_local_agent_and_collaboration_pipeline_expanded_plan.md`。 |

## 工作记录归档

| 归档 | 内容 |
| --- | --- |
| `docs/evidence/v0.4.13/t01a/` | T01A 聚焦/全量结果、独立 CLI transcript、真实模型、恢复、架构边界与 SHA manifest；不包含 API key、access token 或未脱敏 pairing code。 |
| `docs/evidence/v0.4.13/t01b/` | T01B 合同/授权/安全/artifact、独立 HTTP Build/修复、精确回归、只读审计与 SHA manifest；不包含 task token、API key、随机资源 ID 或平台数据库。 |
| `docs/evidence/v0.4.13/t01c/` | T01C 确定性/真实 ASGI/独立进程 HTTP、terminal relay/ack、restart/unavailable/reconnect、安全边界、Studio 环境债务和只读审计；不包含 bearer、pairing code、task token、envelope key 或平台数据库。 |
| `docs/evidence/v0.4.13/t01d/` | T01D 147 项聚焦、1243 项全仓、100 次重连/幂等/租约故障、真实 HTTP commit/gitblob 正反路径、32 项压缩/恢复反例、安全边界和独立只读审计；不包含 bearer、token、平台数据库、私有推理或隐藏 oracle。 |
| `docs/evidence/v0.4.13/t01e/` | T01E 37 项聚焦、433 项广泛回归、25 项前端、1302 项全仓、TypeScript/build、developer CLI、Studio/Customer Runtime 安全 HTTP 投影、受控四状态 fixture、浏览器环境债务、Audit 1 证据完整性失败及刷新后的 Audit 2 `PASS`；不包含 bearer、token、平台数据库、私有推理、伪造截图或隐藏 oracle。 |
| `docs/evidence/v0.4.13/t01f/` | T01F 342 项提交后聚焦、830 项 v0.4.13、1668 项全仓、题包/环境/workspace、archive/verifier、CLI、安全边界、controlled-local claim ceiling、全新上下文独立审计和 SHA manifest；不包含 attestation secret、seed key、bearer、平台数据库、protected oracle、私有推理或伪造企业结果。 |
| `docs/evidence/v0.4.13/t01g/` | T01G Q01-Q28 28/28、四类各 100 次故障注入、平台中立 API/CLI、非 Lilies fixture、manual/autonomous 持久历史、冻结 grant、真实 Codex 实现与 DeepSeek 莉莉丝独立 review、SQLite sidecar TOCTOU 修复、208 项聚焦、1832 项全仓和第二个全新上下文 Audit 2 `PASS`；浏览器证据保留 `blocked_by_environment` 与 claim ceiling，全部结果标记 `enterprise_denominator=false`，不包含凭据、私有推理、隐藏 oracle 或伪造企业结果。 |
| `docs/evidence/v0.4.13/t01h/` | T01H 保留全部历史 Lilies/Codex revision、失败、旧项目 2–6 成果和当前复用应用项目一中间 evidence；r8 另存 Lilies 有界失败、受保护内容暴露/角色禁用、六个 fresh isolated Codex fallback attempt、公开 allowlist/API 工作链、exact 或 unknown usage、八阶段、同一不可变版本的 debug/三个受保护 Seed及统一报告。历史和中间证据不改写，也不计作 r8 final 通过。 |
| `docs/evidence/v0.4.13/t01k/` | T01K 两仓库提交、独立发行与隔离安装、Python/平台/Swift 最终回归、可复现 artifact、真实 discovery/pairing/bridge/replay、daemon-global Token/startup/idle 安全、legacy unknown 迁移、秘密边界、scoped claim ceiling 和 Audit 1 `PARTIAL` 后 Audit 2 `PASS`；不包含 bootstrap/bearer/provider/envelope secret，不声明原生视觉、Developer ID/notarization、Codex 账户账单、T01L-N 或客户结果。 |
| `docs/evidence/v0.4.13/t01l/` | T01L 独立仓库提交 `fd0f1a4`、四角色分权、冻结 grant、FD-only 进程输入、文件/进程/模型 prepared-reconciliation-idempotency、确定性模型—工具 diff/test/usage、Python `322`、合同复核 `147`、跨进程 `2`、Swift `99`、Release/启动、零付费调用与全新 Closure Audit `PASS`；不包含 bearer、配对码、Provider Key、数据库或原始 transcript，不声明 live DeepSeek、跨平台、原生真人交互、企业结果或生产认证。 |

## 下一阶段任务集

| 任务 ID | 来源意图 ID 列表 | 任务 | 为什么现在做 | 闭环目标 | 强制 / 可选 |
| --- | --- | --- | --- | --- | --- |
| `无` | `无` | 当前版本的六项目 T01H、T01M-N、T01I 和 T01J 尚未完成，不能提前选择下一版本任务。 | r8 的当前顺序是 H→M→N→I→J，当前任务为 T01H；这不是下一版本选择。 | 完成当前锁定合同后，由最新阶段报告选择下一版本。 | 无 |

本表是下一阶段任务的唯一权威来源。每项任务必须使用稳定的任务 ID 和来源意图 ID；workingon 和 current-design 不能另行定义下一阶段任务。

## 归档提交

- 提交:
- 闭环验证器:
- 意图覆盖验证器:
- 当前 current-design 是否干净:
- 当前 workingon 是否干净:

## 自动演进交接

- 是否继续: 是
- 当前任务 ID: `V04-13-T01H`
- 下一版本: `无`
- 第一任务 ID: `无`
- 恢复所依据的阶段报告: `docs/stage-reports/v0.4.13_lilies_local_agent_and_collaboration_pipeline.md`
- 停止原因: r8 保留同级 LiliesAgent 的有界失败，并依据用户后续指令由 fresh isolated Codex fallback 只经公开材料白名单和平台公开 API 从六个全新空应用/环境完成 final 重跑；当前协调上下文因受保护内容暴露、当前项目一因复用应用都不能计作 final。每个项目仍须让同一不可变发布版本分别通过 public debug 与三个 protected seeds，Codex usage 只记 exact authoritative counter 或 unknown/unavailable，八阶段完整记录。T01H 完成后按 M→N→I→J 继续。
